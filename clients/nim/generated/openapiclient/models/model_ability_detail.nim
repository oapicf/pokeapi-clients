#
# PokéAPI
# 
# All the Pokémon data you'll ever need in one place, easily accessible through a modern free open-source RESTful API.  ## What is this?  This is a full RESTful API linked to an extensive database detailing everything about the Pokémon main game series.  We've covered everything from Pokémon to Berry Flavors.  ## Where do I start?  We have awesome [documentation](https://pokeapi.co/docs/v2) on how to use this API. It takes minutes to get started.  This API will always be publicly available and will never require any extensive setup process to consume.  Created by [**Paul Hallett**(]https://github.com/phalt) and other [**PokéAPI contributors***](https://github.com/PokeAPI/pokeapi#contributing) around the world. Pokémon and Pokémon character names are trademarks of Nintendo.     
# The version of the OpenAPI document: 2.7.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_ability_change
import model_ability_detail_pokemon_inner
import model_ability_effect_text
import model_ability_flavor_text
import model_ability_name
import model_generation_summary

type AbilityDetail* = object
  ## 
  id*: int
  name*: string
  isMainSeries*: Option[bool]
  generation*: GenerationSummary
  names*: seq[AbilityName]
  effectEntries*: seq[AbilityEffectText]
  effectChanges*: seq[AbilityChange]
  flavorTextEntries*: seq[AbilityFlavorText]
  pokemon*: seq[AbilityDetail_pokemon_inner]


# Custom JSON deserialization for AbilityDetail with custom field names
proc to*(node: JsonNode, T: typedesc[AbilityDetail]): AbilityDetail =
  result = AbilityDetail()
  if node.kind == JObject:
    if node.hasKey("id"):
      result.id = to(node["id"], int)
    if node.hasKey("name"):
      result.name = to(node["name"], string)
    if node.hasKey("is_main_series") and node["is_main_series"].kind != JNull:
      result.isMainSeries = some(to(node["is_main_series"], typeof(result.isMainSeries.get())))
    if node.hasKey("generation"):
      result.generation = to(node["generation"], GenerationSummary)
    if node.hasKey("names"):
      result.names = to(node["names"], seq[AbilityName])
    if node.hasKey("effect_entries"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["effect_entries"]
      if arrayNode.kind == JArray:
        result.effectEntries = @[]
        for item in arrayNode.items:
          result.effectEntries.add(to(item, AbilityEffectText))
    if node.hasKey("effect_changes"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["effect_changes"]
      if arrayNode.kind == JArray:
        result.effectChanges = @[]
        for item in arrayNode.items:
          result.effectChanges.add(to(item, AbilityChange))
    if node.hasKey("flavor_text_entries"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["flavor_text_entries"]
      if arrayNode.kind == JArray:
        result.flavorTextEntries = @[]
        for item in arrayNode.items:
          result.flavorTextEntries.add(to(item, AbilityFlavorText))
    if node.hasKey("pokemon"):
      result.pokemon = to(node["pokemon"], seq[AbilityDetail_pokemon_inner])

# Custom JSON serialization for AbilityDetail with custom field names
proc `%`*(obj: AbilityDetail): JsonNode =
  result = newJObject()
  result["id"] = %obj.id
  result["name"] = %obj.name
  if obj.isMainSeries.isSome():
    result["is_main_series"] = %obj.isMainSeries.get()
  result["generation"] = %obj.generation
  result["names"] = %obj.names
  result["effect_entries"] = %obj.effectEntries
  result["effect_changes"] = %obj.effectChanges
  result["flavor_text_entries"] = %obj.flavorTextEntries
  result["pokemon"] = %obj.pokemon

