#
# PokéAPI
# 
# All the Pokémon data you'll ever need in one place, easily accessible through a modern free open-source RESTful API.  ## What is this?  This is a full RESTful API linked to an extensive database detailing everything about the Pokémon main game series.  We've covered everything from Pokémon to Berry Flavors.  ## Where do I start?  We have awesome [documentation](https://pokeapi.co/docs/v2) on how to use this API. It takes minutes to get started.  This API will always be publicly available and will never require any extensive setup process to consume.  Created by [**Paul Hallett**(]https://github.com/phalt) and other [**PokéAPI contributors***](https://github.com/PokeAPI/pokeapi#contributing) around the world. Pokémon and Pokémon character names are trademarks of Nintendo.     
# The version of the OpenAPI document: 2.7.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_characteristic_summary
import model_move_damage_class_summary
import model_stat_detail_affecting_moves
import model_stat_detail_affecting_natures
import model_stat_name

type StatDetail* = object
  ## 
  id*: int
  name*: string
  gameIndex*: int
  isBattleOnly*: Option[bool]
  affectingMoves*: StatDetail_affecting_moves
  affectingNatures*: StatDetail_affecting_natures
  characteristics*: seq[CharacteristicSummary]
  moveDamageClass*: MoveDamageClassSummary
  names*: seq[StatName]


# Custom JSON deserialization for StatDetail with custom field names
proc to*(node: JsonNode, T: typedesc[StatDetail]): StatDetail =
  result = StatDetail()
  if node.kind == JObject:
    if node.hasKey("id"):
      result.id = to(node["id"], int)
    if node.hasKey("name"):
      result.name = to(node["name"], string)
    if node.hasKey("game_index"):
      result.gameIndex = to(node["game_index"], int)
    if node.hasKey("is_battle_only") and node["is_battle_only"].kind != JNull:
      result.isBattleOnly = some(to(node["is_battle_only"], typeof(result.isBattleOnly.get())))
    if node.hasKey("affecting_moves"):
      result.affectingMoves = to(node["affecting_moves"], StatDetail_affecting_moves)
    if node.hasKey("affecting_natures"):
      result.affectingNatures = to(node["affecting_natures"], StatDetail_affecting_natures)
    if node.hasKey("characteristics"):
      result.characteristics = to(node["characteristics"], seq[CharacteristicSummary])
    if node.hasKey("move_damage_class"):
      result.moveDamageClass = to(node["move_damage_class"], MoveDamageClassSummary)
    if node.hasKey("names"):
      result.names = to(node["names"], seq[StatName])

# Custom JSON serialization for StatDetail with custom field names
proc `%`*(obj: StatDetail): JsonNode =
  result = newJObject()
  result["id"] = %obj.id
  result["name"] = %obj.name
  result["game_index"] = %obj.gameIndex
  if obj.isBattleOnly.isSome():
    result["is_battle_only"] = %obj.isBattleOnly.get()
  result["affecting_moves"] = %obj.affectingMoves
  result["affecting_natures"] = %obj.affectingNatures
  result["characteristics"] = %obj.characteristics
  result["move_damage_class"] = %obj.moveDamageClass
  result["names"] = %obj.names

