#
# PokéAPI
# 
# All the Pokémon data you'll ever need in one place, easily accessible through a modern free open-source RESTful API.  ## What is this?  This is a full RESTful API linked to an extensive database detailing everything about the Pokémon main game series.  We've covered everything from Pokémon to Berry Flavors.  ## Where do I start?  We have awesome [documentation](https://pokeapi.co/docs/v2) on how to use this API. It takes minutes to get started.  This API will always be publicly available and will never require any extensive setup process to consume.  Created by [**Paul Hallett**(]https://github.com/phalt) and other [**PokéAPI contributors***](https://github.com/PokeAPI/pokeapi#contributing) around the world. Pokémon and Pokémon character names are trademarks of Nintendo.     
# The version of the OpenAPI document: 2.7.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_pokemon_detail_abilities_inner
import model_pokemon_detail_cries
import model_pokemon_detail_held_items
import model_pokemon_detail_moves_inner
import model_pokemon_detail_past_abilities_inner
import model_pokemon_detail_past_types_inner
import model_pokemon_detail_sprites
import model_pokemon_detail_types_inner
import model_pokemon_form_summary
import model_pokemon_game_index
import model_pokemon_species_summary
import model_pokemon_stat

type PokemonDetail* = object
  ## 
  id*: int
  name*: string
  baseExperience*: Option[int]
  height*: Option[int]
  isDefault*: Option[bool]
  order*: Option[int]
  weight*: Option[int]
  abilities*: seq[PokemonDetail_abilities_inner]
  pastAbilities*: seq[PokemonDetail_past_abilities_inner]
  forms*: seq[PokemonFormSummary]
  gameIndices*: seq[PokemonGameIndex]
  heldItems*: PokemonDetail_held_items
  locationAreaEncounters*: string
  moves*: seq[PokemonDetail_moves_inner]
  species*: PokemonSpeciesSummary
  sprites*: PokemonDetail_sprites
  cries*: PokemonDetail_cries
  stats*: seq[PokemonStat]
  types*: seq[PokemonDetail_types_inner]
  pastTypes*: seq[PokemonDetail_past_types_inner]


# Custom JSON deserialization for PokemonDetail with custom field names
proc to*(node: JsonNode, T: typedesc[PokemonDetail]): PokemonDetail =
  result = PokemonDetail()
  if node.kind == JObject:
    if node.hasKey("id"):
      result.id = to(node["id"], int)
    if node.hasKey("name"):
      result.name = to(node["name"], string)
    if node.hasKey("base_experience") and node["base_experience"].kind != JNull:
      result.baseExperience = some(to(node["base_experience"], typeof(result.baseExperience.get())))
    if node.hasKey("height") and node["height"].kind != JNull:
      result.height = some(to(node["height"], typeof(result.height.get())))
    if node.hasKey("is_default") and node["is_default"].kind != JNull:
      result.isDefault = some(to(node["is_default"], typeof(result.isDefault.get())))
    if node.hasKey("order") and node["order"].kind != JNull:
      result.order = some(to(node["order"], typeof(result.order.get())))
    if node.hasKey("weight") and node["weight"].kind != JNull:
      result.weight = some(to(node["weight"], typeof(result.weight.get())))
    if node.hasKey("abilities"):
      result.abilities = to(node["abilities"], seq[PokemonDetail_abilities_inner])
    if node.hasKey("past_abilities"):
      result.pastAbilities = to(node["past_abilities"], seq[PokemonDetail_past_abilities_inner])
    if node.hasKey("forms"):
      result.forms = to(node["forms"], seq[PokemonFormSummary])
    if node.hasKey("game_indices"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["game_indices"]
      if arrayNode.kind == JArray:
        result.gameIndices = @[]
        for item in arrayNode.items:
          result.gameIndices.add(to(item, PokemonGameIndex))
    if node.hasKey("held_items"):
      result.heldItems = to(node["held_items"], PokemonDetail_held_items)
    if node.hasKey("location_area_encounters"):
      result.locationAreaEncounters = to(node["location_area_encounters"], string)
    if node.hasKey("moves"):
      result.moves = to(node["moves"], seq[PokemonDetail_moves_inner])
    if node.hasKey("species"):
      result.species = to(node["species"], PokemonSpeciesSummary)
    if node.hasKey("sprites"):
      result.sprites = to(node["sprites"], PokemonDetail_sprites)
    if node.hasKey("cries"):
      result.cries = to(node["cries"], PokemonDetail_cries)
    if node.hasKey("stats"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["stats"]
      if arrayNode.kind == JArray:
        result.stats = @[]
        for item in arrayNode.items:
          result.stats.add(to(item, PokemonStat))
    if node.hasKey("types"):
      result.types = to(node["types"], seq[PokemonDetail_types_inner])
    if node.hasKey("past_types"):
      result.pastTypes = to(node["past_types"], seq[PokemonDetail_past_types_inner])

# Custom JSON serialization for PokemonDetail with custom field names
proc `%`*(obj: PokemonDetail): JsonNode =
  result = newJObject()
  result["id"] = %obj.id
  result["name"] = %obj.name
  if obj.baseExperience.isSome():
    result["base_experience"] = %obj.baseExperience.get()
  if obj.height.isSome():
    result["height"] = %obj.height.get()
  if obj.isDefault.isSome():
    result["is_default"] = %obj.isDefault.get()
  if obj.order.isSome():
    result["order"] = %obj.order.get()
  if obj.weight.isSome():
    result["weight"] = %obj.weight.get()
  result["abilities"] = %obj.abilities
  result["past_abilities"] = %obj.pastAbilities
  result["forms"] = %obj.forms
  result["game_indices"] = %obj.gameIndices
  result["held_items"] = %obj.heldItems
  result["location_area_encounters"] = %obj.locationAreaEncounters
  result["moves"] = %obj.moves
  result["species"] = %obj.species
  result["sprites"] = %obj.sprites
  result["cries"] = %obj.cries
  result["stats"] = %obj.stats
  result["types"] = %obj.types
  result["past_types"] = %obj.pastTypes

