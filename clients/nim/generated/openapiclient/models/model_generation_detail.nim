#
# PokéAPI
# 
# All the Pokémon data you'll ever need in one place, easily accessible through a modern free open-source RESTful API.  ## What is this?  This is a full RESTful API linked to an extensive database detailing everything about the Pokémon main game series.  We've covered everything from Pokémon to Berry Flavors.  ## Where do I start?  We have awesome [documentation](https://pokeapi.co/docs/v2) on how to use this API. It takes minutes to get started.  This API will always be publicly available and will never require any extensive setup process to consume.  Created by [**Paul Hallett**(]https://github.com/phalt) and other [**PokéAPI contributors***](https://github.com/PokeAPI/pokeapi#contributing) around the world. Pokémon and Pokémon character names are trademarks of Nintendo.     
# The version of the OpenAPI document: 2.7.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_ability_summary
import model_generation_name
import model_move_summary
import model_pokemon_species_summary
import model_region_summary
import model_type_summary
import model_version_group_summary

type GenerationDetail* = object
  ## 
  id*: int
  name*: string
  abilities*: seq[AbilitySummary]
  mainRegion*: RegionSummary
  moves*: seq[MoveSummary]
  names*: seq[GenerationName]
  pokemonSpecies*: seq[PokemonSpeciesSummary]
  types*: seq[TypeSummary]
  versionGroups*: seq[VersionGroupSummary]


# Custom JSON deserialization for GenerationDetail with custom field names
proc to*(node: JsonNode, T: typedesc[GenerationDetail]): GenerationDetail =
  result = GenerationDetail()
  if node.kind == JObject:
    if node.hasKey("id"):
      result.id = to(node["id"], int)
    if node.hasKey("name"):
      result.name = to(node["name"], string)
    if node.hasKey("abilities"):
      result.abilities = to(node["abilities"], seq[AbilitySummary])
    if node.hasKey("main_region"):
      result.mainRegion = to(node["main_region"], RegionSummary)
    if node.hasKey("moves"):
      result.moves = to(node["moves"], seq[MoveSummary])
    if node.hasKey("names"):
      result.names = to(node["names"], seq[GenerationName])
    if node.hasKey("pokemon_species"):
      result.pokemonSpecies = to(node["pokemon_species"], seq[PokemonSpeciesSummary])
    if node.hasKey("types"):
      result.types = to(node["types"], seq[TypeSummary])
    if node.hasKey("version_groups"):
      result.versionGroups = to(node["version_groups"], seq[VersionGroupSummary])

# Custom JSON serialization for GenerationDetail with custom field names
proc `%`*(obj: GenerationDetail): JsonNode =
  result = newJObject()
  result["id"] = %obj.id
  result["name"] = %obj.name
  result["abilities"] = %obj.abilities
  result["main_region"] = %obj.mainRegion
  result["moves"] = %obj.moves
  result["names"] = %obj.names
  result["pokemon_species"] = %obj.pokemonSpecies
  result["types"] = %obj.types
  result["version_groups"] = %obj.versionGroups

