#
# PokéAPI
# 
# All the Pokémon data you'll ever need in one place, easily accessible through a modern free open-source RESTful API.  ## What is this?  This is a full RESTful API linked to an extensive database detailing everything about the Pokémon main game series.  We've covered everything from Pokémon to Berry Flavors.  ## Where do I start?  We have awesome [documentation](https://pokeapi.co/docs/v2) on how to use this API. It takes minutes to get started.  This API will always be publicly available and will never require any extensive setup process to consume.  Created by [**Paul Hallett**(]https://github.com/phalt) and other [**PokéAPI contributors***](https://github.com/PokeAPI/pokeapi#contributing) around the world. Pokémon and Pokémon character names are trademarks of Nintendo.     
# The version of the OpenAPI document: 2.7.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_location_area_summary
import model_location_game_index
import model_location_name
import model_region_summary

type LocationDetail* = object
  ## 
  id*: int
  name*: string
  region*: RegionSummary
  names*: seq[LocationName]
  gameIndices*: seq[LocationGameIndex]
  areas*: seq[LocationAreaSummary]


# Custom JSON deserialization for LocationDetail with custom field names
proc to*(node: JsonNode, T: typedesc[LocationDetail]): LocationDetail =
  result = LocationDetail()
  if node.kind == JObject:
    if node.hasKey("id"):
      result.id = to(node["id"], int)
    if node.hasKey("name"):
      result.name = to(node["name"], string)
    if node.hasKey("region"):
      result.region = to(node["region"], RegionSummary)
    if node.hasKey("names"):
      result.names = to(node["names"], seq[LocationName])
    if node.hasKey("game_indices"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["game_indices"]
      if arrayNode.kind == JArray:
        result.gameIndices = @[]
        for item in arrayNode.items:
          result.gameIndices.add(to(item, LocationGameIndex))
    if node.hasKey("areas"):
      result.areas = to(node["areas"], seq[LocationAreaSummary])

# Custom JSON serialization for LocationDetail with custom field names
proc `%`*(obj: LocationDetail): JsonNode =
  result = newJObject()
  result["id"] = %obj.id
  result["name"] = %obj.name
  result["region"] = %obj.region
  result["names"] = %obj.names
  result["game_indices"] = %obj.gameIndices
  result["areas"] = %obj.areas

