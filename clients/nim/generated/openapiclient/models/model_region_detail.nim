#
# PokéAPI
# 
# All the Pokémon data you'll ever need in one place, easily accessible through a modern free open-source RESTful API.  ## What is this?  This is a full RESTful API linked to an extensive database detailing everything about the Pokémon main game series.  We've covered everything from Pokémon to Berry Flavors.  ## Where do I start?  We have awesome [documentation](https://pokeapi.co/docs/v2) on how to use this API. It takes minutes to get started.  This API will always be publicly available and will never require any extensive setup process to consume.  Created by [**Paul Hallett**(]https://github.com/phalt) and other [**PokéAPI contributors***](https://github.com/PokeAPI/pokeapi#contributing) around the world. Pokémon and Pokémon character names are trademarks of Nintendo.     
# The version of the OpenAPI document: 2.7.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_ability_detail_pokemon_inner_pokemon
import model_generation_summary
import model_location_summary
import model_pokedex_summary
import model_region_name

type RegionDetail* = object
  ## 
  id*: int
  name*: string
  locations*: seq[LocationSummary]
  mainGeneration*: GenerationSummary
  names*: seq[RegionName]
  pokedexes*: seq[PokedexSummary]
  versionGroups*: seq[AbilityDetail_pokemon_inner_pokemon]


# Custom JSON deserialization for RegionDetail with custom field names
proc to*(node: JsonNode, T: typedesc[RegionDetail]): RegionDetail =
  result = RegionDetail()
  if node.kind == JObject:
    if node.hasKey("id"):
      result.id = to(node["id"], int)
    if node.hasKey("name"):
      result.name = to(node["name"], string)
    if node.hasKey("locations"):
      result.locations = to(node["locations"], seq[LocationSummary])
    if node.hasKey("main_generation"):
      result.mainGeneration = to(node["main_generation"], GenerationSummary)
    if node.hasKey("names"):
      result.names = to(node["names"], seq[RegionName])
    if node.hasKey("pokedexes"):
      result.pokedexes = to(node["pokedexes"], seq[PokedexSummary])
    if node.hasKey("version_groups"):
      result.versionGroups = to(node["version_groups"], seq[AbilityDetail_pokemon_inner_pokemon])

# Custom JSON serialization for RegionDetail with custom field names
proc `%`*(obj: RegionDetail): JsonNode =
  result = newJObject()
  result["id"] = %obj.id
  result["name"] = %obj.name
  result["locations"] = %obj.locations
  result["main_generation"] = %obj.mainGeneration
  result["names"] = %obj.names
  result["pokedexes"] = %obj.pokedexes
  result["version_groups"] = %obj.versionGroups

