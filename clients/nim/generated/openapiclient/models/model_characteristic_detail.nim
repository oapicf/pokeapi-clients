#
# PokéAPI
# 
# All the Pokémon data you'll ever need in one place, easily accessible through a modern free open-source RESTful API.  ## What is this?  This is a full RESTful API linked to an extensive database detailing everything about the Pokémon main game series.  We've covered everything from Pokémon to Berry Flavors.  ## Where do I start?  We have awesome [documentation](https://pokeapi.co/docs/v2) on how to use this API. It takes minutes to get started.  This API will always be publicly available and will never require any extensive setup process to consume.  Created by [**Paul Hallett**(]https://github.com/phalt) and other [**PokéAPI contributors***](https://github.com/PokeAPI/pokeapi#contributing) around the world. Pokémon and Pokémon character names are trademarks of Nintendo.     
# The version of the OpenAPI document: 2.7.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_characteristic_description
import model_stat_summary

type CharacteristicDetail* = object
  ## 
  id*: int
  geneModulo*: int
  possibleValues*: seq[int]
  highestStat*: StatSummary
  descriptions*: seq[CharacteristicDescription]


# Custom JSON deserialization for CharacteristicDetail with custom field names
proc to*(node: JsonNode, T: typedesc[CharacteristicDetail]): CharacteristicDetail =
  result = CharacteristicDetail()
  if node.kind == JObject:
    if node.hasKey("id"):
      result.id = to(node["id"], int)
    if node.hasKey("gene_modulo"):
      result.geneModulo = to(node["gene_modulo"], int)
    if node.hasKey("possible_values"):
      result.possibleValues = to(node["possible_values"], seq[int])
    if node.hasKey("highest_stat"):
      result.highestStat = to(node["highest_stat"], StatSummary)
    if node.hasKey("descriptions"):
      result.descriptions = to(node["descriptions"], seq[CharacteristicDescription])

# Custom JSON serialization for CharacteristicDetail with custom field names
proc `%`*(obj: CharacteristicDetail): JsonNode =
  result = newJObject()
  result["id"] = %obj.id
  result["gene_modulo"] = %obj.geneModulo
  result["possible_values"] = %obj.possibleValues
  result["highest_stat"] = %obj.highestStat
  result["descriptions"] = %obj.descriptions

