#
# PokéAPI
# 
# All the Pokémon data you'll ever need in one place, easily accessible through a modern free open-source RESTful API.  ## What is this?  This is a full RESTful API linked to an extensive database detailing everything about the Pokémon main game series.  We've covered everything from Pokémon to Berry Flavors.  ## Where do I start?  We have awesome [documentation](https://pokeapi.co/docs/v2) on how to use this API. It takes minutes to get started.  This API will always be publicly available and will never require any extensive setup process to consume.  Created by [**Paul Hallett**(]https://github.com/phalt) and other [**PokéAPI contributors***](https://github.com/PokeAPI/pokeapi#contributing) around the world. Pokémon and Pokémon character names are trademarks of Nintendo.     
# The version of the OpenAPI document: 2.7.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_berry_detail_flavors_inner
import model_berry_firmness_summary
import model_item_summary
import model_type_summary

type BerryDetail* = object
  ## 
  id*: int
  name*: string
  growthTime*: int
  maxHarvest*: int
  naturalGiftPower*: int
  size*: int
  smoothness*: int
  soilDryness*: int
  firmness*: BerryFirmnessSummary
  flavors*: seq[BerryDetail_flavors_inner]
  item*: ItemSummary
  naturalGiftType*: TypeSummary


# Custom JSON deserialization for BerryDetail with custom field names
proc to*(node: JsonNode, T: typedesc[BerryDetail]): BerryDetail =
  result = BerryDetail()
  if node.kind == JObject:
    if node.hasKey("id"):
      result.id = to(node["id"], int)
    if node.hasKey("name"):
      result.name = to(node["name"], string)
    if node.hasKey("growth_time"):
      result.growthTime = to(node["growth_time"], int)
    if node.hasKey("max_harvest"):
      result.maxHarvest = to(node["max_harvest"], int)
    if node.hasKey("natural_gift_power"):
      result.naturalGiftPower = to(node["natural_gift_power"], int)
    if node.hasKey("size"):
      result.size = to(node["size"], int)
    if node.hasKey("smoothness"):
      result.smoothness = to(node["smoothness"], int)
    if node.hasKey("soil_dryness"):
      result.soilDryness = to(node["soil_dryness"], int)
    if node.hasKey("firmness"):
      result.firmness = to(node["firmness"], BerryFirmnessSummary)
    if node.hasKey("flavors"):
      result.flavors = to(node["flavors"], seq[BerryDetail_flavors_inner])
    if node.hasKey("item"):
      result.item = to(node["item"], ItemSummary)
    if node.hasKey("natural_gift_type"):
      result.naturalGiftType = to(node["natural_gift_type"], TypeSummary)

# Custom JSON serialization for BerryDetail with custom field names
proc `%`*(obj: BerryDetail): JsonNode =
  result = newJObject()
  result["id"] = %obj.id
  result["name"] = %obj.name
  result["growth_time"] = %obj.growthTime
  result["max_harvest"] = %obj.maxHarvest
  result["natural_gift_power"] = %obj.naturalGiftPower
  result["size"] = %obj.size
  result["smoothness"] = %obj.smoothness
  result["soil_dryness"] = %obj.soilDryness
  result["firmness"] = %obj.firmness
  result["flavors"] = %obj.flavors
  result["item"] = %obj.item
  result["natural_gift_type"] = %obj.naturalGiftType

