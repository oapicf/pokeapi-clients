#
# PokéAPI
# 
# All the Pokémon data you'll ever need in one place, easily accessible through a modern free open-source RESTful API.  ## What is this?  This is a full RESTful API linked to an extensive database detailing everything about the Pokémon main game series.  We've covered everything from Pokémon to Berry Flavors.  ## Where do I start?  We have awesome [documentation](https://pokeapi.co/docs/v2) on how to use this API. It takes minutes to get started.  This API will always be publicly available and will never require any extensive setup process to consume.  Created by [**Paul Hallett**(]https://github.com/phalt) and other [**PokéAPI contributors***](https://github.com/PokeAPI/pokeapi#contributing) around the world. Pokémon and Pokémon character names are trademarks of Nintendo.     
# The version of the OpenAPI document: 2.7.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_berry_flavor_summary
import model_berry_summary
import model_nature_battle_style_preference
import model_nature_detail_pokeathlon_stat_changes_inner
import model_nature_name
import model_stat_summary

type NatureDetail* = object
  ## 
  id*: int
  name*: string
  decreasedStat*: StatSummary
  increasedStat*: StatSummary
  likesFlavor*: BerryFlavorSummary
  hatesFlavor*: BerryFlavorSummary
  berries*: seq[BerrySummary]
  pokeathlonStatChanges*: seq[NatureDetail_pokeathlon_stat_changes_inner]
  moveBattleStylePreferences*: seq[NatureBattleStylePreference]
  names*: seq[NatureName]


# Custom JSON deserialization for NatureDetail with custom field names
proc to*(node: JsonNode, T: typedesc[NatureDetail]): NatureDetail =
  result = NatureDetail()
  if node.kind == JObject:
    if node.hasKey("id"):
      result.id = to(node["id"], int)
    if node.hasKey("name"):
      result.name = to(node["name"], string)
    if node.hasKey("decreased_stat"):
      result.decreasedStat = to(node["decreased_stat"], StatSummary)
    if node.hasKey("increased_stat"):
      result.increasedStat = to(node["increased_stat"], StatSummary)
    if node.hasKey("likes_flavor"):
      result.likesFlavor = to(node["likes_flavor"], BerryFlavorSummary)
    if node.hasKey("hates_flavor"):
      result.hatesFlavor = to(node["hates_flavor"], BerryFlavorSummary)
    if node.hasKey("berries"):
      result.berries = to(node["berries"], seq[BerrySummary])
    if node.hasKey("pokeathlon_stat_changes"):
      result.pokeathlonStatChanges = to(node["pokeathlon_stat_changes"], seq[NatureDetail_pokeathlon_stat_changes_inner])
    if node.hasKey("move_battle_style_preferences"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["move_battle_style_preferences"]
      if arrayNode.kind == JArray:
        result.moveBattleStylePreferences = @[]
        for item in arrayNode.items:
          result.moveBattleStylePreferences.add(to(item, NatureBattleStylePreference))
    if node.hasKey("names"):
      result.names = to(node["names"], seq[NatureName])

# Custom JSON serialization for NatureDetail with custom field names
proc `%`*(obj: NatureDetail): JsonNode =
  result = newJObject()
  result["id"] = %obj.id
  result["name"] = %obj.name
  result["decreased_stat"] = %obj.decreasedStat
  result["increased_stat"] = %obj.increasedStat
  result["likes_flavor"] = %obj.likesFlavor
  result["hates_flavor"] = %obj.hatesFlavor
  result["berries"] = %obj.berries
  result["pokeathlon_stat_changes"] = %obj.pokeathlonStatChanges
  result["move_battle_style_preferences"] = %obj.moveBattleStylePreferences
  result["names"] = %obj.names

