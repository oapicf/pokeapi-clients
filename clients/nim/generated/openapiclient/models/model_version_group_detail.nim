#
# PokéAPI
# 
# All the Pokémon data you'll ever need in one place, easily accessible through a modern free open-source RESTful API.  ## What is this?  This is a full RESTful API linked to an extensive database detailing everything about the Pokémon main game series.  We've covered everything from Pokémon to Berry Flavors.  ## Where do I start?  We have awesome [documentation](https://pokeapi.co/docs/v2) on how to use this API. It takes minutes to get started.  This API will always be publicly available and will never require any extensive setup process to consume.  Created by [**Paul Hallett**(]https://github.com/phalt) and other [**PokéAPI contributors***](https://github.com/PokeAPI/pokeapi#contributing) around the world. Pokémon and Pokémon character names are trademarks of Nintendo.     
# The version of the OpenAPI document: 2.7.0
# Contact: blah+oapicf@cliffano.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_ability_detail_pokemon_inner_pokemon
import model_generation_summary
import model_version_summary

type VersionGroupDetail* = object
  ## 
  id*: int
  name*: string
  order*: Option[int]
  generation*: GenerationSummary
  moveLearnMethods*: seq[AbilityDetail_pokemon_inner_pokemon]
  pokedexes*: seq[AbilityDetail_pokemon_inner_pokemon]
  regions*: seq[AbilityDetail_pokemon_inner_pokemon]
  versions*: seq[VersionSummary]


# Custom JSON deserialization for VersionGroupDetail with custom field names
proc to*(node: JsonNode, T: typedesc[VersionGroupDetail]): VersionGroupDetail =
  result = VersionGroupDetail()
  if node.kind == JObject:
    if node.hasKey("id"):
      result.id = to(node["id"], int)
    if node.hasKey("name"):
      result.name = to(node["name"], string)
    if node.hasKey("order") and node["order"].kind != JNull:
      result.order = some(to(node["order"], typeof(result.order.get())))
    if node.hasKey("generation"):
      result.generation = to(node["generation"], GenerationSummary)
    if node.hasKey("move_learn_methods"):
      result.moveLearnMethods = to(node["move_learn_methods"], seq[AbilityDetail_pokemon_inner_pokemon])
    if node.hasKey("pokedexes"):
      result.pokedexes = to(node["pokedexes"], seq[AbilityDetail_pokemon_inner_pokemon])
    if node.hasKey("regions"):
      result.regions = to(node["regions"], seq[AbilityDetail_pokemon_inner_pokemon])
    if node.hasKey("versions"):
      result.versions = to(node["versions"], seq[VersionSummary])

# Custom JSON serialization for VersionGroupDetail with custom field names
proc `%`*(obj: VersionGroupDetail): JsonNode =
  result = newJObject()
  result["id"] = %obj.id
  result["name"] = %obj.name
  if obj.order.isSome():
    result["order"] = %obj.order.get()
  result["generation"] = %obj.generation
  result["move_learn_methods"] = %obj.moveLearnMethods
  result["pokedexes"] = %obj.pokedexes
  result["regions"] = %obj.regions
  result["versions"] = %obj.versions

