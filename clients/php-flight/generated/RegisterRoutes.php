<?php

/**
 * PokéAPI
 * PHP version 8.x
 *
 * @package OpenAPIServer
 * @author  OpenAPI Generator team
 * @link    https://github.com/openapitools/openapi-generator
 */

/**
 * All the Pokémon data you'll ever need in one place, easily accessible through a modern free open-source RESTful API.  ## What is this?  This is a full RESTful API linked to an extensive database detailing everything about the Pokémon main game series.  We've covered everything from Pokémon to Berry Flavors.  ## Where do I start?  We have awesome [documentation](https://pokeapi.co/docs/v2) on how to use this API. It takes minutes to get started.  This API will always be publicly available and will never require any extensive setup process to consume.  Created by [**Paul Hallett**(]https://github.com/phalt) and other [**PokéAPI contributors***](https://github.com/PokeAPI/pokeapi#contributing) around the world. Pokémon and Pokémon character names are trademarks of Nintendo.
 * The version of the OpenAPI document: 2.7.0
 * Contact: blah+oapicf@cliffano.com
 * Generated by: https://github.com/openapitools/openapi-generator.git
 */

namespace OpenAPIServer;

class RegisterRoutes {

    static public function registerRoutes(\OpenAPIServer\Api\AbstractBerriesApi|\OpenAPIServer\Api\AbstractContestsApi|\OpenAPIServer\Api\AbstractEncountersApi|\OpenAPIServer\Api\AbstractEvolutionApi|\OpenAPIServer\Api\AbstractGamesApi|\OpenAPIServer\Api\AbstractItemsApi|\OpenAPIServer\Api\AbstractLocationApi|\OpenAPIServer\Api\AbstractMachinesApi|\OpenAPIServer\Api\AbstractMovesApi|\OpenAPIServer\Api\AbstractPokemonApi|\OpenAPIServer\Api\AbstractUtilityApi $handler): void
    {
        $reflectionClass = new \ReflectionClass($handler);

        if (declaresMethod($reflectionClass, 'berryFirmnessList') && declaresMethod($reflectionClass, 'berryFirmnessListStream')) {
            throw new \Exception('Operation berryFirmnessList cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'berryFirmnessList')) {
            \Flight::route('GET /api/v2/berry-firmness/', function () use ($handler) {
                $r = \Flight::request();
                $result = $handler->berryFirmnessList(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'berryFirmnessListStream')) {
            \Flight::route('GET /api/v2/berry-firmness/', function () use ($handler) {
                $r = \Flight::request();
                $handler->berryFirmnessListStream(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'berryFirmnessRetrieve') && declaresMethod($reflectionClass, 'berryFirmnessRetrieveStream')) {
            throw new \Exception('Operation berryFirmnessRetrieve cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'berryFirmnessRetrieve')) {
            \Flight::route('GET /api/v2/berry-firmness/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $result = $handler->berryFirmnessRetrieve(
                    parseParam($id, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'berryFirmnessRetrieveStream')) {
            \Flight::route('GET /api/v2/berry-firmness/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $handler->berryFirmnessRetrieveStream(
                    parseParam($id, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'berryFlavorList') && declaresMethod($reflectionClass, 'berryFlavorListStream')) {
            throw new \Exception('Operation berryFlavorList cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'berryFlavorList')) {
            \Flight::route('GET /api/v2/berry-flavor/', function () use ($handler) {
                $r = \Flight::request();
                $result = $handler->berryFlavorList(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'berryFlavorListStream')) {
            \Flight::route('GET /api/v2/berry-flavor/', function () use ($handler) {
                $r = \Flight::request();
                $handler->berryFlavorListStream(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'berryFlavorRetrieve') && declaresMethod($reflectionClass, 'berryFlavorRetrieveStream')) {
            throw new \Exception('Operation berryFlavorRetrieve cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'berryFlavorRetrieve')) {
            \Flight::route('GET /api/v2/berry-flavor/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $result = $handler->berryFlavorRetrieve(
                    parseParam($id, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'berryFlavorRetrieveStream')) {
            \Flight::route('GET /api/v2/berry-flavor/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $handler->berryFlavorRetrieveStream(
                    parseParam($id, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'berryList') && declaresMethod($reflectionClass, 'berryListStream')) {
            throw new \Exception('Operation berryList cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'berryList')) {
            \Flight::route('GET /api/v2/berry/', function () use ($handler) {
                $r = \Flight::request();
                $result = $handler->berryList(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'berryListStream')) {
            \Flight::route('GET /api/v2/berry/', function () use ($handler) {
                $r = \Flight::request();
                $handler->berryListStream(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'berryRetrieve') && declaresMethod($reflectionClass, 'berryRetrieveStream')) {
            throw new \Exception('Operation berryRetrieve cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'berryRetrieve')) {
            \Flight::route('GET /api/v2/berry/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $result = $handler->berryRetrieve(
                    parseParam($id, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'berryRetrieveStream')) {
            \Flight::route('GET /api/v2/berry/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $handler->berryRetrieveStream(
                    parseParam($id, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'contestEffectList') && declaresMethod($reflectionClass, 'contestEffectListStream')) {
            throw new \Exception('Operation contestEffectList cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'contestEffectList')) {
            \Flight::route('GET /api/v2/contest-effect/', function () use ($handler) {
                $r = \Flight::request();
                $result = $handler->contestEffectList(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'contestEffectListStream')) {
            \Flight::route('GET /api/v2/contest-effect/', function () use ($handler) {
                $r = \Flight::request();
                $handler->contestEffectListStream(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'contestEffectRetrieve') && declaresMethod($reflectionClass, 'contestEffectRetrieveStream')) {
            throw new \Exception('Operation contestEffectRetrieve cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'contestEffectRetrieve')) {
            \Flight::route('GET /api/v2/contest-effect/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $result = $handler->contestEffectRetrieve(
                    parseParam($id, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'contestEffectRetrieveStream')) {
            \Flight::route('GET /api/v2/contest-effect/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $handler->contestEffectRetrieveStream(
                    parseParam($id, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'contestTypeList') && declaresMethod($reflectionClass, 'contestTypeListStream')) {
            throw new \Exception('Operation contestTypeList cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'contestTypeList')) {
            \Flight::route('GET /api/v2/contest-type/', function () use ($handler) {
                $r = \Flight::request();
                $result = $handler->contestTypeList(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'contestTypeListStream')) {
            \Flight::route('GET /api/v2/contest-type/', function () use ($handler) {
                $r = \Flight::request();
                $handler->contestTypeListStream(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'contestTypeRetrieve') && declaresMethod($reflectionClass, 'contestTypeRetrieveStream')) {
            throw new \Exception('Operation contestTypeRetrieve cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'contestTypeRetrieve')) {
            \Flight::route('GET /api/v2/contest-type/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $result = $handler->contestTypeRetrieve(
                    parseParam($id, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'contestTypeRetrieveStream')) {
            \Flight::route('GET /api/v2/contest-type/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $handler->contestTypeRetrieveStream(
                    parseParam($id, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'superContestEffectList') && declaresMethod($reflectionClass, 'superContestEffectListStream')) {
            throw new \Exception('Operation superContestEffectList cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'superContestEffectList')) {
            \Flight::route('GET /api/v2/super-contest-effect/', function () use ($handler) {
                $r = \Flight::request();
                $result = $handler->superContestEffectList(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'superContestEffectListStream')) {
            \Flight::route('GET /api/v2/super-contest-effect/', function () use ($handler) {
                $r = \Flight::request();
                $handler->superContestEffectListStream(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'superContestEffectRetrieve') && declaresMethod($reflectionClass, 'superContestEffectRetrieveStream')) {
            throw new \Exception('Operation superContestEffectRetrieve cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'superContestEffectRetrieve')) {
            \Flight::route('GET /api/v2/super-contest-effect/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $result = $handler->superContestEffectRetrieve(
                    parseParam($id, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'superContestEffectRetrieveStream')) {
            \Flight::route('GET /api/v2/super-contest-effect/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $handler->superContestEffectRetrieveStream(
                    parseParam($id, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'encounterConditionList') && declaresMethod($reflectionClass, 'encounterConditionListStream')) {
            throw new \Exception('Operation encounterConditionList cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'encounterConditionList')) {
            \Flight::route('GET /api/v2/encounter-condition/', function () use ($handler) {
                $r = \Flight::request();
                $result = $handler->encounterConditionList(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'encounterConditionListStream')) {
            \Flight::route('GET /api/v2/encounter-condition/', function () use ($handler) {
                $r = \Flight::request();
                $handler->encounterConditionListStream(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'encounterConditionRetrieve') && declaresMethod($reflectionClass, 'encounterConditionRetrieveStream')) {
            throw new \Exception('Operation encounterConditionRetrieve cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'encounterConditionRetrieve')) {
            \Flight::route('GET /api/v2/encounter-condition/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $result = $handler->encounterConditionRetrieve(
                    parseParam($id, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'encounterConditionRetrieveStream')) {
            \Flight::route('GET /api/v2/encounter-condition/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $handler->encounterConditionRetrieveStream(
                    parseParam($id, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'encounterConditionValueList') && declaresMethod($reflectionClass, 'encounterConditionValueListStream')) {
            throw new \Exception('Operation encounterConditionValueList cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'encounterConditionValueList')) {
            \Flight::route('GET /api/v2/encounter-condition-value/', function () use ($handler) {
                $r = \Flight::request();
                $result = $handler->encounterConditionValueList(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'encounterConditionValueListStream')) {
            \Flight::route('GET /api/v2/encounter-condition-value/', function () use ($handler) {
                $r = \Flight::request();
                $handler->encounterConditionValueListStream(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'encounterConditionValueRetrieve') && declaresMethod($reflectionClass, 'encounterConditionValueRetrieveStream')) {
            throw new \Exception('Operation encounterConditionValueRetrieve cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'encounterConditionValueRetrieve')) {
            \Flight::route('GET /api/v2/encounter-condition-value/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $result = $handler->encounterConditionValueRetrieve(
                    parseParam($id, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'encounterConditionValueRetrieveStream')) {
            \Flight::route('GET /api/v2/encounter-condition-value/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $handler->encounterConditionValueRetrieveStream(
                    parseParam($id, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'encounterMethodList') && declaresMethod($reflectionClass, 'encounterMethodListStream')) {
            throw new \Exception('Operation encounterMethodList cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'encounterMethodList')) {
            \Flight::route('GET /api/v2/encounter-method/', function () use ($handler) {
                $r = \Flight::request();
                $result = $handler->encounterMethodList(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'encounterMethodListStream')) {
            \Flight::route('GET /api/v2/encounter-method/', function () use ($handler) {
                $r = \Flight::request();
                $handler->encounterMethodListStream(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'encounterMethodRetrieve') && declaresMethod($reflectionClass, 'encounterMethodRetrieveStream')) {
            throw new \Exception('Operation encounterMethodRetrieve cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'encounterMethodRetrieve')) {
            \Flight::route('GET /api/v2/encounter-method/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $result = $handler->encounterMethodRetrieve(
                    parseParam($id, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'encounterMethodRetrieveStream')) {
            \Flight::route('GET /api/v2/encounter-method/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $handler->encounterMethodRetrieveStream(
                    parseParam($id, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'evolutionChainList') && declaresMethod($reflectionClass, 'evolutionChainListStream')) {
            throw new \Exception('Operation evolutionChainList cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'evolutionChainList')) {
            \Flight::route('GET /api/v2/evolution-chain/', function () use ($handler) {
                $r = \Flight::request();
                $result = $handler->evolutionChainList(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'evolutionChainListStream')) {
            \Flight::route('GET /api/v2/evolution-chain/', function () use ($handler) {
                $r = \Flight::request();
                $handler->evolutionChainListStream(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'evolutionChainRetrieve') && declaresMethod($reflectionClass, 'evolutionChainRetrieveStream')) {
            throw new \Exception('Operation evolutionChainRetrieve cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'evolutionChainRetrieve')) {
            \Flight::route('GET /api/v2/evolution-chain/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $result = $handler->evolutionChainRetrieve(
                    parseParam($id, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'evolutionChainRetrieveStream')) {
            \Flight::route('GET /api/v2/evolution-chain/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $handler->evolutionChainRetrieveStream(
                    parseParam($id, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'evolutionTriggerList') && declaresMethod($reflectionClass, 'evolutionTriggerListStream')) {
            throw new \Exception('Operation evolutionTriggerList cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'evolutionTriggerList')) {
            \Flight::route('GET /api/v2/evolution-trigger/', function () use ($handler) {
                $r = \Flight::request();
                $result = $handler->evolutionTriggerList(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'evolutionTriggerListStream')) {
            \Flight::route('GET /api/v2/evolution-trigger/', function () use ($handler) {
                $r = \Flight::request();
                $handler->evolutionTriggerListStream(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'evolutionTriggerRetrieve') && declaresMethod($reflectionClass, 'evolutionTriggerRetrieveStream')) {
            throw new \Exception('Operation evolutionTriggerRetrieve cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'evolutionTriggerRetrieve')) {
            \Flight::route('GET /api/v2/evolution-trigger/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $result = $handler->evolutionTriggerRetrieve(
                    parseParam($id, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'evolutionTriggerRetrieveStream')) {
            \Flight::route('GET /api/v2/evolution-trigger/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $handler->evolutionTriggerRetrieveStream(
                    parseParam($id, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'generationList') && declaresMethod($reflectionClass, 'generationListStream')) {
            throw new \Exception('Operation generationList cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'generationList')) {
            \Flight::route('GET /api/v2/generation/', function () use ($handler) {
                $r = \Flight::request();
                $result = $handler->generationList(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'generationListStream')) {
            \Flight::route('GET /api/v2/generation/', function () use ($handler) {
                $r = \Flight::request();
                $handler->generationListStream(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'generationRetrieve') && declaresMethod($reflectionClass, 'generationRetrieveStream')) {
            throw new \Exception('Operation generationRetrieve cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'generationRetrieve')) {
            \Flight::route('GET /api/v2/generation/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $result = $handler->generationRetrieve(
                    parseParam($id, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'generationRetrieveStream')) {
            \Flight::route('GET /api/v2/generation/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $handler->generationRetrieveStream(
                    parseParam($id, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'pokedexList') && declaresMethod($reflectionClass, 'pokedexListStream')) {
            throw new \Exception('Operation pokedexList cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'pokedexList')) {
            \Flight::route('GET /api/v2/pokedex/', function () use ($handler) {
                $r = \Flight::request();
                $result = $handler->pokedexList(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'pokedexListStream')) {
            \Flight::route('GET /api/v2/pokedex/', function () use ($handler) {
                $r = \Flight::request();
                $handler->pokedexListStream(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'pokedexRetrieve') && declaresMethod($reflectionClass, 'pokedexRetrieveStream')) {
            throw new \Exception('Operation pokedexRetrieve cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'pokedexRetrieve')) {
            \Flight::route('GET /api/v2/pokedex/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $result = $handler->pokedexRetrieve(
                    parseParam($id, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'pokedexRetrieveStream')) {
            \Flight::route('GET /api/v2/pokedex/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $handler->pokedexRetrieveStream(
                    parseParam($id, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'versionGroupList') && declaresMethod($reflectionClass, 'versionGroupListStream')) {
            throw new \Exception('Operation versionGroupList cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'versionGroupList')) {
            \Flight::route('GET /api/v2/version-group/', function () use ($handler) {
                $r = \Flight::request();
                $result = $handler->versionGroupList(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'versionGroupListStream')) {
            \Flight::route('GET /api/v2/version-group/', function () use ($handler) {
                $r = \Flight::request();
                $handler->versionGroupListStream(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'versionGroupRetrieve') && declaresMethod($reflectionClass, 'versionGroupRetrieveStream')) {
            throw new \Exception('Operation versionGroupRetrieve cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'versionGroupRetrieve')) {
            \Flight::route('GET /api/v2/version-group/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $result = $handler->versionGroupRetrieve(
                    parseParam($id, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'versionGroupRetrieveStream')) {
            \Flight::route('GET /api/v2/version-group/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $handler->versionGroupRetrieveStream(
                    parseParam($id, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'versionList') && declaresMethod($reflectionClass, 'versionListStream')) {
            throw new \Exception('Operation versionList cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'versionList')) {
            \Flight::route('GET /api/v2/version/', function () use ($handler) {
                $r = \Flight::request();
                $result = $handler->versionList(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'versionListStream')) {
            \Flight::route('GET /api/v2/version/', function () use ($handler) {
                $r = \Flight::request();
                $handler->versionListStream(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'versionRetrieve') && declaresMethod($reflectionClass, 'versionRetrieveStream')) {
            throw new \Exception('Operation versionRetrieve cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'versionRetrieve')) {
            \Flight::route('GET /api/v2/version/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $result = $handler->versionRetrieve(
                    parseParam($id, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'versionRetrieveStream')) {
            \Flight::route('GET /api/v2/version/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $handler->versionRetrieveStream(
                    parseParam($id, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'itemAttributeList') && declaresMethod($reflectionClass, 'itemAttributeListStream')) {
            throw new \Exception('Operation itemAttributeList cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'itemAttributeList')) {
            \Flight::route('GET /api/v2/item-attribute/', function () use ($handler) {
                $r = \Flight::request();
                $result = $handler->itemAttributeList(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'itemAttributeListStream')) {
            \Flight::route('GET /api/v2/item-attribute/', function () use ($handler) {
                $r = \Flight::request();
                $handler->itemAttributeListStream(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'itemAttributeRetrieve') && declaresMethod($reflectionClass, 'itemAttributeRetrieveStream')) {
            throw new \Exception('Operation itemAttributeRetrieve cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'itemAttributeRetrieve')) {
            \Flight::route('GET /api/v2/item-attribute/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $result = $handler->itemAttributeRetrieve(
                    parseParam($id, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'itemAttributeRetrieveStream')) {
            \Flight::route('GET /api/v2/item-attribute/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $handler->itemAttributeRetrieveStream(
                    parseParam($id, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'itemCategoryList') && declaresMethod($reflectionClass, 'itemCategoryListStream')) {
            throw new \Exception('Operation itemCategoryList cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'itemCategoryList')) {
            \Flight::route('GET /api/v2/item-category/', function () use ($handler) {
                $r = \Flight::request();
                $result = $handler->itemCategoryList(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'itemCategoryListStream')) {
            \Flight::route('GET /api/v2/item-category/', function () use ($handler) {
                $r = \Flight::request();
                $handler->itemCategoryListStream(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'itemCategoryRetrieve') && declaresMethod($reflectionClass, 'itemCategoryRetrieveStream')) {
            throw new \Exception('Operation itemCategoryRetrieve cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'itemCategoryRetrieve')) {
            \Flight::route('GET /api/v2/item-category/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $result = $handler->itemCategoryRetrieve(
                    parseParam($id, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'itemCategoryRetrieveStream')) {
            \Flight::route('GET /api/v2/item-category/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $handler->itemCategoryRetrieveStream(
                    parseParam($id, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'itemFlingEffectList') && declaresMethod($reflectionClass, 'itemFlingEffectListStream')) {
            throw new \Exception('Operation itemFlingEffectList cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'itemFlingEffectList')) {
            \Flight::route('GET /api/v2/item-fling-effect/', function () use ($handler) {
                $r = \Flight::request();
                $result = $handler->itemFlingEffectList(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'itemFlingEffectListStream')) {
            \Flight::route('GET /api/v2/item-fling-effect/', function () use ($handler) {
                $r = \Flight::request();
                $handler->itemFlingEffectListStream(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'itemFlingEffectRetrieve') && declaresMethod($reflectionClass, 'itemFlingEffectRetrieveStream')) {
            throw new \Exception('Operation itemFlingEffectRetrieve cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'itemFlingEffectRetrieve')) {
            \Flight::route('GET /api/v2/item-fling-effect/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $result = $handler->itemFlingEffectRetrieve(
                    parseParam($id, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'itemFlingEffectRetrieveStream')) {
            \Flight::route('GET /api/v2/item-fling-effect/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $handler->itemFlingEffectRetrieveStream(
                    parseParam($id, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'itemList') && declaresMethod($reflectionClass, 'itemListStream')) {
            throw new \Exception('Operation itemList cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'itemList')) {
            \Flight::route('GET /api/v2/item/', function () use ($handler) {
                $r = \Flight::request();
                $result = $handler->itemList(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'itemListStream')) {
            \Flight::route('GET /api/v2/item/', function () use ($handler) {
                $r = \Flight::request();
                $handler->itemListStream(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'itemPocketList') && declaresMethod($reflectionClass, 'itemPocketListStream')) {
            throw new \Exception('Operation itemPocketList cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'itemPocketList')) {
            \Flight::route('GET /api/v2/item-pocket/', function () use ($handler) {
                $r = \Flight::request();
                $result = $handler->itemPocketList(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'itemPocketListStream')) {
            \Flight::route('GET /api/v2/item-pocket/', function () use ($handler) {
                $r = \Flight::request();
                $handler->itemPocketListStream(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'itemPocketRetrieve') && declaresMethod($reflectionClass, 'itemPocketRetrieveStream')) {
            throw new \Exception('Operation itemPocketRetrieve cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'itemPocketRetrieve')) {
            \Flight::route('GET /api/v2/item-pocket/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $result = $handler->itemPocketRetrieve(
                    parseParam($id, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'itemPocketRetrieveStream')) {
            \Flight::route('GET /api/v2/item-pocket/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $handler->itemPocketRetrieveStream(
                    parseParam($id, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'itemRetrieve') && declaresMethod($reflectionClass, 'itemRetrieveStream')) {
            throw new \Exception('Operation itemRetrieve cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'itemRetrieve')) {
            \Flight::route('GET /api/v2/item/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $result = $handler->itemRetrieve(
                    parseParam($id, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'itemRetrieveStream')) {
            \Flight::route('GET /api/v2/item/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $handler->itemRetrieveStream(
                    parseParam($id, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'locationAreaList') && declaresMethod($reflectionClass, 'locationAreaListStream')) {
            throw new \Exception('Operation locationAreaList cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'locationAreaList')) {
            \Flight::route('GET /api/v2/location-area/', function () use ($handler) {
                $r = \Flight::request();
                $result = $handler->locationAreaList(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'locationAreaListStream')) {
            \Flight::route('GET /api/v2/location-area/', function () use ($handler) {
                $r = \Flight::request();
                $handler->locationAreaListStream(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'locationAreaRetrieve') && declaresMethod($reflectionClass, 'locationAreaRetrieveStream')) {
            throw new \Exception('Operation locationAreaRetrieve cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'locationAreaRetrieve')) {
            \Flight::route('GET /api/v2/location-area/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $result = $handler->locationAreaRetrieve(
                    parseParam($id, 'int')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'locationAreaRetrieveStream')) {
            \Flight::route('GET /api/v2/location-area/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $handler->locationAreaRetrieveStream(
                    parseParam($id, 'int')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'locationList') && declaresMethod($reflectionClass, 'locationListStream')) {
            throw new \Exception('Operation locationList cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'locationList')) {
            \Flight::route('GET /api/v2/location/', function () use ($handler) {
                $r = \Flight::request();
                $result = $handler->locationList(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'locationListStream')) {
            \Flight::route('GET /api/v2/location/', function () use ($handler) {
                $r = \Flight::request();
                $handler->locationListStream(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'locationRetrieve') && declaresMethod($reflectionClass, 'locationRetrieveStream')) {
            throw new \Exception('Operation locationRetrieve cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'locationRetrieve')) {
            \Flight::route('GET /api/v2/location/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $result = $handler->locationRetrieve(
                    parseParam($id, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'locationRetrieveStream')) {
            \Flight::route('GET /api/v2/location/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $handler->locationRetrieveStream(
                    parseParam($id, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'palParkAreaList') && declaresMethod($reflectionClass, 'palParkAreaListStream')) {
            throw new \Exception('Operation palParkAreaList cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'palParkAreaList')) {
            \Flight::route('GET /api/v2/pal-park-area/', function () use ($handler) {
                $r = \Flight::request();
                $result = $handler->palParkAreaList(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'palParkAreaListStream')) {
            \Flight::route('GET /api/v2/pal-park-area/', function () use ($handler) {
                $r = \Flight::request();
                $handler->palParkAreaListStream(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'palParkAreaRetrieve') && declaresMethod($reflectionClass, 'palParkAreaRetrieveStream')) {
            throw new \Exception('Operation palParkAreaRetrieve cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'palParkAreaRetrieve')) {
            \Flight::route('GET /api/v2/pal-park-area/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $result = $handler->palParkAreaRetrieve(
                    parseParam($id, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'palParkAreaRetrieveStream')) {
            \Flight::route('GET /api/v2/pal-park-area/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $handler->palParkAreaRetrieveStream(
                    parseParam($id, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'regionList') && declaresMethod($reflectionClass, 'regionListStream')) {
            throw new \Exception('Operation regionList cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'regionList')) {
            \Flight::route('GET /api/v2/region/', function () use ($handler) {
                $r = \Flight::request();
                $result = $handler->regionList(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'regionListStream')) {
            \Flight::route('GET /api/v2/region/', function () use ($handler) {
                $r = \Flight::request();
                $handler->regionListStream(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'regionRetrieve') && declaresMethod($reflectionClass, 'regionRetrieveStream')) {
            throw new \Exception('Operation regionRetrieve cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'regionRetrieve')) {
            \Flight::route('GET /api/v2/region/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $result = $handler->regionRetrieve(
                    parseParam($id, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'regionRetrieveStream')) {
            \Flight::route('GET /api/v2/region/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $handler->regionRetrieveStream(
                    parseParam($id, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'machineList') && declaresMethod($reflectionClass, 'machineListStream')) {
            throw new \Exception('Operation machineList cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'machineList')) {
            \Flight::route('GET /api/v2/machine/', function () use ($handler) {
                $r = \Flight::request();
                $result = $handler->machineList(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'machineListStream')) {
            \Flight::route('GET /api/v2/machine/', function () use ($handler) {
                $r = \Flight::request();
                $handler->machineListStream(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'machineRetrieve') && declaresMethod($reflectionClass, 'machineRetrieveStream')) {
            throw new \Exception('Operation machineRetrieve cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'machineRetrieve')) {
            \Flight::route('GET /api/v2/machine/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $result = $handler->machineRetrieve(
                    parseParam($id, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'machineRetrieveStream')) {
            \Flight::route('GET /api/v2/machine/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $handler->machineRetrieveStream(
                    parseParam($id, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'moveAilmentList') && declaresMethod($reflectionClass, 'moveAilmentListStream')) {
            throw new \Exception('Operation moveAilmentList cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'moveAilmentList')) {
            \Flight::route('GET /api/v2/move-ailment/', function () use ($handler) {
                $r = \Flight::request();
                $result = $handler->moveAilmentList(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'moveAilmentListStream')) {
            \Flight::route('GET /api/v2/move-ailment/', function () use ($handler) {
                $r = \Flight::request();
                $handler->moveAilmentListStream(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'moveAilmentRetrieve') && declaresMethod($reflectionClass, 'moveAilmentRetrieveStream')) {
            throw new \Exception('Operation moveAilmentRetrieve cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'moveAilmentRetrieve')) {
            \Flight::route('GET /api/v2/move-ailment/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $result = $handler->moveAilmentRetrieve(
                    parseParam($id, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'moveAilmentRetrieveStream')) {
            \Flight::route('GET /api/v2/move-ailment/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $handler->moveAilmentRetrieveStream(
                    parseParam($id, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'moveBattleStyleList') && declaresMethod($reflectionClass, 'moveBattleStyleListStream')) {
            throw new \Exception('Operation moveBattleStyleList cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'moveBattleStyleList')) {
            \Flight::route('GET /api/v2/move-battle-style/', function () use ($handler) {
                $r = \Flight::request();
                $result = $handler->moveBattleStyleList(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'moveBattleStyleListStream')) {
            \Flight::route('GET /api/v2/move-battle-style/', function () use ($handler) {
                $r = \Flight::request();
                $handler->moveBattleStyleListStream(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'moveBattleStyleRetrieve') && declaresMethod($reflectionClass, 'moveBattleStyleRetrieveStream')) {
            throw new \Exception('Operation moveBattleStyleRetrieve cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'moveBattleStyleRetrieve')) {
            \Flight::route('GET /api/v2/move-battle-style/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $result = $handler->moveBattleStyleRetrieve(
                    parseParam($id, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'moveBattleStyleRetrieveStream')) {
            \Flight::route('GET /api/v2/move-battle-style/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $handler->moveBattleStyleRetrieveStream(
                    parseParam($id, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'moveCategoryList') && declaresMethod($reflectionClass, 'moveCategoryListStream')) {
            throw new \Exception('Operation moveCategoryList cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'moveCategoryList')) {
            \Flight::route('GET /api/v2/move-category/', function () use ($handler) {
                $r = \Flight::request();
                $result = $handler->moveCategoryList(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'moveCategoryListStream')) {
            \Flight::route('GET /api/v2/move-category/', function () use ($handler) {
                $r = \Flight::request();
                $handler->moveCategoryListStream(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'moveCategoryRetrieve') && declaresMethod($reflectionClass, 'moveCategoryRetrieveStream')) {
            throw new \Exception('Operation moveCategoryRetrieve cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'moveCategoryRetrieve')) {
            \Flight::route('GET /api/v2/move-category/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $result = $handler->moveCategoryRetrieve(
                    parseParam($id, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'moveCategoryRetrieveStream')) {
            \Flight::route('GET /api/v2/move-category/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $handler->moveCategoryRetrieveStream(
                    parseParam($id, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'moveLearnMethodList') && declaresMethod($reflectionClass, 'moveLearnMethodListStream')) {
            throw new \Exception('Operation moveLearnMethodList cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'moveLearnMethodList')) {
            \Flight::route('GET /api/v2/move-learn-method/', function () use ($handler) {
                $r = \Flight::request();
                $result = $handler->moveLearnMethodList(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'moveLearnMethodListStream')) {
            \Flight::route('GET /api/v2/move-learn-method/', function () use ($handler) {
                $r = \Flight::request();
                $handler->moveLearnMethodListStream(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'moveLearnMethodRetrieve') && declaresMethod($reflectionClass, 'moveLearnMethodRetrieveStream')) {
            throw new \Exception('Operation moveLearnMethodRetrieve cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'moveLearnMethodRetrieve')) {
            \Flight::route('GET /api/v2/move-learn-method/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $result = $handler->moveLearnMethodRetrieve(
                    parseParam($id, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'moveLearnMethodRetrieveStream')) {
            \Flight::route('GET /api/v2/move-learn-method/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $handler->moveLearnMethodRetrieveStream(
                    parseParam($id, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'moveList') && declaresMethod($reflectionClass, 'moveListStream')) {
            throw new \Exception('Operation moveList cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'moveList')) {
            \Flight::route('GET /api/v2/move/', function () use ($handler) {
                $r = \Flight::request();
                $result = $handler->moveList(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'moveListStream')) {
            \Flight::route('GET /api/v2/move/', function () use ($handler) {
                $r = \Flight::request();
                $handler->moveListStream(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'moveRetrieve') && declaresMethod($reflectionClass, 'moveRetrieveStream')) {
            throw new \Exception('Operation moveRetrieve cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'moveRetrieve')) {
            \Flight::route('GET /api/v2/move/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $result = $handler->moveRetrieve(
                    parseParam($id, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'moveRetrieveStream')) {
            \Flight::route('GET /api/v2/move/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $handler->moveRetrieveStream(
                    parseParam($id, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'moveTargetList') && declaresMethod($reflectionClass, 'moveTargetListStream')) {
            throw new \Exception('Operation moveTargetList cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'moveTargetList')) {
            \Flight::route('GET /api/v2/move-target/', function () use ($handler) {
                $r = \Flight::request();
                $result = $handler->moveTargetList(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'moveTargetListStream')) {
            \Flight::route('GET /api/v2/move-target/', function () use ($handler) {
                $r = \Flight::request();
                $handler->moveTargetListStream(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'moveTargetRetrieve') && declaresMethod($reflectionClass, 'moveTargetRetrieveStream')) {
            throw new \Exception('Operation moveTargetRetrieve cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'moveTargetRetrieve')) {
            \Flight::route('GET /api/v2/move-target/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $result = $handler->moveTargetRetrieve(
                    parseParam($id, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'moveTargetRetrieveStream')) {
            \Flight::route('GET /api/v2/move-target/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $handler->moveTargetRetrieveStream(
                    parseParam($id, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'abilityList') && declaresMethod($reflectionClass, 'abilityListStream')) {
            throw new \Exception('Operation abilityList cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'abilityList')) {
            \Flight::route('GET /api/v2/ability/', function () use ($handler) {
                $r = \Flight::request();
                $result = $handler->abilityList(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'abilityListStream')) {
            \Flight::route('GET /api/v2/ability/', function () use ($handler) {
                $r = \Flight::request();
                $handler->abilityListStream(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'abilityRetrieve') && declaresMethod($reflectionClass, 'abilityRetrieveStream')) {
            throw new \Exception('Operation abilityRetrieve cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'abilityRetrieve')) {
            \Flight::route('GET /api/v2/ability/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $result = $handler->abilityRetrieve(
                    parseParam($id, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'abilityRetrieveStream')) {
            \Flight::route('GET /api/v2/ability/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $handler->abilityRetrieveStream(
                    parseParam($id, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'characteristicList') && declaresMethod($reflectionClass, 'characteristicListStream')) {
            throw new \Exception('Operation characteristicList cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'characteristicList')) {
            \Flight::route('GET /api/v2/characteristic/', function () use ($handler) {
                $r = \Flight::request();
                $result = $handler->characteristicList(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'characteristicListStream')) {
            \Flight::route('GET /api/v2/characteristic/', function () use ($handler) {
                $r = \Flight::request();
                $handler->characteristicListStream(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'characteristicRetrieve') && declaresMethod($reflectionClass, 'characteristicRetrieveStream')) {
            throw new \Exception('Operation characteristicRetrieve cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'characteristicRetrieve')) {
            \Flight::route('GET /api/v2/characteristic/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $result = $handler->characteristicRetrieve(
                    parseParam($id, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'characteristicRetrieveStream')) {
            \Flight::route('GET /api/v2/characteristic/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $handler->characteristicRetrieveStream(
                    parseParam($id, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'eggGroupList') && declaresMethod($reflectionClass, 'eggGroupListStream')) {
            throw new \Exception('Operation eggGroupList cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'eggGroupList')) {
            \Flight::route('GET /api/v2/egg-group/', function () use ($handler) {
                $r = \Flight::request();
                $result = $handler->eggGroupList(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'eggGroupListStream')) {
            \Flight::route('GET /api/v2/egg-group/', function () use ($handler) {
                $r = \Flight::request();
                $handler->eggGroupListStream(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'eggGroupRetrieve') && declaresMethod($reflectionClass, 'eggGroupRetrieveStream')) {
            throw new \Exception('Operation eggGroupRetrieve cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'eggGroupRetrieve')) {
            \Flight::route('GET /api/v2/egg-group/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $result = $handler->eggGroupRetrieve(
                    parseParam($id, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'eggGroupRetrieveStream')) {
            \Flight::route('GET /api/v2/egg-group/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $handler->eggGroupRetrieveStream(
                    parseParam($id, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'genderList') && declaresMethod($reflectionClass, 'genderListStream')) {
            throw new \Exception('Operation genderList cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'genderList')) {
            \Flight::route('GET /api/v2/gender/', function () use ($handler) {
                $r = \Flight::request();
                $result = $handler->genderList(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'genderListStream')) {
            \Flight::route('GET /api/v2/gender/', function () use ($handler) {
                $r = \Flight::request();
                $handler->genderListStream(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'genderRetrieve') && declaresMethod($reflectionClass, 'genderRetrieveStream')) {
            throw new \Exception('Operation genderRetrieve cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'genderRetrieve')) {
            \Flight::route('GET /api/v2/gender/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $result = $handler->genderRetrieve(
                    parseParam($id, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'genderRetrieveStream')) {
            \Flight::route('GET /api/v2/gender/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $handler->genderRetrieveStream(
                    parseParam($id, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'growthRateList') && declaresMethod($reflectionClass, 'growthRateListStream')) {
            throw new \Exception('Operation growthRateList cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'growthRateList')) {
            \Flight::route('GET /api/v2/growth-rate/', function () use ($handler) {
                $r = \Flight::request();
                $result = $handler->growthRateList(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'growthRateListStream')) {
            \Flight::route('GET /api/v2/growth-rate/', function () use ($handler) {
                $r = \Flight::request();
                $handler->growthRateListStream(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'growthRateRetrieve') && declaresMethod($reflectionClass, 'growthRateRetrieveStream')) {
            throw new \Exception('Operation growthRateRetrieve cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'growthRateRetrieve')) {
            \Flight::route('GET /api/v2/growth-rate/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $result = $handler->growthRateRetrieve(
                    parseParam($id, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'growthRateRetrieveStream')) {
            \Flight::route('GET /api/v2/growth-rate/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $handler->growthRateRetrieveStream(
                    parseParam($id, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'moveDamageClassList') && declaresMethod($reflectionClass, 'moveDamageClassListStream')) {
            throw new \Exception('Operation moveDamageClassList cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'moveDamageClassList')) {
            \Flight::route('GET /api/v2/move-damage-class/', function () use ($handler) {
                $r = \Flight::request();
                $result = $handler->moveDamageClassList(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'moveDamageClassListStream')) {
            \Flight::route('GET /api/v2/move-damage-class/', function () use ($handler) {
                $r = \Flight::request();
                $handler->moveDamageClassListStream(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'moveDamageClassRetrieve') && declaresMethod($reflectionClass, 'moveDamageClassRetrieveStream')) {
            throw new \Exception('Operation moveDamageClassRetrieve cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'moveDamageClassRetrieve')) {
            \Flight::route('GET /api/v2/move-damage-class/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $result = $handler->moveDamageClassRetrieve(
                    parseParam($id, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'moveDamageClassRetrieveStream')) {
            \Flight::route('GET /api/v2/move-damage-class/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $handler->moveDamageClassRetrieveStream(
                    parseParam($id, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'natureList') && declaresMethod($reflectionClass, 'natureListStream')) {
            throw new \Exception('Operation natureList cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'natureList')) {
            \Flight::route('GET /api/v2/nature/', function () use ($handler) {
                $r = \Flight::request();
                $result = $handler->natureList(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'natureListStream')) {
            \Flight::route('GET /api/v2/nature/', function () use ($handler) {
                $r = \Flight::request();
                $handler->natureListStream(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'natureRetrieve') && declaresMethod($reflectionClass, 'natureRetrieveStream')) {
            throw new \Exception('Operation natureRetrieve cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'natureRetrieve')) {
            \Flight::route('GET /api/v2/nature/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $result = $handler->natureRetrieve(
                    parseParam($id, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'natureRetrieveStream')) {
            \Flight::route('GET /api/v2/nature/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $handler->natureRetrieveStream(
                    parseParam($id, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'pokeathlonStatList') && declaresMethod($reflectionClass, 'pokeathlonStatListStream')) {
            throw new \Exception('Operation pokeathlonStatList cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'pokeathlonStatList')) {
            \Flight::route('GET /api/v2/pokeathlon-stat/', function () use ($handler) {
                $r = \Flight::request();
                $result = $handler->pokeathlonStatList(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'pokeathlonStatListStream')) {
            \Flight::route('GET /api/v2/pokeathlon-stat/', function () use ($handler) {
                $r = \Flight::request();
                $handler->pokeathlonStatListStream(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'pokeathlonStatRetrieve') && declaresMethod($reflectionClass, 'pokeathlonStatRetrieveStream')) {
            throw new \Exception('Operation pokeathlonStatRetrieve cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'pokeathlonStatRetrieve')) {
            \Flight::route('GET /api/v2/pokeathlon-stat/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $result = $handler->pokeathlonStatRetrieve(
                    parseParam($id, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'pokeathlonStatRetrieveStream')) {
            \Flight::route('GET /api/v2/pokeathlon-stat/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $handler->pokeathlonStatRetrieveStream(
                    parseParam($id, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'pokemonColorList') && declaresMethod($reflectionClass, 'pokemonColorListStream')) {
            throw new \Exception('Operation pokemonColorList cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'pokemonColorList')) {
            \Flight::route('GET /api/v2/pokemon-color/', function () use ($handler) {
                $r = \Flight::request();
                $result = $handler->pokemonColorList(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'pokemonColorListStream')) {
            \Flight::route('GET /api/v2/pokemon-color/', function () use ($handler) {
                $r = \Flight::request();
                $handler->pokemonColorListStream(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'pokemonColorRetrieve') && declaresMethod($reflectionClass, 'pokemonColorRetrieveStream')) {
            throw new \Exception('Operation pokemonColorRetrieve cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'pokemonColorRetrieve')) {
            \Flight::route('GET /api/v2/pokemon-color/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $result = $handler->pokemonColorRetrieve(
                    parseParam($id, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'pokemonColorRetrieveStream')) {
            \Flight::route('GET /api/v2/pokemon-color/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $handler->pokemonColorRetrieveStream(
                    parseParam($id, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'pokemonFormList') && declaresMethod($reflectionClass, 'pokemonFormListStream')) {
            throw new \Exception('Operation pokemonFormList cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'pokemonFormList')) {
            \Flight::route('GET /api/v2/pokemon-form/', function () use ($handler) {
                $r = \Flight::request();
                $result = $handler->pokemonFormList(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'pokemonFormListStream')) {
            \Flight::route('GET /api/v2/pokemon-form/', function () use ($handler) {
                $r = \Flight::request();
                $handler->pokemonFormListStream(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'pokemonFormRetrieve') && declaresMethod($reflectionClass, 'pokemonFormRetrieveStream')) {
            throw new \Exception('Operation pokemonFormRetrieve cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'pokemonFormRetrieve')) {
            \Flight::route('GET /api/v2/pokemon-form/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $result = $handler->pokemonFormRetrieve(
                    parseParam($id, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'pokemonFormRetrieveStream')) {
            \Flight::route('GET /api/v2/pokemon-form/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $handler->pokemonFormRetrieveStream(
                    parseParam($id, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'pokemonHabitatList') && declaresMethod($reflectionClass, 'pokemonHabitatListStream')) {
            throw new \Exception('Operation pokemonHabitatList cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'pokemonHabitatList')) {
            \Flight::route('GET /api/v2/pokemon-habitat/', function () use ($handler) {
                $r = \Flight::request();
                $result = $handler->pokemonHabitatList(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'pokemonHabitatListStream')) {
            \Flight::route('GET /api/v2/pokemon-habitat/', function () use ($handler) {
                $r = \Flight::request();
                $handler->pokemonHabitatListStream(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'pokemonHabitatRetrieve') && declaresMethod($reflectionClass, 'pokemonHabitatRetrieveStream')) {
            throw new \Exception('Operation pokemonHabitatRetrieve cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'pokemonHabitatRetrieve')) {
            \Flight::route('GET /api/v2/pokemon-habitat/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $result = $handler->pokemonHabitatRetrieve(
                    parseParam($id, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'pokemonHabitatRetrieveStream')) {
            \Flight::route('GET /api/v2/pokemon-habitat/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $handler->pokemonHabitatRetrieveStream(
                    parseParam($id, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'pokemonList') && declaresMethod($reflectionClass, 'pokemonListStream')) {
            throw new \Exception('Operation pokemonList cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'pokemonList')) {
            \Flight::route('GET /api/v2/pokemon/', function () use ($handler) {
                $r = \Flight::request();
                $result = $handler->pokemonList(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'pokemonListStream')) {
            \Flight::route('GET /api/v2/pokemon/', function () use ($handler) {
                $r = \Flight::request();
                $handler->pokemonListStream(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'pokemonRetrieve') && declaresMethod($reflectionClass, 'pokemonRetrieveStream')) {
            throw new \Exception('Operation pokemonRetrieve cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'pokemonRetrieve')) {
            \Flight::route('GET /api/v2/pokemon/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $result = $handler->pokemonRetrieve(
                    parseParam($id, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'pokemonRetrieveStream')) {
            \Flight::route('GET /api/v2/pokemon/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $handler->pokemonRetrieveStream(
                    parseParam($id, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'pokemonShapeList') && declaresMethod($reflectionClass, 'pokemonShapeListStream')) {
            throw new \Exception('Operation pokemonShapeList cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'pokemonShapeList')) {
            \Flight::route('GET /api/v2/pokemon-shape/', function () use ($handler) {
                $r = \Flight::request();
                $result = $handler->pokemonShapeList(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'pokemonShapeListStream')) {
            \Flight::route('GET /api/v2/pokemon-shape/', function () use ($handler) {
                $r = \Flight::request();
                $handler->pokemonShapeListStream(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'pokemonShapeRetrieve') && declaresMethod($reflectionClass, 'pokemonShapeRetrieveStream')) {
            throw new \Exception('Operation pokemonShapeRetrieve cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'pokemonShapeRetrieve')) {
            \Flight::route('GET /api/v2/pokemon-shape/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $result = $handler->pokemonShapeRetrieve(
                    parseParam($id, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'pokemonShapeRetrieveStream')) {
            \Flight::route('GET /api/v2/pokemon-shape/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $handler->pokemonShapeRetrieveStream(
                    parseParam($id, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'pokemonSpeciesList') && declaresMethod($reflectionClass, 'pokemonSpeciesListStream')) {
            throw new \Exception('Operation pokemonSpeciesList cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'pokemonSpeciesList')) {
            \Flight::route('GET /api/v2/pokemon-species/', function () use ($handler) {
                $r = \Flight::request();
                $result = $handler->pokemonSpeciesList(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'pokemonSpeciesListStream')) {
            \Flight::route('GET /api/v2/pokemon-species/', function () use ($handler) {
                $r = \Flight::request();
                $handler->pokemonSpeciesListStream(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'pokemonSpeciesRetrieve') && declaresMethod($reflectionClass, 'pokemonSpeciesRetrieveStream')) {
            throw new \Exception('Operation pokemonSpeciesRetrieve cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'pokemonSpeciesRetrieve')) {
            \Flight::route('GET /api/v2/pokemon-species/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $result = $handler->pokemonSpeciesRetrieve(
                    parseParam($id, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'pokemonSpeciesRetrieveStream')) {
            \Flight::route('GET /api/v2/pokemon-species/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $handler->pokemonSpeciesRetrieveStream(
                    parseParam($id, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'statList') && declaresMethod($reflectionClass, 'statListStream')) {
            throw new \Exception('Operation statList cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'statList')) {
            \Flight::route('GET /api/v2/stat/', function () use ($handler) {
                $r = \Flight::request();
                $result = $handler->statList(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'statListStream')) {
            \Flight::route('GET /api/v2/stat/', function () use ($handler) {
                $r = \Flight::request();
                $handler->statListStream(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'statRetrieve') && declaresMethod($reflectionClass, 'statRetrieveStream')) {
            throw new \Exception('Operation statRetrieve cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'statRetrieve')) {
            \Flight::route('GET /api/v2/stat/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $result = $handler->statRetrieve(
                    parseParam($id, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'statRetrieveStream')) {
            \Flight::route('GET /api/v2/stat/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $handler->statRetrieveStream(
                    parseParam($id, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'typeList') && declaresMethod($reflectionClass, 'typeListStream')) {
            throw new \Exception('Operation typeList cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'typeList')) {
            \Flight::route('GET /api/v2/type/', function () use ($handler) {
                $r = \Flight::request();
                $result = $handler->typeList(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'typeListStream')) {
            \Flight::route('GET /api/v2/type/', function () use ($handler) {
                $r = \Flight::request();
                $handler->typeListStream(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'typeRetrieve') && declaresMethod($reflectionClass, 'typeRetrieveStream')) {
            throw new \Exception('Operation typeRetrieve cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'typeRetrieve')) {
            \Flight::route('GET /api/v2/type/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $result = $handler->typeRetrieve(
                    parseParam($id, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'typeRetrieveStream')) {
            \Flight::route('GET /api/v2/type/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $handler->typeRetrieveStream(
                    parseParam($id, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'languageList') && declaresMethod($reflectionClass, 'languageListStream')) {
            throw new \Exception('Operation languageList cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'languageList')) {
            \Flight::route('GET /api/v2/language/', function () use ($handler) {
                $r = \Flight::request();
                $result = $handler->languageList(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'languageListStream')) {
            \Flight::route('GET /api/v2/language/', function () use ($handler) {
                $r = \Flight::request();
                $handler->languageListStream(
                    parseParam($r->query['limit'] ?? null, '?int'),
                    parseParam($r->query['offset'] ?? null, '?int'),
                    parseParam($r->query['q'] ?? null, '?string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

        if (declaresMethod($reflectionClass, 'languageRetrieve') && declaresMethod($reflectionClass, 'languageRetrieveStream')) {
            throw new \Exception('Operation languageRetrieve cannot be both streaming and non-streaming');
        }
        if (declaresMethod($reflectionClass, 'languageRetrieve')) {
            \Flight::route('GET /api/v2/language/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $result = $handler->languageRetrieve(
                    parseParam($id, 'string')
                );
                if ($result === null) {
                    \Flight::halt(200);
                } else {
                    \Flight::json($result, 200);
                }
            });
        }
        if (declaresMethod($reflectionClass, 'languageRetrieveStream')) {
            \Flight::route('GET /api/v2/language/@id/', function (string $id) use ($handler) {
                $r = \Flight::request();
                $handler->languageRetrieveStream(
                    parseParam($id, 'string')
                );
                // ignore return value: streaming expected
            })->streamWithHeaders(['status' => 200, 'Content-Type' => 'application/json']);
        }

    }
}


function parseParam(mixed $param, string $type)
{
    $nonNullType = str_replace('?', '', $type);
    if ($param === null) {
        return null;
    } elseif ($nonNullType === 'int') {
        return intval($param);
    } elseif ($nonNullType === 'float') {
        return floatval($param);
    } elseif ($nonNullType === 'bool') {
        return filter_var($param, FILTER_VALIDATE_BOOLEAN);
    } elseif (str_ends_with($type, '[]')) {
        return array_map(fn($el) => parseParam($el, substr($type, 0, -2)), $param);
    } elseif (str_starts_with($nonNullType, '\\OpenAPIServer\\Model')) {
        if (enum_exists($nonNullType)) {
            return $nonNullType::tryFrom($param);
        }
        return $nonNullType::fromArray($param);
    } else {
        return $param;
    }
}

function declaresMethod(\ReflectionClass $reflectionClass, string $methodName): bool
{
    return $reflectionClass->hasMethod($methodName) && $reflectionClass->getMethod($methodName)->getDeclaringClass()->getName() === $reflectionClass->getName();
}
