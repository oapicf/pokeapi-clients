/**
 * PokéAPI
 * All the Pokémon data you\'ll ever need in one place, easily accessible through a modern free open-source RESTful API.  ## What is this?  This is a full RESTful API linked to an extensive database detailing everything about the Pokémon main game series.  We\'ve covered everything from Pokémon to Berry Flavors.  ## Where do I start?  We have awesome [documentation](https://pokeapi.co/docs/v2) on how to use this API. It takes minutes to get started.  This API will always be publicly available and will never require any extensive setup process to consume.  Created by [**Paul Hallett**(]https://github.com/phalt) and other [**PokéAPI contributors***](https://github.com/PokeAPI/pokeapi#contributing) around the world. Pokémon and Pokémon character names are trademarks of Nintendo.     
 *
 * The version of the OpenAPI document: 2.7.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { autoinject } from 'aurelia-framework';
import { HttpClient } from 'aurelia-http-client';
import { Api } from './Api';
import { AuthStorage } from './AuthStorage';
import {
  PaginatedGenerationSummaryList,
  VersionGroupDetail,
  GenerationDetail,
  VersionDetail,
  PokedexDetail,
  PaginatedVersionGroupSummaryList,
  PaginatedPokedexSummaryList,
  PaginatedVersionSummaryList,
} from './models';

/**
 * generationList - parameters interface
 */
export interface IGenerationListParams {
  limit?: number;
  offset?: number;
  q?: string;
}

/**
 * generationRetrieve - parameters interface
 */
export interface IGenerationRetrieveParams {
  id: string;
}

/**
 * pokedexList - parameters interface
 */
export interface IPokedexListParams {
  limit?: number;
  offset?: number;
  q?: string;
}

/**
 * pokedexRetrieve - parameters interface
 */
export interface IPokedexRetrieveParams {
  id: string;
}

/**
 * versionGroupList - parameters interface
 */
export interface IVersionGroupListParams {
  limit?: number;
  offset?: number;
  q?: string;
}

/**
 * versionGroupRetrieve - parameters interface
 */
export interface IVersionGroupRetrieveParams {
  id: string;
}

/**
 * versionList - parameters interface
 */
export interface IVersionListParams {
  limit?: number;
  offset?: number;
  q?: string;
}

/**
 * versionRetrieve - parameters interface
 */
export interface IVersionRetrieveParams {
  id: string;
}

/**
 * GamesApi - API class
 */
@autoinject()
export class GamesApi extends Api {

  /**
   * Creates a new GamesApi class.
   *
   * @param httpClient The Aurelia HTTP client to be injected.
   * @param authStorage A storage for authentication data.
   */
  constructor(httpClient: HttpClient, authStorage: AuthStorage) {
    super(httpClient, authStorage);
  }

  /**
   * List genrations
   * A generation is a grouping of the Pokémon games that separates them based on the Pokémon they include. In each generation, a new set of Pokémon, Moves, Abilities and Types that did not exist in the previous generation are released.
   * @param params.limit Number of results to return per page.
   * @param params.offset The initial index from which to return the results.
   * @param params.q &gt; Only available locally and not at [pokeapi.co](https://pokeapi.co/docs/v2) Case-insensitive query applied on the &#x60;name&#x60; property. 
   */
  async generationList(params: IGenerationListParams): Promise<PaginatedGenerationSummaryList> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/api/v2/generation/`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'limit': params['limit'],
        'offset': params['offset'],
        'q': params['q'],
      })

      // Authentication 'basicAuth' required
      // Authentication 'cookieAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get genration
   * A generation is a grouping of the Pokémon games that separates them based on the Pokémon they include. In each generation, a new set of Pokémon, Moves, Abilities and Types that did not exist in the previous generation are released.
   * @param params.id This parameter can be a string or an integer.
   */
  async generationRetrieve(params: IGenerationRetrieveParams): Promise<GenerationDetail> {
    // Verify required parameters are set
    this.ensureParamIsSet('generationRetrieve', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/api/v2/generation/{id}/`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'basicAuth' required
      // Authentication 'cookieAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * List pokedex
   * A Pokédex is a handheld electronic encyclopedia device; one which is capable of recording and retaining information of the various Pokémon in a given region with the exception of the national dex and some smaller dexes related to portions of a region. See [Bulbapedia](http://bulbapedia.bulbagarden.net/wiki/Pokedex) for greater detail.
   * @param params.limit Number of results to return per page.
   * @param params.offset The initial index from which to return the results.
   * @param params.q &gt; Only available locally and not at [pokeapi.co](https://pokeapi.co/docs/v2) Case-insensitive query applied on the &#x60;name&#x60; property. 
   */
  async pokedexList(params: IPokedexListParams): Promise<PaginatedPokedexSummaryList> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/api/v2/pokedex/`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'limit': params['limit'],
        'offset': params['offset'],
        'q': params['q'],
      })

      // Authentication 'basicAuth' required
      // Authentication 'cookieAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get pokedex
   * A Pokédex is a handheld electronic encyclopedia device; one which is capable of recording and retaining information of the various Pokémon in a given region with the exception of the national dex and some smaller dexes related to portions of a region. See [Bulbapedia](http://bulbapedia.bulbagarden.net/wiki/Pokedex) for greater detail.
   * @param params.id This parameter can be a string or an integer.
   */
  async pokedexRetrieve(params: IPokedexRetrieveParams): Promise<PokedexDetail> {
    // Verify required parameters are set
    this.ensureParamIsSet('pokedexRetrieve', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/api/v2/pokedex/{id}/`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'basicAuth' required
      // Authentication 'cookieAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * List version groups
   * Version groups categorize highly similar versions of the games.
   * @param params.limit Number of results to return per page.
   * @param params.offset The initial index from which to return the results.
   * @param params.q &gt; Only available locally and not at [pokeapi.co](https://pokeapi.co/docs/v2) Case-insensitive query applied on the &#x60;name&#x60; property. 
   */
  async versionGroupList(params: IVersionGroupListParams): Promise<PaginatedVersionGroupSummaryList> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/api/v2/version-group/`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'limit': params['limit'],
        'offset': params['offset'],
        'q': params['q'],
      })

      // Authentication 'basicAuth' required
      // Authentication 'cookieAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get version group
   * Version groups categorize highly similar versions of the games.
   * @param params.id This parameter can be a string or an integer.
   */
  async versionGroupRetrieve(params: IVersionGroupRetrieveParams): Promise<VersionGroupDetail> {
    // Verify required parameters are set
    this.ensureParamIsSet('versionGroupRetrieve', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/api/v2/version-group/{id}/`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'basicAuth' required
      // Authentication 'cookieAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * List versions
   * Versions of the games, e.g., Red, Blue or Yellow.
   * @param params.limit Number of results to return per page.
   * @param params.offset The initial index from which to return the results.
   * @param params.q &gt; Only available locally and not at [pokeapi.co](https://pokeapi.co/docs/v2) Case-insensitive query applied on the &#x60;name&#x60; property. 
   */
  async versionList(params: IVersionListParams): Promise<PaginatedVersionSummaryList> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/api/v2/version/`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'limit': params['limit'],
        'offset': params['offset'],
        'q': params['q'],
      })

      // Authentication 'basicAuth' required
      // Authentication 'cookieAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get version
   * Versions of the games, e.g., Red, Blue or Yellow.
   * @param params.id This parameter can be a string or an integer.
   */
  async versionRetrieve(params: IVersionRetrieveParams): Promise<VersionDetail> {
    // Verify required parameters are set
    this.ensureParamIsSet('versionRetrieve', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/api/v2/version/{id}/`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'basicAuth' required
      // Authentication 'cookieAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

}

