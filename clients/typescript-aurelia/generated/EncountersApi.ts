/**
 * PokéAPI
 * All the Pokémon data you\'ll ever need in one place, easily accessible through a modern free open-source RESTful API.  ## What is this?  This is a full RESTful API linked to an extensive database detailing everything about the Pokémon main game series.  We\'ve covered everything from Pokémon to Berry Flavors.  ## Where do I start?  We have awesome [documentation](https://pokeapi.co/docs/v2) on how to use this API. It takes minutes to get started.  This API will always be publicly available and will never require any extensive setup process to consume.  Created by [**Paul Hallett**(]https://github.com/phalt) and other [**PokéAPI contributors***](https://github.com/PokeAPI/pokeapi#contributing) around the world. Pokémon and Pokémon character names are trademarks of Nintendo.     
 *
 * The version of the OpenAPI document: 2.7.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { autoinject } from 'aurelia-framework';
import { HttpClient } from 'aurelia-http-client';
import { Api } from './Api';
import { AuthStorage } from './AuthStorage';
import {
  PaginatedEncounterConditionSummaryList,
  PaginatedEncounterMethodSummaryList,
  EncounterConditionValueDetail,
  PaginatedEncounterConditionValueSummaryList,
  EncounterMethodDetail,
  EncounterConditionDetail,
} from './models';

/**
 * encounterConditionList - parameters interface
 */
export interface IEncounterConditionListParams {
  limit?: number;
  offset?: number;
  q?: string;
}

/**
 * encounterConditionRetrieve - parameters interface
 */
export interface IEncounterConditionRetrieveParams {
  id: string;
}

/**
 * encounterConditionValueList - parameters interface
 */
export interface IEncounterConditionValueListParams {
  limit?: number;
  offset?: number;
  q?: string;
}

/**
 * encounterConditionValueRetrieve - parameters interface
 */
export interface IEncounterConditionValueRetrieveParams {
  id: string;
}

/**
 * encounterMethodList - parameters interface
 */
export interface IEncounterMethodListParams {
  limit?: number;
  offset?: number;
  q?: string;
}

/**
 * encounterMethodRetrieve - parameters interface
 */
export interface IEncounterMethodRetrieveParams {
  id: string;
}

/**
 * EncountersApi - API class
 */
@autoinject()
export class EncountersApi extends Api {

  /**
   * Creates a new EncountersApi class.
   *
   * @param httpClient The Aurelia HTTP client to be injected.
   * @param authStorage A storage for authentication data.
   */
  constructor(httpClient: HttpClient, authStorage: AuthStorage) {
    super(httpClient, authStorage);
  }

  /**
   * List encounter conditions
   * Conditions which affect what pokemon might appear in the wild, e.g., day or night.
   * @param params.limit Number of results to return per page.
   * @param params.offset The initial index from which to return the results.
   * @param params.q &gt; Only available locally and not at [pokeapi.co](https://pokeapi.co/docs/v2) Case-insensitive query applied on the &#x60;name&#x60; property. 
   */
  async encounterConditionList(params: IEncounterConditionListParams): Promise<PaginatedEncounterConditionSummaryList> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/api/v2/encounter-condition/`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'limit': params['limit'],
        'offset': params['offset'],
        'q': params['q'],
      })

      // Authentication 'basicAuth' required
      // Authentication 'cookieAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get encounter condition
   * Conditions which affect what pokemon might appear in the wild, e.g., day or night.
   * @param params.id This parameter can be a string or an integer.
   */
  async encounterConditionRetrieve(params: IEncounterConditionRetrieveParams): Promise<EncounterConditionDetail> {
    // Verify required parameters are set
    this.ensureParamIsSet('encounterConditionRetrieve', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/api/v2/encounter-condition/{id}/`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'basicAuth' required
      // Authentication 'cookieAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * List encounter condition values
   * Encounter condition values are the various states that an encounter condition can have, i.e., time of day can be either day or night.
   * @param params.limit Number of results to return per page.
   * @param params.offset The initial index from which to return the results.
   * @param params.q &gt; Only available locally and not at [pokeapi.co](https://pokeapi.co/docs/v2) Case-insensitive query applied on the &#x60;name&#x60; property. 
   */
  async encounterConditionValueList(params: IEncounterConditionValueListParams): Promise<PaginatedEncounterConditionValueSummaryList> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/api/v2/encounter-condition-value/`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'limit': params['limit'],
        'offset': params['offset'],
        'q': params['q'],
      })

      // Authentication 'basicAuth' required
      // Authentication 'cookieAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get encounter condition value
   * Encounter condition values are the various states that an encounter condition can have, i.e., time of day can be either day or night.
   * @param params.id This parameter can be a string or an integer.
   */
  async encounterConditionValueRetrieve(params: IEncounterConditionValueRetrieveParams): Promise<EncounterConditionValueDetail> {
    // Verify required parameters are set
    this.ensureParamIsSet('encounterConditionValueRetrieve', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/api/v2/encounter-condition-value/{id}/`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'basicAuth' required
      // Authentication 'cookieAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * List encounter methods
   * Methods by which the player might can encounter Pokémon in the wild, e.g., walking in tall grass. Check out Bulbapedia for greater detail.
   * @param params.limit Number of results to return per page.
   * @param params.offset The initial index from which to return the results.
   * @param params.q &gt; Only available locally and not at [pokeapi.co](https://pokeapi.co/docs/v2) Case-insensitive query applied on the &#x60;name&#x60; property. 
   */
  async encounterMethodList(params: IEncounterMethodListParams): Promise<PaginatedEncounterMethodSummaryList> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/api/v2/encounter-method/`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'limit': params['limit'],
        'offset': params['offset'],
        'q': params['q'],
      })

      // Authentication 'basicAuth' required
      // Authentication 'cookieAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get encounter method
   * Methods by which the player might can encounter Pokémon in the wild, e.g., walking in tall grass. Check out Bulbapedia for greater detail.
   * @param params.id This parameter can be a string or an integer.
   */
  async encounterMethodRetrieve(params: IEncounterMethodRetrieveParams): Promise<EncounterMethodDetail> {
    // Verify required parameters are set
    this.ensureParamIsSet('encounterMethodRetrieve', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/api/v2/encounter-method/{id}/`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'basicAuth' required
      // Authentication 'cookieAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

}

