/**
 * PokéAPI
 * All the Pokémon data you\'ll ever need in one place, easily accessible through a modern free open-source RESTful API.  ## What is this?  This is a full RESTful API linked to an extensive database detailing everything about the Pokémon main game series.  We\'ve covered everything from Pokémon to Berry Flavors.  ## Where do I start?  We have awesome [documentation](https://pokeapi.co/docs/v2) on how to use this API. It takes minutes to get started.  This API will always be publicly available and will never require any extensive setup process to consume.  Created by [**Paul Hallett**(]https://github.com/phalt) and other [**PokéAPI contributors***](https://github.com/PokeAPI/pokeapi#contributing) around the world. Pokémon and Pokémon character names are trademarks of Nintendo.     
 *
 * The version of the OpenAPI document: 2.7.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { autoinject } from 'aurelia-framework';
import { HttpClient } from 'aurelia-http-client';
import { Api } from './Api';
import { AuthStorage } from './AuthStorage';
import {
  PalParkAreaDetail,
  PaginatedLocationAreaSummaryList,
  PaginatedPalParkAreaSummaryList,
  PaginatedLocationSummaryList,
  PaginatedRegionSummaryList,
  RegionDetail,
  LocationDetail,
  LocationAreaDetail,
} from './models';

/**
 * locationAreaList - parameters interface
 */
export interface ILocationAreaListParams {
  limit?: number;
  offset?: number;
}

/**
 * locationAreaRetrieve - parameters interface
 */
export interface ILocationAreaRetrieveParams {
  id: number;
}

/**
 * locationList - parameters interface
 */
export interface ILocationListParams {
  limit?: number;
  offset?: number;
  q?: string;
}

/**
 * locationRetrieve - parameters interface
 */
export interface ILocationRetrieveParams {
  id: string;
}

/**
 * palParkAreaList - parameters interface
 */
export interface IPalParkAreaListParams {
  limit?: number;
  offset?: number;
  q?: string;
}

/**
 * palParkAreaRetrieve - parameters interface
 */
export interface IPalParkAreaRetrieveParams {
  id: string;
}

/**
 * regionList - parameters interface
 */
export interface IRegionListParams {
  limit?: number;
  offset?: number;
  q?: string;
}

/**
 * regionRetrieve - parameters interface
 */
export interface IRegionRetrieveParams {
  id: string;
}

/**
 * LocationApi - API class
 */
@autoinject()
export class LocationApi extends Api {

  /**
   * Creates a new LocationApi class.
   *
   * @param httpClient The Aurelia HTTP client to be injected.
   * @param authStorage A storage for authentication data.
   */
  constructor(httpClient: HttpClient, authStorage: AuthStorage) {
    super(httpClient, authStorage);
  }

  /**
   * List location areas
   * Location areas are sections of areas, such as floors in a building or cave. Each area has its own set of possible Pokémon encounters.
   * @param params.limit Number of results to return per page.
   * @param params.offset The initial index from which to return the results.
   */
  async locationAreaList(params: ILocationAreaListParams): Promise<PaginatedLocationAreaSummaryList> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/api/v2/location-area/`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'limit': params['limit'],
        'offset': params['offset'],
      })

      // Authentication 'basicAuth' required
      // Authentication 'cookieAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get location area
   * Location areas are sections of areas, such as floors in a building or cave. Each area has its own set of possible Pokémon encounters.
   * @param params.id A unique integer value identifying this location area.
   */
  async locationAreaRetrieve(params: ILocationAreaRetrieveParams): Promise<LocationAreaDetail> {
    // Verify required parameters are set
    this.ensureParamIsSet('locationAreaRetrieve', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/api/v2/location-area/{id}/`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'basicAuth' required
      // Authentication 'cookieAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * List locations
   * Locations that can be visited within the games. Locations make up sizable portions of regions, like cities or routes.
   * @param params.limit Number of results to return per page.
   * @param params.offset The initial index from which to return the results.
   * @param params.q &gt; Only available locally and not at [pokeapi.co](https://pokeapi.co/docs/v2) Case-insensitive query applied on the &#x60;name&#x60; property. 
   */
  async locationList(params: ILocationListParams): Promise<PaginatedLocationSummaryList> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/api/v2/location/`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'limit': params['limit'],
        'offset': params['offset'],
        'q': params['q'],
      })

      // Authentication 'basicAuth' required
      // Authentication 'cookieAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get location
   * Locations that can be visited within the games. Locations make up sizable portions of regions, like cities or routes.
   * @param params.id This parameter can be a string or an integer.
   */
  async locationRetrieve(params: ILocationRetrieveParams): Promise<LocationDetail> {
    // Verify required parameters are set
    this.ensureParamIsSet('locationRetrieve', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/api/v2/location/{id}/`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'basicAuth' required
      // Authentication 'cookieAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * List pal park areas
   * Areas used for grouping Pokémon encounters in Pal Park. They\&#39;re like habitats that are specific to Pal Park.
   * @param params.limit Number of results to return per page.
   * @param params.offset The initial index from which to return the results.
   * @param params.q &gt; Only available locally and not at [pokeapi.co](https://pokeapi.co/docs/v2) Case-insensitive query applied on the &#x60;name&#x60; property. 
   */
  async palParkAreaList(params: IPalParkAreaListParams): Promise<PaginatedPalParkAreaSummaryList> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/api/v2/pal-park-area/`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'limit': params['limit'],
        'offset': params['offset'],
        'q': params['q'],
      })

      // Authentication 'basicAuth' required
      // Authentication 'cookieAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get pal park area
   * Areas used for grouping Pokémon encounters in Pal Park. They\&#39;re like habitats that are specific to Pal Park.
   * @param params.id This parameter can be a string or an integer.
   */
  async palParkAreaRetrieve(params: IPalParkAreaRetrieveParams): Promise<PalParkAreaDetail> {
    // Verify required parameters are set
    this.ensureParamIsSet('palParkAreaRetrieve', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/api/v2/pal-park-area/{id}/`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'basicAuth' required
      // Authentication 'cookieAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * List regions
   * A region is an organized area of the Pokémon world. Most often, the main difference between regions is the species of Pokémon that can be encountered within them.
   * @param params.limit Number of results to return per page.
   * @param params.offset The initial index from which to return the results.
   * @param params.q &gt; Only available locally and not at [pokeapi.co](https://pokeapi.co/docs/v2) Case-insensitive query applied on the &#x60;name&#x60; property. 
   */
  async regionList(params: IRegionListParams): Promise<PaginatedRegionSummaryList> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/api/v2/region/`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'limit': params['limit'],
        'offset': params['offset'],
        'q': params['q'],
      })

      // Authentication 'basicAuth' required
      // Authentication 'cookieAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get region
   * A region is an organized area of the Pokémon world. Most often, the main difference between regions is the species of Pokémon that can be encountered within them.
   * @param params.id This parameter can be a string or an integer.
   */
  async regionRetrieve(params: IRegionRetrieveParams): Promise<RegionDetail> {
    // Verify required parameters are set
    this.ensureParamIsSet('regionRetrieve', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/api/v2/region/{id}/`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'basicAuth' required
      // Authentication 'cookieAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

}

