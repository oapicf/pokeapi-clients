/**
 * PokéAPI
 * All the Pokémon data you\'ll ever need in one place, easily accessible through a modern free open-source RESTful API.  ## What is this?  This is a full RESTful API linked to an extensive database detailing everything about the Pokémon main game series.  We\'ve covered everything from Pokémon to Berry Flavors.  ## Where do I start?  We have awesome [documentation](https://pokeapi.co/docs/v2) on how to use this API. It takes minutes to get started.  This API will always be publicly available and will never require any extensive setup process to consume.  Created by [**Paul Hallett**(]https://github.com/phalt) and other [**PokéAPI contributors***](https://github.com/PokeAPI/pokeapi#contributing) around the world. Pokémon and Pokémon character names are trademarks of Nintendo.     
 *
 * The version of the OpenAPI document: 2.7.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { autoinject } from 'aurelia-framework';
import { HttpClient } from 'aurelia-http-client';
import { Api } from './Api';
import { AuthStorage } from './AuthStorage';
import {
  PaginatedSuperContestEffectSummaryList,
  PaginatedContestEffectSummaryList,
  SuperContestEffectDetail,
  PaginatedContestTypeSummaryList,
  ContestTypeDetail,
  ContestEffectDetail,
} from './models';

/**
 * contestEffectList - parameters interface
 */
export interface IContestEffectListParams {
  limit?: number;
  offset?: number;
  q?: string;
}

/**
 * contestEffectRetrieve - parameters interface
 */
export interface IContestEffectRetrieveParams {
  id: string;
}

/**
 * contestTypeList - parameters interface
 */
export interface IContestTypeListParams {
  limit?: number;
  offset?: number;
  q?: string;
}

/**
 * contestTypeRetrieve - parameters interface
 */
export interface IContestTypeRetrieveParams {
  id: string;
}

/**
 * superContestEffectList - parameters interface
 */
export interface ISuperContestEffectListParams {
  limit?: number;
  offset?: number;
  q?: string;
}

/**
 * superContestEffectRetrieve - parameters interface
 */
export interface ISuperContestEffectRetrieveParams {
  id: string;
}

/**
 * ContestsApi - API class
 */
@autoinject()
export class ContestsApi extends Api {

  /**
   * Creates a new ContestsApi class.
   *
   * @param httpClient The Aurelia HTTP client to be injected.
   * @param authStorage A storage for authentication data.
   */
  constructor(httpClient: HttpClient, authStorage: AuthStorage) {
    super(httpClient, authStorage);
  }

  /**
   * List contest effects
   * Contest effects refer to the effects of moves when used in contests.
   * @param params.limit Number of results to return per page.
   * @param params.offset The initial index from which to return the results.
   * @param params.q &gt; Only available locally and not at [pokeapi.co](https://pokeapi.co/docs/v2) Case-insensitive query applied on the &#x60;name&#x60; property. 
   */
  async contestEffectList(params: IContestEffectListParams): Promise<PaginatedContestEffectSummaryList> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/api/v2/contest-effect/`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'limit': params['limit'],
        'offset': params['offset'],
        'q': params['q'],
      })

      // Authentication 'basicAuth' required
      // Authentication 'cookieAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get contest effect
   * Contest effects refer to the effects of moves when used in contests.
   * @param params.id This parameter can be a string or an integer.
   */
  async contestEffectRetrieve(params: IContestEffectRetrieveParams): Promise<ContestEffectDetail> {
    // Verify required parameters are set
    this.ensureParamIsSet('contestEffectRetrieve', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/api/v2/contest-effect/{id}/`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'basicAuth' required
      // Authentication 'cookieAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * List contest types
   * Contest types are categories judges used to weigh a Pokémon\&#39;s condition in Pokémon contests. Check out [Bulbapedia](http://bulbapedia.bulbagarden.net/wiki/Contest_condition) for greater detail.
   * @param params.limit Number of results to return per page.
   * @param params.offset The initial index from which to return the results.
   * @param params.q &gt; Only available locally and not at [pokeapi.co](https://pokeapi.co/docs/v2) Case-insensitive query applied on the &#x60;name&#x60; property. 
   */
  async contestTypeList(params: IContestTypeListParams): Promise<PaginatedContestTypeSummaryList> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/api/v2/contest-type/`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'limit': params['limit'],
        'offset': params['offset'],
        'q': params['q'],
      })

      // Authentication 'basicAuth' required
      // Authentication 'cookieAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get contest type
   * Contest types are categories judges used to weigh a Pokémon\&#39;s condition in Pokémon contests. Check out [Bulbapedia](http://bulbapedia.bulbagarden.net/wiki/Contest_condition) for greater detail.
   * @param params.id This parameter can be a string or an integer.
   */
  async contestTypeRetrieve(params: IContestTypeRetrieveParams): Promise<ContestTypeDetail> {
    // Verify required parameters are set
    this.ensureParamIsSet('contestTypeRetrieve', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/api/v2/contest-type/{id}/`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'basicAuth' required
      // Authentication 'cookieAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * List super contest effects
   * Super contest effects refer to the effects of moves when used in super contests.
   * @param params.limit Number of results to return per page.
   * @param params.offset The initial index from which to return the results.
   * @param params.q &gt; Only available locally and not at [pokeapi.co](https://pokeapi.co/docs/v2) Case-insensitive query applied on the &#x60;name&#x60; property. 
   */
  async superContestEffectList(params: ISuperContestEffectListParams): Promise<PaginatedSuperContestEffectSummaryList> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/api/v2/super-contest-effect/`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'limit': params['limit'],
        'offset': params['offset'],
        'q': params['q'],
      })

      // Authentication 'basicAuth' required
      // Authentication 'cookieAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get super contest effect
   * Super contest effects refer to the effects of moves when used in super contests.
   * @param params.id This parameter can be a string or an integer.
   */
  async superContestEffectRetrieve(params: ISuperContestEffectRetrieveParams): Promise<SuperContestEffectDetail> {
    // Verify required parameters are set
    this.ensureParamIsSet('superContestEffectRetrieve', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/api/v2/super-contest-effect/{id}/`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'basicAuth' required
      // Authentication 'cookieAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

}

