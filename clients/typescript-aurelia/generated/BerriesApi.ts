/**
 * PokéAPI
 * All the Pokémon data you\'ll ever need in one place, easily accessible through a modern free open-source RESTful API.  ## What is this?  This is a full RESTful API linked to an extensive database detailing everything about the Pokémon main game series.  We\'ve covered everything from Pokémon to Berry Flavors.  ## Where do I start?  We have awesome [documentation](https://pokeapi.co/docs/v2) on how to use this API. It takes minutes to get started.  This API will always be publicly available and will never require any extensive setup process to consume.  Created by [**Paul Hallett**(]https://github.com/phalt) and other [**PokéAPI contributors***](https://github.com/PokeAPI/pokeapi#contributing) around the world. Pokémon and Pokémon character names are trademarks of Nintendo.     
 *
 * The version of the OpenAPI document: 2.7.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { autoinject } from 'aurelia-framework';
import { HttpClient } from 'aurelia-http-client';
import { Api } from './Api';
import { AuthStorage } from './AuthStorage';
import {
  BerryFlavorDetail,
  BerryFirmnessDetail,
  BerryDetail,
  PaginatedBerryFlavorSummaryList,
  PaginatedBerrySummaryList,
  PaginatedBerryFirmnessSummaryList,
} from './models';

/**
 * berryFirmnessList - parameters interface
 */
export interface IBerryFirmnessListParams {
  limit?: number;
  offset?: number;
  q?: string;
}

/**
 * berryFirmnessRetrieve - parameters interface
 */
export interface IBerryFirmnessRetrieveParams {
  id: string;
}

/**
 * berryFlavorList - parameters interface
 */
export interface IBerryFlavorListParams {
  limit?: number;
  offset?: number;
  q?: string;
}

/**
 * berryFlavorRetrieve - parameters interface
 */
export interface IBerryFlavorRetrieveParams {
  id: string;
}

/**
 * berryList - parameters interface
 */
export interface IBerryListParams {
  limit?: number;
  offset?: number;
  q?: string;
}

/**
 * berryRetrieve - parameters interface
 */
export interface IBerryRetrieveParams {
  id: string;
}

/**
 * BerriesApi - API class
 */
@autoinject()
export class BerriesApi extends Api {

  /**
   * Creates a new BerriesApi class.
   *
   * @param httpClient The Aurelia HTTP client to be injected.
   * @param authStorage A storage for authentication data.
   */
  constructor(httpClient: HttpClient, authStorage: AuthStorage) {
    super(httpClient, authStorage);
  }

  /**
   * List berry firmness
   * Berries can be soft or hard. Check out [Bulbapedia](http://bulbapedia.bulbagarden.net/wiki/Category:Berries_by_firmness) for greater detail.
   * @param params.limit Number of results to return per page.
   * @param params.offset The initial index from which to return the results.
   * @param params.q &gt; Only available locally and not at [pokeapi.co](https://pokeapi.co/docs/v2) Case-insensitive query applied on the &#x60;name&#x60; property. 
   */
  async berryFirmnessList(params: IBerryFirmnessListParams): Promise<PaginatedBerryFirmnessSummaryList> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/api/v2/berry-firmness/`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'limit': params['limit'],
        'offset': params['offset'],
        'q': params['q'],
      })

      // Authentication 'basicAuth' required
      // Authentication 'cookieAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get berry by firmness
   * Berries can be soft or hard. Check out [Bulbapedia](http://bulbapedia.bulbagarden.net/wiki/Category:Berries_by_firmness) for greater detail.
   * @param params.id This parameter can be a string or an integer.
   */
  async berryFirmnessRetrieve(params: IBerryFirmnessRetrieveParams): Promise<BerryFirmnessDetail> {
    // Verify required parameters are set
    this.ensureParamIsSet('berryFirmnessRetrieve', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/api/v2/berry-firmness/{id}/`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'basicAuth' required
      // Authentication 'cookieAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * List berry flavors
   * Flavors determine whether a Pokémon will benefit or suffer from eating a berry based on their **nature**. Check out [Bulbapedia](http://bulbapedia.bulbagarden.net/wiki/Flavor) for greater detail.
   * @param params.limit Number of results to return per page.
   * @param params.offset The initial index from which to return the results.
   * @param params.q &gt; Only available locally and not at [pokeapi.co](https://pokeapi.co/docs/v2) Case-insensitive query applied on the &#x60;name&#x60; property. 
   */
  async berryFlavorList(params: IBerryFlavorListParams): Promise<PaginatedBerryFlavorSummaryList> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/api/v2/berry-flavor/`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'limit': params['limit'],
        'offset': params['offset'],
        'q': params['q'],
      })

      // Authentication 'basicAuth' required
      // Authentication 'cookieAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get berries by flavor
   * Flavors determine whether a Pokémon will benefit or suffer from eating a berry based on their **nature**. Check out [Bulbapedia](http://bulbapedia.bulbagarden.net/wiki/Flavor) for greater detail.
   * @param params.id This parameter can be a string or an integer.
   */
  async berryFlavorRetrieve(params: IBerryFlavorRetrieveParams): Promise<BerryFlavorDetail> {
    // Verify required parameters are set
    this.ensureParamIsSet('berryFlavorRetrieve', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/api/v2/berry-flavor/{id}/`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'basicAuth' required
      // Authentication 'cookieAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * List berries
   * Berries are small fruits that can provide HP and status condition restoration, stat enhancement, and even damage negation when eaten by Pokémon. Check out [Bulbapedia](http://bulbapedia.bulbagarden.net/wiki/Berry) for greater detail.
   * @param params.limit Number of results to return per page.
   * @param params.offset The initial index from which to return the results.
   * @param params.q &gt; Only available locally and not at [pokeapi.co](https://pokeapi.co/docs/v2) Case-insensitive query applied on the &#x60;name&#x60; property. 
   */
  async berryList(params: IBerryListParams): Promise<PaginatedBerrySummaryList> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/api/v2/berry/`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'limit': params['limit'],
        'offset': params['offset'],
        'q': params['q'],
      })

      // Authentication 'basicAuth' required
      // Authentication 'cookieAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get a berry
   * Berries are small fruits that can provide HP and status condition restoration, stat enhancement, and even damage negation when eaten by Pokémon. Check out [Bulbapedia](http://bulbapedia.bulbagarden.net/wiki/Berry) for greater detail.
   * @param params.id This parameter can be a string or an integer.
   */
  async berryRetrieve(params: IBerryRetrieveParams): Promise<BerryDetail> {
    // Verify required parameters are set
    this.ensureParamIsSet('berryRetrieve', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/api/v2/berry/{id}/`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'basicAuth' required
      // Authentication 'cookieAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

}

