/**
 * PokéAPI
 * All the Pokémon data you\'ll ever need in one place, easily accessible through a modern free open-source RESTful API.  ## What is this?  This is a full RESTful API linked to an extensive database detailing everything about the Pokémon main game series.  We\'ve covered everything from Pokémon to Berry Flavors.  ## Where do I start?  We have awesome [documentation](https://pokeapi.co/docs/v2) on how to use this API. It takes minutes to get started.  This API will always be publicly available and will never require any extensive setup process to consume.  Created by [**Paul Hallett**(]https://github.com/phalt) and other [**PokéAPI contributors***](https://github.com/PokeAPI/pokeapi#contributing) around the world. Pokémon and Pokémon character names are trademarks of Nintendo.     
 *
 * The version of the OpenAPI document: 2.7.0
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { autoinject } from 'aurelia-framework';
import { HttpClient } from 'aurelia-http-client';
import { Api } from './Api';
import { AuthStorage } from './AuthStorage';
import {
  PaginatedMoveSummaryList,
  MoveDetail,
  PaginatedMoveBattleStyleSummaryList,
  MoveLearnMethodDetail,
  MoveTargetDetail,
  PaginatedMoveTargetSummaryList,
  PaginatedMoveLearnMethodSummaryList,
  PaginatedMoveMetaAilmentSummaryList,
  PaginatedMoveMetaCategorySummaryList,
  MoveMetaCategoryDetail,
  MoveMetaAilmentDetail,
  MoveBattleStyleDetail,
} from './models';

/**
 * moveAilmentList - parameters interface
 */
export interface IMoveAilmentListParams {
  limit?: number;
  offset?: number;
  q?: string;
}

/**
 * moveAilmentRetrieve - parameters interface
 */
export interface IMoveAilmentRetrieveParams {
  id: string;
}

/**
 * moveBattleStyleList - parameters interface
 */
export interface IMoveBattleStyleListParams {
  limit?: number;
  offset?: number;
  q?: string;
}

/**
 * moveBattleStyleRetrieve - parameters interface
 */
export interface IMoveBattleStyleRetrieveParams {
  id: string;
}

/**
 * moveCategoryList - parameters interface
 */
export interface IMoveCategoryListParams {
  limit?: number;
  offset?: number;
  q?: string;
}

/**
 * moveCategoryRetrieve - parameters interface
 */
export interface IMoveCategoryRetrieveParams {
  id: string;
}

/**
 * moveLearnMethodList - parameters interface
 */
export interface IMoveLearnMethodListParams {
  limit?: number;
  offset?: number;
  q?: string;
}

/**
 * moveLearnMethodRetrieve - parameters interface
 */
export interface IMoveLearnMethodRetrieveParams {
  id: string;
}

/**
 * moveList - parameters interface
 */
export interface IMoveListParams {
  limit?: number;
  offset?: number;
  q?: string;
}

/**
 * moveRetrieve - parameters interface
 */
export interface IMoveRetrieveParams {
  id: string;
}

/**
 * moveTargetList - parameters interface
 */
export interface IMoveTargetListParams {
  limit?: number;
  offset?: number;
  q?: string;
}

/**
 * moveTargetRetrieve - parameters interface
 */
export interface IMoveTargetRetrieveParams {
  id: string;
}

/**
 * MovesApi - API class
 */
@autoinject()
export class MovesApi extends Api {

  /**
   * Creates a new MovesApi class.
   *
   * @param httpClient The Aurelia HTTP client to be injected.
   * @param authStorage A storage for authentication data.
   */
  constructor(httpClient: HttpClient, authStorage: AuthStorage) {
    super(httpClient, authStorage);
  }

  /**
   * List move meta ailments
   * Move Ailments are status conditions caused by moves used during battle. See [Bulbapedia](https://bulbapedia.bulbagarden.net/wiki/Status_condition) for greater detail.
   * @param params.limit Number of results to return per page.
   * @param params.offset The initial index from which to return the results.
   * @param params.q &gt; Only available locally and not at [pokeapi.co](https://pokeapi.co/docs/v2) Case-insensitive query applied on the &#x60;name&#x60; property. 
   */
  async moveAilmentList(params: IMoveAilmentListParams): Promise<PaginatedMoveMetaAilmentSummaryList> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/api/v2/move-ailment/`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'limit': params['limit'],
        'offset': params['offset'],
        'q': params['q'],
      })

      // Authentication 'basicAuth' required
      // Authentication 'cookieAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get move meta ailment
   * Move Ailments are status conditions caused by moves used during battle. See [Bulbapedia](https://bulbapedia.bulbagarden.net/wiki/Status_condition) for greater detail.
   * @param params.id This parameter can be a string or an integer.
   */
  async moveAilmentRetrieve(params: IMoveAilmentRetrieveParams): Promise<MoveMetaAilmentDetail> {
    // Verify required parameters are set
    this.ensureParamIsSet('moveAilmentRetrieve', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/api/v2/move-ailment/{id}/`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'basicAuth' required
      // Authentication 'cookieAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * List move battle styles
   * Styles of moves when used in the Battle Palace. See [Bulbapedia](http://bulbapedia.bulbagarden.net/wiki/Battle_Frontier_(Generation_III)) for greater detail.
   * @param params.limit Number of results to return per page.
   * @param params.offset The initial index from which to return the results.
   * @param params.q &gt; Only available locally and not at [pokeapi.co](https://pokeapi.co/docs/v2) Case-insensitive query applied on the &#x60;name&#x60; property. 
   */
  async moveBattleStyleList(params: IMoveBattleStyleListParams): Promise<PaginatedMoveBattleStyleSummaryList> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/api/v2/move-battle-style/`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'limit': params['limit'],
        'offset': params['offset'],
        'q': params['q'],
      })

      // Authentication 'basicAuth' required
      // Authentication 'cookieAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get move battle style
   * Styles of moves when used in the Battle Palace. See [Bulbapedia](http://bulbapedia.bulbagarden.net/wiki/Battle_Frontier_(Generation_III)) for greater detail.
   * @param params.id This parameter can be a string or an integer.
   */
  async moveBattleStyleRetrieve(params: IMoveBattleStyleRetrieveParams): Promise<MoveBattleStyleDetail> {
    // Verify required parameters are set
    this.ensureParamIsSet('moveBattleStyleRetrieve', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/api/v2/move-battle-style/{id}/`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'basicAuth' required
      // Authentication 'cookieAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * List move meta categories
   * Very general categories that loosely group move effects.
   * @param params.limit Number of results to return per page.
   * @param params.offset The initial index from which to return the results.
   * @param params.q &gt; Only available locally and not at [pokeapi.co](https://pokeapi.co/docs/v2) Case-insensitive query applied on the &#x60;name&#x60; property. 
   */
  async moveCategoryList(params: IMoveCategoryListParams): Promise<PaginatedMoveMetaCategorySummaryList> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/api/v2/move-category/`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'limit': params['limit'],
        'offset': params['offset'],
        'q': params['q'],
      })

      // Authentication 'basicAuth' required
      // Authentication 'cookieAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get move meta category
   * Very general categories that loosely group move effects.
   * @param params.id This parameter can be a string or an integer.
   */
  async moveCategoryRetrieve(params: IMoveCategoryRetrieveParams): Promise<MoveMetaCategoryDetail> {
    // Verify required parameters are set
    this.ensureParamIsSet('moveCategoryRetrieve', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/api/v2/move-category/{id}/`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'basicAuth' required
      // Authentication 'cookieAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * List move learn methods
   * Methods by which Pokémon can learn moves.
   * @param params.limit Number of results to return per page.
   * @param params.offset The initial index from which to return the results.
   * @param params.q &gt; Only available locally and not at [pokeapi.co](https://pokeapi.co/docs/v2) Case-insensitive query applied on the &#x60;name&#x60; property. 
   */
  async moveLearnMethodList(params: IMoveLearnMethodListParams): Promise<PaginatedMoveLearnMethodSummaryList> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/api/v2/move-learn-method/`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'limit': params['limit'],
        'offset': params['offset'],
        'q': params['q'],
      })

      // Authentication 'basicAuth' required
      // Authentication 'cookieAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get move learn method
   * Methods by which Pokémon can learn moves.
   * @param params.id This parameter can be a string or an integer.
   */
  async moveLearnMethodRetrieve(params: IMoveLearnMethodRetrieveParams): Promise<MoveLearnMethodDetail> {
    // Verify required parameters are set
    this.ensureParamIsSet('moveLearnMethodRetrieve', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/api/v2/move-learn-method/{id}/`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'basicAuth' required
      // Authentication 'cookieAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * List moves
   * Moves are the skills of Pokémon in battle. In battle, a Pokémon uses one move each turn. Some moves (including those learned by Hidden Machine) can be used outside of battle as well, usually for the purpose of removing obstacles or exploring new areas.
   * @param params.limit Number of results to return per page.
   * @param params.offset The initial index from which to return the results.
   * @param params.q &gt; Only available locally and not at [pokeapi.co](https://pokeapi.co/docs/v2) Case-insensitive query applied on the &#x60;name&#x60; property. 
   */
  async moveList(params: IMoveListParams): Promise<PaginatedMoveSummaryList> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/api/v2/move/`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'limit': params['limit'],
        'offset': params['offset'],
        'q': params['q'],
      })

      // Authentication 'basicAuth' required
      // Authentication 'cookieAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get move
   * Moves are the skills of Pokémon in battle. In battle, a Pokémon uses one move each turn. Some moves (including those learned by Hidden Machine) can be used outside of battle as well, usually for the purpose of removing obstacles or exploring new areas.
   * @param params.id This parameter can be a string or an integer.
   */
  async moveRetrieve(params: IMoveRetrieveParams): Promise<MoveDetail> {
    // Verify required parameters are set
    this.ensureParamIsSet('moveRetrieve', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/api/v2/move/{id}/`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'basicAuth' required
      // Authentication 'cookieAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * List move targets
   * Targets moves can be directed at during battle. Targets can be Pokémon, environments or even other moves.
   * @param params.limit Number of results to return per page.
   * @param params.offset The initial index from which to return the results.
   * @param params.q &gt; Only available locally and not at [pokeapi.co](https://pokeapi.co/docs/v2) Case-insensitive query applied on the &#x60;name&#x60; property. 
   */
  async moveTargetList(params: IMoveTargetListParams): Promise<PaginatedMoveTargetSummaryList> {
    // Verify required parameters are set

    // Create URL to call
    const url = `${this.basePath}/api/v2/move-target/`;

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()
      // Set query parameters
      .withParams({ 
        'limit': params['limit'],
        'offset': params['offset'],
        'q': params['q'],
      })

      // Authentication 'basicAuth' required
      // Authentication 'cookieAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

  /**
   * Get move target
   * Targets moves can be directed at during battle. Targets can be Pokémon, environments or even other moves.
   * @param params.id This parameter can be a string or an integer.
   */
  async moveTargetRetrieve(params: IMoveTargetRetrieveParams): Promise<MoveTargetDetail> {
    // Verify required parameters are set
    this.ensureParamIsSet('moveTargetRetrieve', params, 'id');

    // Create URL to call
    const url = `${this.basePath}/api/v2/move-target/{id}/`
      .replace(`{${'id'}}`, encodeURIComponent(`${params['id']}`));

    const response = await this.httpClient.createRequest(url)
      // Set HTTP method
      .asGet()

      // Authentication 'basicAuth' required
      // Authentication 'cookieAuth' required
      // Send the request
      .send();

    if (response.statusCode < 200 || response.statusCode >= 300) {
      throw new Error(response.content);
    }

    // Extract the content
    return response.content;
  }

}

