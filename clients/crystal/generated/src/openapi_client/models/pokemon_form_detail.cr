# #PokéAPI
#
##All the Pokémon data you'll ever need in one place, easily accessible through a modern free open-source RESTful API.  ## What is this?  This is a full RESTful API linked to an extensive database detailing everything about the Pokémon main game series.  We've covered everything from Pokémon to Berry Flavors.  ## Where do I start?  We have awesome [documentation](https://pokeapi.co/docs/v2) on how to use this API. It takes minutes to get started.  This API will always be publicly available and will never require any extensive setup process to consume.  Created by [**Paul Hallett**(]https://github.com/phalt) and other [**PokéAPI contributors***](https://github.com/PokeAPI/pokeapi#contributing) around the world. Pokémon and Pokémon character names are trademarks of Nintendo.     
#
#The version of the OpenAPI document: 2.7.0
#Contact: blah+oapicf@cliffano.com
#Generated by: https://openapi-generator.tech
#Generator version: 7.18.0
#

require "big"
require "json"
require "yaml"
require "time"

module OpenAPIClient
  class PokemonFormDetail
    include JSON::Serializable
    include YAML::Serializable

    # Required properties
    @[JSON::Field(key: "id", type: Int32, nillable: false, emit_null: false)]
    property id : Int32

    @[JSON::Field(key: "name", type: String, nillable: false, emit_null: false)]
    property name : String

    @[JSON::Field(key: "form_name", type: String, nillable: false, emit_null: false)]
    property form_name : String

    @[JSON::Field(key: "pokemon", type: PokemonSummary, nillable: false, emit_null: false)]
    property pokemon : PokemonSummary

    @[JSON::Field(key: "sprites", type: PokemonFormDetailSprites, nillable: false, emit_null: false)]
    property sprites : PokemonFormDetailSprites

    @[JSON::Field(key: "version_group", type: VersionGroupSummary, nillable: false, emit_null: false)]
    property version_group : VersionGroupSummary

    @[JSON::Field(key: "form_names", type: Array(PokemonFormDetailFormNamesInner), nillable: false, emit_null: false)]
    property form_names : Array(PokemonFormDetailFormNamesInner)

    @[JSON::Field(key: "names", type: Array(PokemonFormDetailFormNamesInner), nillable: false, emit_null: false)]
    property names : Array(PokemonFormDetailFormNamesInner)

    @[JSON::Field(key: "types", type: Array(PokemonDetailTypesInner), nillable: false, emit_null: false)]
    property types : Array(PokemonDetailTypesInner)

    # Optional properties
    @[JSON::Field(key: "order", type: Int32?, nillable: true, emit_null: false)]
    property order : Int32?

    @[JSON::Field(key: "form_order", type: Int32?, nillable: true, emit_null: false)]
    property form_order : Int32?

    @[JSON::Field(key: "is_default", type: Bool?, nillable: true, emit_null: false)]
    property is_default : Bool?

    @[JSON::Field(key: "is_battle_only", type: Bool?, nillable: true, emit_null: false)]
    property is_battle_only : Bool?

    @[JSON::Field(key: "is_mega", type: Bool?, nillable: true, emit_null: false)]
    property is_mega : Bool?

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(@id : Int32, @name : String, @form_name : String, @pokemon : PokemonSummary, @sprites : PokemonFormDetailSprites, @version_group : VersionGroupSummary, @form_names : Array(PokemonFormDetailFormNamesInner), @names : Array(PokemonFormDetailFormNamesInner), @types : Array(PokemonDetailTypesInner), @order : Int32? = nil, @form_order : Int32? = nil, @is_default : Bool? = nil, @is_battle_only : Bool? = nil, @is_mega : Bool? = nil)
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array(String).new
      if @name.try &.to_s.try &.size.try &.> 100
        invalid_properties.push("invalid value for \"name\", the character length must be smaller than or equal to 100.")
      end

      if @form_name.try &.to_s.try &.size.try &.> 30
        invalid_properties.push("invalid value for \"form_name\", the character length must be smaller than or equal to 30.")
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      return false if @name.try &.to_s.try &.size.try &.> 100
      return false if @form_name.try &.to_s.try &.size.try &.> 30
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] name Value to be assigned
    def name=(name)
      if name.to_s.size > 100
        raise ArgumentError.new("invalid value for \"name\", the character length must be smaller than or equal to 100.")
      end

      @name = name
    end

    # Custom attribute writer method with validation
    # @param [Object] form_name Value to be assigned
    def form_name=(form_name)
      if form_name.to_s.size > 30
        raise ArgumentError.new("invalid value for \"form_name\", the character length must be smaller than or equal to 30.")
      end

      @form_name = form_name
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if self.same?(other)
      self.class == other.class &&
          id == other.id &&
          name == other.name &&
          order == other.order &&
          form_order == other.form_order &&
          is_default == other.is_default &&
          is_battle_only == other.is_battle_only &&
          is_mega == other.is_mega &&
          form_name == other.form_name &&
          pokemon == other.pokemon &&
          sprites == other.sprites &&
          version_group == other.version_group &&
          form_names == other.form_names &&
          names == other.names &&
          types == other.types
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [id, name, order, form_order, is_default, is_battle_only, is_mega, form_name, pokemon, sprites, version_group, form_names, names, types].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if !attributes[self.class.attribute_map[key]]? && self.class.openapi_nullable.includes?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        ({} of Symbol => String).tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = OpenAPIClient.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_h.to_s
    end

    # to_body is an alias to to_h (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_h
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_h
      hash = NetboxClient::RecursiveHash.new
      hash["id"] = _to_h(id)
      hash["name"] = _to_h(name)
      hash["order"] = _to_h(order)
      hash["form_order"] = _to_h(form_order)
      hash["is_default"] = _to_h(is_default)
      hash["is_battle_only"] = _to_h(is_battle_only)
      hash["is_mega"] = _to_h(is_mega)
      hash["form_name"] = _to_h(form_name)
      hash["pokemon"] = _to_h(pokemon)
      hash["sprites"] = _to_h(sprites)
      hash["version_group"] = _to_h(version_group)
      hash["form_names"] = _to_h(form_names)
      hash["names"] = _to_h(names)
      hash["types"] = _to_h(types)
      hash.to_h
    end

    # Outputs non-array value in the form of hash
    # For object, use to_h. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    private def _to_h(value)
      if value.is_a?(Hash)
        hash = NetboxClient::RecursiveHash.new
        value.each { |k, v| hash[k] = _to_h(v) }
        hash
      elsif value.is_a?(Array)
        value.compact.map { |v| _to_h(v) }
      elsif value.responds_to?(:to_h)
        value.to_h
      else
        value
      end
    end

  end

end
