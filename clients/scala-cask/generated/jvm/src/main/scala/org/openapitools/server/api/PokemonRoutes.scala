//> using scala "3.3.1"
//> using lib "com.lihaoyi::cask:0.8.3"
//> using lib "com.lihaoyi::scalatags:0.12.0"
/**
 * PokéAPI
 * All the Pokémon data you'll ever need in one place, easily accessible through a modern free open-source RESTful API.  ## What is this?  This is a full RESTful API linked to an extensive database detailing everything about the Pokémon main game series.  We've covered everything from Pokémon to Berry Flavors.  ## Where do I start?  We have awesome [documentation](https://pokeapi.co/docs/v2) on how to use this API. It takes minutes to get started.  This API will always be publicly available and will never require any extensive setup process to consume.  Created by [**Paul Hallett**(]https://github.com/phalt) and other [**PokéAPI contributors***](https://github.com/PokeAPI/pokeapi#contributing) around the world. Pokémon and Pokémon character names are trademarks of Nintendo.     
 *
 * OpenAPI spec version: 2.7.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */


// this is generated from apiRoutes.mustache
package org.openapitools.server.api

import org.openapitools.server.model.*

import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*
import scala.util.Try

import org.openapitools.server.model.AbilityDetail
import org.openapitools.server.model.CharacteristicDetail
import org.openapitools.server.model.EggGroupDetail
import org.openapitools.server.model.GenderDetail
import org.openapitools.server.model.GrowthRateDetail
import org.openapitools.server.model.MoveDamageClassDetail
import org.openapitools.server.model.NatureDetail
import org.openapitools.server.model.PaginatedAbilitySummaryList
import org.openapitools.server.model.PaginatedCharacteristicSummaryList
import org.openapitools.server.model.PaginatedEggGroupSummaryList
import org.openapitools.server.model.PaginatedGenderSummaryList
import org.openapitools.server.model.PaginatedGrowthRateSummaryList
import org.openapitools.server.model.PaginatedMoveDamageClassSummaryList
import org.openapitools.server.model.PaginatedNatureSummaryList
import org.openapitools.server.model.PaginatedPokeathlonStatSummaryList
import org.openapitools.server.model.PaginatedPokemonColorSummaryList
import org.openapitools.server.model.PaginatedPokemonFormSummaryList
import org.openapitools.server.model.PaginatedPokemonHabitatSummaryList
import org.openapitools.server.model.PaginatedPokemonShapeSummaryList
import org.openapitools.server.model.PaginatedPokemonSpeciesSummaryList
import org.openapitools.server.model.PaginatedPokemonSummaryList
import org.openapitools.server.model.PaginatedStatSummaryList
import org.openapitools.server.model.PaginatedTypeSummaryList
import org.openapitools.server.model.PokeathlonStatDetail
import org.openapitools.server.model.PokemonColorDetail
import org.openapitools.server.model.PokemonDetail
import org.openapitools.server.model.PokemonFormDetail
import org.openapitools.server.model.PokemonHabitatDetail
import org.openapitools.server.model.PokemonShapeDetail
import org.openapitools.server.model.PokemonSpeciesDetail
import org.openapitools.server.model.StatDetail
import org.openapitools.server.model.TypeDetail

class PokemonRoutes(service : PokemonService[Try]) extends cask.Routes {

    // route group for routeWorkAroundForGETApiV2Pokemonspecies
    @cask.get("/api/v2/pokemon-species", true)
    def routeWorkAroundForGETApiV2Pokemonspecies(request: cask.Request,limit : Option[Int] = None,offset : Option[Int] = None,q : Option[String] = None) = {
        request.remainingPathSegments match {
            case Seq("-species") => pokemonSpeciesList(request,limit, offset, q)
            case Seq("-species",id) => pokemonSpeciesRetrieve(id,request)
            case _          => cask.Response("Not Found", statusCode = 404)
        }
    }
    // route group for routeWorkAroundForGETApiV2Stat
    @cask.get("/api/v2/stat", true)
    def routeWorkAroundForGETApiV2Stat(request: cask.Request,limit : Option[Int] = None,offset : Option[Int] = None,q : Option[String] = None) = {
        request.remainingPathSegments match {
            case Seq() => statList(request,limit, offset, q)
            case Seq(id) => statRetrieve(id,request)
            case _          => cask.Response("Not Found", statusCode = 404)
        }
    }
    // route group for routeWorkAroundForGETApiV2Gender
    @cask.get("/api/v2/gender", true)
    def routeWorkAroundForGETApiV2Gender(request: cask.Request,limit : Option[Int] = None,offset : Option[Int] = None,q : Option[String] = None) = {
        request.remainingPathSegments match {
            case Seq() => genderList(request,limit, offset, q)
            case Seq(id) => genderRetrieve(id,request)
            case _          => cask.Response("Not Found", statusCode = 404)
        }
    }
    // route group for routeWorkAroundForGETApiV2Growthrate
    @cask.get("/api/v2/growth-rate", true)
    def routeWorkAroundForGETApiV2Growthrate(request: cask.Request,limit : Option[Int] = None,offset : Option[Int] = None,q : Option[String] = None) = {
        request.remainingPathSegments match {
            case Seq() => growthRateList(request,limit, offset, q)
            case Seq(id) => growthRateRetrieve(id,request)
            case _          => cask.Response("Not Found", statusCode = 404)
        }
    }
    // route group for routeWorkAroundForGETApiV2Movedamageclass
    @cask.get("/api/v2/move-damage-class", true)
    def routeWorkAroundForGETApiV2Movedamageclass(request: cask.Request,limit : Option[Int] = None,offset : Option[Int] = None,q : Option[String] = None) = {
        request.remainingPathSegments match {
            case Seq() => moveDamageClassList(request,limit, offset, q)
            case Seq(id) => moveDamageClassRetrieve(id,request)
            case _          => cask.Response("Not Found", statusCode = 404)
        }
    }
    // route group for routeWorkAroundForGETApiV2Pokeathlonstat
    @cask.get("/api/v2/pokeathlon-stat", true)
    def routeWorkAroundForGETApiV2Pokeathlonstat(request: cask.Request,limit : Option[Int] = None,offset : Option[Int] = None,q : Option[String] = None) = {
        request.remainingPathSegments match {
            case Seq() => pokeathlonStatList(request,limit, offset, q)
            case Seq(id) => pokeathlonStatRetrieve(id,request)
            case _          => cask.Response("Not Found", statusCode = 404)
        }
    }
    // route group for routeWorkAroundForGETApiV2Egggroup
    @cask.get("/api/v2/egg-group", true)
    def routeWorkAroundForGETApiV2Egggroup(request: cask.Request,limit : Option[Int] = None,offset : Option[Int] = None,q : Option[String] = None) = {
        request.remainingPathSegments match {
            case Seq() => eggGroupList(request,limit, offset, q)
            case Seq(id) => eggGroupRetrieve(id,request)
            case _          => cask.Response("Not Found", statusCode = 404)
        }
    }
    // route group for routeWorkAroundForGETApiV2Pokemon
    @cask.get("/api/v2/pokemon", true)
    def routeWorkAroundForGETApiV2Pokemon(request: cask.Request,limit : Option[Int] = None,offset : Option[Int] = None,q : Option[String] = None) = {
        request.remainingPathSegments match {
            case Seq() => pokemonColorList(request,limit, offset, q)
            case Seq() => pokemonFormList(request,limit, offset, q)
            case Seq() => pokemonHabitatList(request,limit, offset, q)
            case Seq() => pokemonList(request,limit, offset, q)
            case Seq() => pokemonShapeList(request,limit, offset, q)
            case Seq("-species") => pokemonSpeciesList(request,limit, offset, q)
            case Seq(id) => pokemonRetrieve(id,request)
            case Seq("-color",id) => pokemonColorRetrieve(id,request)
            case Seq("-form",id) => pokemonFormRetrieve(id,request)
            case Seq("-habitat",id) => pokemonHabitatRetrieve(id,request)
            case Seq("-shape",id) => pokemonShapeRetrieve(id,request)
            case Seq("-species",id) => pokemonSpeciesRetrieve(id,request)
            case _          => cask.Response("Not Found", statusCode = 404)
        }
    }
    // route group for routeWorkAroundForGETApiV2Pokemonshape
    @cask.get("/api/v2/pokemon-shape", true)
    def routeWorkAroundForGETApiV2Pokemonshape(request: cask.Request,limit : Option[Int] = None,offset : Option[Int] = None,q : Option[String] = None) = {
        request.remainingPathSegments match {
            case Seq() => pokemonShapeList(request,limit, offset, q)
            case Seq("-shape",id) => pokemonShapeRetrieve(id,request)
            case _          => cask.Response("Not Found", statusCode = 404)
        }
    }
    // route group for routeWorkAroundForGETApiV2Pokemonhabitat
    @cask.get("/api/v2/pokemon-habitat", true)
    def routeWorkAroundForGETApiV2Pokemonhabitat(request: cask.Request,limit : Option[Int] = None,offset : Option[Int] = None,q : Option[String] = None) = {
        request.remainingPathSegments match {
            case Seq() => pokemonHabitatList(request,limit, offset, q)
            case Seq("-habitat",id) => pokemonHabitatRetrieve(id,request)
            case _          => cask.Response("Not Found", statusCode = 404)
        }
    }
    // route group for routeWorkAroundForGETApiV2Pokemoncolor
    @cask.get("/api/v2/pokemon-color", true)
    def routeWorkAroundForGETApiV2Pokemoncolor(request: cask.Request,limit : Option[Int] = None,offset : Option[Int] = None,q : Option[String] = None) = {
        request.remainingPathSegments match {
            case Seq() => pokemonColorList(request,limit, offset, q)
            case Seq("-color",id) => pokemonColorRetrieve(id,request)
            case _          => cask.Response("Not Found", statusCode = 404)
        }
    }
    // route group for routeWorkAroundForGETApiV2Nature
    @cask.get("/api/v2/nature", true)
    def routeWorkAroundForGETApiV2Nature(request: cask.Request,limit : Option[Int] = None,offset : Option[Int] = None,q : Option[String] = None) = {
        request.remainingPathSegments match {
            case Seq() => natureList(request,limit, offset, q)
            case Seq(id) => natureRetrieve(id,request)
            case _          => cask.Response("Not Found", statusCode = 404)
        }
    }
    // route group for routeWorkAroundForGETApiV2Pokemonform
    @cask.get("/api/v2/pokemon-form", true)
    def routeWorkAroundForGETApiV2Pokemonform(request: cask.Request,limit : Option[Int] = None,offset : Option[Int] = None,q : Option[String] = None) = {
        request.remainingPathSegments match {
            case Seq() => pokemonFormList(request,limit, offset, q)
            case Seq("-form",id) => pokemonFormRetrieve(id,request)
            case _          => cask.Response("Not Found", statusCode = 404)
        }
    }
    // route group for routeWorkAroundForGETApiV2Characteristic
    @cask.get("/api/v2/characteristic", true)
    def routeWorkAroundForGETApiV2Characteristic(request: cask.Request,limit : Option[Int] = None,offset : Option[Int] = None,q : Option[String] = None) = {
        request.remainingPathSegments match {
            case Seq() => characteristicList(request,limit, offset, q)
            case Seq(id) => characteristicRetrieve(id,request)
            case _          => cask.Response("Not Found", statusCode = 404)
        }
    }
    // route group for routeWorkAroundForGETApiV2Ability
    @cask.get("/api/v2/ability", true)
    def routeWorkAroundForGETApiV2Ability(request: cask.Request,limit : Option[Int] = None,offset : Option[Int] = None,q : Option[String] = None) = {
        request.remainingPathSegments match {
            case Seq() => abilityList(request,limit, offset, q)
            case Seq(id) => abilityRetrieve(id,request)
            case _          => cask.Response("Not Found", statusCode = 404)
        }
    }
    // route group for routeWorkAroundForGETApiV2Type
    @cask.get("/api/v2/type", true)
    def routeWorkAroundForGETApiV2Type(request: cask.Request,limit : Option[Int] = None,offset : Option[Int] = None,q : Option[String] = None) = {
        request.remainingPathSegments match {
            case Seq() => typeList(request,limit, offset, q)
            case Seq(id) => typeRetrieve(id,request)
            case _          => cask.Response("Not Found", statusCode = 404)
        }
    }

        /** 
         * 
         */
        // conflicts with [/api/v2/ability/{id}/, /api/v2/ability/] after/api/v2/ability, ignoring @cask.get("/api/v2/ability/")
        def abilityList(request: cask.Request, limit : Option[Int], offset : Option[Int], q : Option[String]) = {
            // auth method basicAuth : http, keyParamName: 
            // auth method cookieAuth : apiKey, keyParamName: sessionid

        def failFast = request.queryParams.keySet.contains("failFast")

        val result =         for {
            resultTry <- Parsed.eval(service.abilityList(limit, offset, q))
            result <- Parsed.fromTry(resultTry)
        } yield result

        import PaginatedAbilitySummaryList.{given, *} // this brings in upickle in the case of union (oneOf) types

        (result : @unchecked) match {
          case Left(error) => cask.Response(error, 500)
          case Right(value : PaginatedAbilitySummaryList) => cask.Response(data = write(value), 200, headers = Seq("Content-Type" -> "application/json"))
          case Right(other) => cask.Response(s"$other", 200)
        }
      }
        /** 
         * 
         */
        // conflicts with [/api/v2/ability/{id}/, /api/v2/ability/] after/api/v2/ability, ignoring @cask.get("/api/v2/ability/:id/")
        def abilityRetrieve(id : String, request: cask.Request) = {
            // auth method basicAuth : http, keyParamName: 
            // auth method cookieAuth : apiKey, keyParamName: sessionid

        def failFast = request.queryParams.keySet.contains("failFast")

        val result =         for {
            id <- Parsed(id)
            resultTry <- Parsed.eval(service.abilityRetrieve(id))
            result <- Parsed.fromTry(resultTry)
        } yield result

        import AbilityDetail.{given, *} // this brings in upickle in the case of union (oneOf) types

        (result : @unchecked) match {
          case Left(error) => cask.Response(error, 500)
          case Right(value : AbilityDetail) => cask.Response(data = write(value), 200, headers = Seq("Content-Type" -> "application/json"))
          case Right(other) => cask.Response(s"$other", 200)
        }
      }
        /** List charecterictics
         * 
         */
        // conflicts with [/api/v2/characteristic/{id}/, /api/v2/characteristic/] after/api/v2/characteristic, ignoring @cask.get("/api/v2/characteristic/")
        def characteristicList(request: cask.Request, limit : Option[Int], offset : Option[Int], q : Option[String]) = {
            // auth method basicAuth : http, keyParamName: 
            // auth method cookieAuth : apiKey, keyParamName: sessionid

        def failFast = request.queryParams.keySet.contains("failFast")

        val result =         for {
            resultTry <- Parsed.eval(service.characteristicList(limit, offset, q))
            result <- Parsed.fromTry(resultTry)
        } yield result

        import PaginatedCharacteristicSummaryList.{given, *} // this brings in upickle in the case of union (oneOf) types

        (result : @unchecked) match {
          case Left(error) => cask.Response(error, 500)
          case Right(value : PaginatedCharacteristicSummaryList) => cask.Response(data = write(value), 200, headers = Seq("Content-Type" -> "application/json"))
          case Right(other) => cask.Response(s"$other", 200)
        }
      }
        /** Get characteristic
         * 
         */
        // conflicts with [/api/v2/characteristic/{id}/, /api/v2/characteristic/] after/api/v2/characteristic, ignoring @cask.get("/api/v2/characteristic/:id/")
        def characteristicRetrieve(id : String, request: cask.Request) = {
            // auth method basicAuth : http, keyParamName: 
            // auth method cookieAuth : apiKey, keyParamName: sessionid

        def failFast = request.queryParams.keySet.contains("failFast")

        val result =         for {
            id <- Parsed(id)
            resultTry <- Parsed.eval(service.characteristicRetrieve(id))
            result <- Parsed.fromTry(resultTry)
        } yield result

        import CharacteristicDetail.{given, *} // this brings in upickle in the case of union (oneOf) types

        (result : @unchecked) match {
          case Left(error) => cask.Response(error, 500)
          case Right(value : CharacteristicDetail) => cask.Response(data = write(value), 200, headers = Seq("Content-Type" -> "application/json"))
          case Right(other) => cask.Response(s"$other", 200)
        }
      }
        /** List egg groups
         * 
         */
        // conflicts with [/api/v2/egg-group/{id}/, /api/v2/egg-group/] after/api/v2/egg-group, ignoring @cask.get("/api/v2/egg-group/")
        def eggGroupList(request: cask.Request, limit : Option[Int], offset : Option[Int], q : Option[String]) = {
            // auth method basicAuth : http, keyParamName: 
            // auth method cookieAuth : apiKey, keyParamName: sessionid

        def failFast = request.queryParams.keySet.contains("failFast")

        val result =         for {
            resultTry <- Parsed.eval(service.eggGroupList(limit, offset, q))
            result <- Parsed.fromTry(resultTry)
        } yield result

        import PaginatedEggGroupSummaryList.{given, *} // this brings in upickle in the case of union (oneOf) types

        (result : @unchecked) match {
          case Left(error) => cask.Response(error, 500)
          case Right(value : PaginatedEggGroupSummaryList) => cask.Response(data = write(value), 200, headers = Seq("Content-Type" -> "application/json"))
          case Right(other) => cask.Response(s"$other", 200)
        }
      }
        /** Get egg group
         * 
         */
        // conflicts with [/api/v2/egg-group/{id}/, /api/v2/egg-group/] after/api/v2/egg-group, ignoring @cask.get("/api/v2/egg-group/:id/")
        def eggGroupRetrieve(id : String, request: cask.Request) = {
            // auth method basicAuth : http, keyParamName: 
            // auth method cookieAuth : apiKey, keyParamName: sessionid

        def failFast = request.queryParams.keySet.contains("failFast")

        val result =         for {
            id <- Parsed(id)
            resultTry <- Parsed.eval(service.eggGroupRetrieve(id))
            result <- Parsed.fromTry(resultTry)
        } yield result

        import EggGroupDetail.{given, *} // this brings in upickle in the case of union (oneOf) types

        (result : @unchecked) match {
          case Left(error) => cask.Response(error, 500)
          case Right(value : EggGroupDetail) => cask.Response(data = write(value), 200, headers = Seq("Content-Type" -> "application/json"))
          case Right(other) => cask.Response(s"$other", 200)
        }
      }
        /** List genders
         * 
         */
        // conflicts with [/api/v2/gender/{id}/, /api/v2/gender/] after/api/v2/gender, ignoring @cask.get("/api/v2/gender/")
        def genderList(request: cask.Request, limit : Option[Int], offset : Option[Int], q : Option[String]) = {
            // auth method basicAuth : http, keyParamName: 
            // auth method cookieAuth : apiKey, keyParamName: sessionid

        def failFast = request.queryParams.keySet.contains("failFast")

        val result =         for {
            resultTry <- Parsed.eval(service.genderList(limit, offset, q))
            result <- Parsed.fromTry(resultTry)
        } yield result

        import PaginatedGenderSummaryList.{given, *} // this brings in upickle in the case of union (oneOf) types

        (result : @unchecked) match {
          case Left(error) => cask.Response(error, 500)
          case Right(value : PaginatedGenderSummaryList) => cask.Response(data = write(value), 200, headers = Seq("Content-Type" -> "application/json"))
          case Right(other) => cask.Response(s"$other", 200)
        }
      }
        /** Get gender
         * 
         */
        // conflicts with [/api/v2/gender/{id}/, /api/v2/gender/] after/api/v2/gender, ignoring @cask.get("/api/v2/gender/:id/")
        def genderRetrieve(id : String, request: cask.Request) = {
            // auth method basicAuth : http, keyParamName: 
            // auth method cookieAuth : apiKey, keyParamName: sessionid

        def failFast = request.queryParams.keySet.contains("failFast")

        val result =         for {
            id <- Parsed(id)
            resultTry <- Parsed.eval(service.genderRetrieve(id))
            result <- Parsed.fromTry(resultTry)
        } yield result

        import GenderDetail.{given, *} // this brings in upickle in the case of union (oneOf) types

        (result : @unchecked) match {
          case Left(error) => cask.Response(error, 500)
          case Right(value : GenderDetail) => cask.Response(data = write(value), 200, headers = Seq("Content-Type" -> "application/json"))
          case Right(other) => cask.Response(s"$other", 200)
        }
      }
        /** List growth rates
         * 
         */
        // conflicts with [/api/v2/growth-rate/{id}/, /api/v2/growth-rate/] after/api/v2/growth-rate, ignoring @cask.get("/api/v2/growth-rate/")
        def growthRateList(request: cask.Request, limit : Option[Int], offset : Option[Int], q : Option[String]) = {
            // auth method basicAuth : http, keyParamName: 
            // auth method cookieAuth : apiKey, keyParamName: sessionid

        def failFast = request.queryParams.keySet.contains("failFast")

        val result =         for {
            resultTry <- Parsed.eval(service.growthRateList(limit, offset, q))
            result <- Parsed.fromTry(resultTry)
        } yield result

        import PaginatedGrowthRateSummaryList.{given, *} // this brings in upickle in the case of union (oneOf) types

        (result : @unchecked) match {
          case Left(error) => cask.Response(error, 500)
          case Right(value : PaginatedGrowthRateSummaryList) => cask.Response(data = write(value), 200, headers = Seq("Content-Type" -> "application/json"))
          case Right(other) => cask.Response(s"$other", 200)
        }
      }
        /** Get growth rate
         * 
         */
        // conflicts with [/api/v2/growth-rate/{id}/, /api/v2/growth-rate/] after/api/v2/growth-rate, ignoring @cask.get("/api/v2/growth-rate/:id/")
        def growthRateRetrieve(id : String, request: cask.Request) = {
            // auth method basicAuth : http, keyParamName: 
            // auth method cookieAuth : apiKey, keyParamName: sessionid

        def failFast = request.queryParams.keySet.contains("failFast")

        val result =         for {
            id <- Parsed(id)
            resultTry <- Parsed.eval(service.growthRateRetrieve(id))
            result <- Parsed.fromTry(resultTry)
        } yield result

        import GrowthRateDetail.{given, *} // this brings in upickle in the case of union (oneOf) types

        (result : @unchecked) match {
          case Left(error) => cask.Response(error, 500)
          case Right(value : GrowthRateDetail) => cask.Response(data = write(value), 200, headers = Seq("Content-Type" -> "application/json"))
          case Right(other) => cask.Response(s"$other", 200)
        }
      }
        /** List move damage classes
         * 
         */
        // conflicts with [/api/v2/move-damage-class/{id}/, /api/v2/move-damage-class/] after/api/v2/move-damage-class, ignoring @cask.get("/api/v2/move-damage-class/")
        def moveDamageClassList(request: cask.Request, limit : Option[Int], offset : Option[Int], q : Option[String]) = {
            // auth method basicAuth : http, keyParamName: 
            // auth method cookieAuth : apiKey, keyParamName: sessionid

        def failFast = request.queryParams.keySet.contains("failFast")

        val result =         for {
            resultTry <- Parsed.eval(service.moveDamageClassList(limit, offset, q))
            result <- Parsed.fromTry(resultTry)
        } yield result

        import PaginatedMoveDamageClassSummaryList.{given, *} // this brings in upickle in the case of union (oneOf) types

        (result : @unchecked) match {
          case Left(error) => cask.Response(error, 500)
          case Right(value : PaginatedMoveDamageClassSummaryList) => cask.Response(data = write(value), 200, headers = Seq("Content-Type" -> "application/json"))
          case Right(other) => cask.Response(s"$other", 200)
        }
      }
        /** Get move damage class
         * 
         */
        // conflicts with [/api/v2/move-damage-class/{id}/, /api/v2/move-damage-class/] after/api/v2/move-damage-class, ignoring @cask.get("/api/v2/move-damage-class/:id/")
        def moveDamageClassRetrieve(id : String, request: cask.Request) = {
            // auth method basicAuth : http, keyParamName: 
            // auth method cookieAuth : apiKey, keyParamName: sessionid

        def failFast = request.queryParams.keySet.contains("failFast")

        val result =         for {
            id <- Parsed(id)
            resultTry <- Parsed.eval(service.moveDamageClassRetrieve(id))
            result <- Parsed.fromTry(resultTry)
        } yield result

        import MoveDamageClassDetail.{given, *} // this brings in upickle in the case of union (oneOf) types

        (result : @unchecked) match {
          case Left(error) => cask.Response(error, 500)
          case Right(value : MoveDamageClassDetail) => cask.Response(data = write(value), 200, headers = Seq("Content-Type" -> "application/json"))
          case Right(other) => cask.Response(s"$other", 200)
        }
      }
        /** List natures
         * 
         */
        // conflicts with [/api/v2/nature/{id}/, /api/v2/nature/] after/api/v2/nature, ignoring @cask.get("/api/v2/nature/")
        def natureList(request: cask.Request, limit : Option[Int], offset : Option[Int], q : Option[String]) = {
            // auth method basicAuth : http, keyParamName: 
            // auth method cookieAuth : apiKey, keyParamName: sessionid

        def failFast = request.queryParams.keySet.contains("failFast")

        val result =         for {
            resultTry <- Parsed.eval(service.natureList(limit, offset, q))
            result <- Parsed.fromTry(resultTry)
        } yield result

        import PaginatedNatureSummaryList.{given, *} // this brings in upickle in the case of union (oneOf) types

        (result : @unchecked) match {
          case Left(error) => cask.Response(error, 500)
          case Right(value : PaginatedNatureSummaryList) => cask.Response(data = write(value), 200, headers = Seq("Content-Type" -> "application/json"))
          case Right(other) => cask.Response(s"$other", 200)
        }
      }
        /** Get nature
         * 
         */
        // conflicts with [/api/v2/nature/{id}/, /api/v2/nature/] after/api/v2/nature, ignoring @cask.get("/api/v2/nature/:id/")
        def natureRetrieve(id : String, request: cask.Request) = {
            // auth method basicAuth : http, keyParamName: 
            // auth method cookieAuth : apiKey, keyParamName: sessionid

        def failFast = request.queryParams.keySet.contains("failFast")

        val result =         for {
            id <- Parsed(id)
            resultTry <- Parsed.eval(service.natureRetrieve(id))
            result <- Parsed.fromTry(resultTry)
        } yield result

        import NatureDetail.{given, *} // this brings in upickle in the case of union (oneOf) types

        (result : @unchecked) match {
          case Left(error) => cask.Response(error, 500)
          case Right(value : NatureDetail) => cask.Response(data = write(value), 200, headers = Seq("Content-Type" -> "application/json"))
          case Right(other) => cask.Response(s"$other", 200)
        }
      }
        /** List pokeathlon stats
         * 
         */
        // conflicts with [/api/v2/pokeathlon-stat/{id}/, /api/v2/pokeathlon-stat/] after/api/v2/pokeathlon-stat, ignoring @cask.get("/api/v2/pokeathlon-stat/")
        def pokeathlonStatList(request: cask.Request, limit : Option[Int], offset : Option[Int], q : Option[String]) = {
            // auth method basicAuth : http, keyParamName: 
            // auth method cookieAuth : apiKey, keyParamName: sessionid

        def failFast = request.queryParams.keySet.contains("failFast")

        val result =         for {
            resultTry <- Parsed.eval(service.pokeathlonStatList(limit, offset, q))
            result <- Parsed.fromTry(resultTry)
        } yield result

        import PaginatedPokeathlonStatSummaryList.{given, *} // this brings in upickle in the case of union (oneOf) types

        (result : @unchecked) match {
          case Left(error) => cask.Response(error, 500)
          case Right(value : PaginatedPokeathlonStatSummaryList) => cask.Response(data = write(value), 200, headers = Seq("Content-Type" -> "application/json"))
          case Right(other) => cask.Response(s"$other", 200)
        }
      }
        /** Get pokeathlon stat
         * 
         */
        // conflicts with [/api/v2/pokeathlon-stat/{id}/, /api/v2/pokeathlon-stat/] after/api/v2/pokeathlon-stat, ignoring @cask.get("/api/v2/pokeathlon-stat/:id/")
        def pokeathlonStatRetrieve(id : String, request: cask.Request) = {
            // auth method basicAuth : http, keyParamName: 
            // auth method cookieAuth : apiKey, keyParamName: sessionid

        def failFast = request.queryParams.keySet.contains("failFast")

        val result =         for {
            id <- Parsed(id)
            resultTry <- Parsed.eval(service.pokeathlonStatRetrieve(id))
            result <- Parsed.fromTry(resultTry)
        } yield result

        import PokeathlonStatDetail.{given, *} // this brings in upickle in the case of union (oneOf) types

        (result : @unchecked) match {
          case Left(error) => cask.Response(error, 500)
          case Right(value : PokeathlonStatDetail) => cask.Response(data = write(value), 200, headers = Seq("Content-Type" -> "application/json"))
          case Right(other) => cask.Response(s"$other", 200)
        }
      }
        /** List pokemon colors
         * 
         */
        // conflicts with [/api/v2/pokemon-color/{id}/, /api/v2/pokemon-color/] after/api/v2/pokemon-color, ignoring // conflicts with [/api/v2/pokemon/{id}/, /api/v2/pokemon-color/, /api/v2/pokemon-color/{id}/, /api/v2/pokemon-form/, /api/v2/pokemon-form/{id}/, /api/v2/pokemon-habitat/, /api/v2/pokemon-habitat/{id}/, /api/v2/pokemon/, /api/v2/pokemon-shape/, /api/v2/pokemon-shape/{id}/, /api/v2/pokemon-species/, /api/v2/pokemon-species/{id}/] after/api/v2/pokemon, ignoring @cask.get("/api/v2/pokemon-color/")
        def pokemonColorList(request: cask.Request, limit : Option[Int], offset : Option[Int], q : Option[String]) = {
            // auth method basicAuth : http, keyParamName: 
            // auth method cookieAuth : apiKey, keyParamName: sessionid

        def failFast = request.queryParams.keySet.contains("failFast")

        val result =         for {
            resultTry <- Parsed.eval(service.pokemonColorList(limit, offset, q))
            result <- Parsed.fromTry(resultTry)
        } yield result

        import PaginatedPokemonColorSummaryList.{given, *} // this brings in upickle in the case of union (oneOf) types

        (result : @unchecked) match {
          case Left(error) => cask.Response(error, 500)
          case Right(value : PaginatedPokemonColorSummaryList) => cask.Response(data = write(value), 200, headers = Seq("Content-Type" -> "application/json"))
          case Right(other) => cask.Response(s"$other", 200)
        }
      }
        /** Get pokemon color
         * 
         */
        // conflicts with [/api/v2/pokemon-color/{id}/, /api/v2/pokemon-color/] after/api/v2/pokemon-color, ignoring // conflicts with [/api/v2/pokemon/{id}/, /api/v2/pokemon-color/, /api/v2/pokemon-color/{id}/, /api/v2/pokemon-form/, /api/v2/pokemon-form/{id}/, /api/v2/pokemon-habitat/, /api/v2/pokemon-habitat/{id}/, /api/v2/pokemon/, /api/v2/pokemon-shape/, /api/v2/pokemon-shape/{id}/, /api/v2/pokemon-species/, /api/v2/pokemon-species/{id}/] after/api/v2/pokemon, ignoring @cask.get("/api/v2/pokemon-color/:id/")
        def pokemonColorRetrieve(id : String, request: cask.Request) = {
            // auth method basicAuth : http, keyParamName: 
            // auth method cookieAuth : apiKey, keyParamName: sessionid

        def failFast = request.queryParams.keySet.contains("failFast")

        val result =         for {
            id <- Parsed(id)
            resultTry <- Parsed.eval(service.pokemonColorRetrieve(id))
            result <- Parsed.fromTry(resultTry)
        } yield result

        import PokemonColorDetail.{given, *} // this brings in upickle in the case of union (oneOf) types

        (result : @unchecked) match {
          case Left(error) => cask.Response(error, 500)
          case Right(value : PokemonColorDetail) => cask.Response(data = write(value), 200, headers = Seq("Content-Type" -> "application/json"))
          case Right(other) => cask.Response(s"$other", 200)
        }
      }
        /** List pokemon forms
         * 
         */
        // conflicts with [/api/v2/pokemon-form/{id}/, /api/v2/pokemon-form/] after/api/v2/pokemon-form, ignoring // conflicts with [/api/v2/pokemon/{id}/, /api/v2/pokemon-color/, /api/v2/pokemon-color/{id}/, /api/v2/pokemon-form/, /api/v2/pokemon-form/{id}/, /api/v2/pokemon-habitat/, /api/v2/pokemon-habitat/{id}/, /api/v2/pokemon/, /api/v2/pokemon-shape/, /api/v2/pokemon-shape/{id}/, /api/v2/pokemon-species/, /api/v2/pokemon-species/{id}/] after/api/v2/pokemon, ignoring @cask.get("/api/v2/pokemon-form/")
        def pokemonFormList(request: cask.Request, limit : Option[Int], offset : Option[Int], q : Option[String]) = {
            // auth method basicAuth : http, keyParamName: 
            // auth method cookieAuth : apiKey, keyParamName: sessionid

        def failFast = request.queryParams.keySet.contains("failFast")

        val result =         for {
            resultTry <- Parsed.eval(service.pokemonFormList(limit, offset, q))
            result <- Parsed.fromTry(resultTry)
        } yield result

        import PaginatedPokemonFormSummaryList.{given, *} // this brings in upickle in the case of union (oneOf) types

        (result : @unchecked) match {
          case Left(error) => cask.Response(error, 500)
          case Right(value : PaginatedPokemonFormSummaryList) => cask.Response(data = write(value), 200, headers = Seq("Content-Type" -> "application/json"))
          case Right(other) => cask.Response(s"$other", 200)
        }
      }
        /** Get pokemon form
         * 
         */
        // conflicts with [/api/v2/pokemon-form/{id}/, /api/v2/pokemon-form/] after/api/v2/pokemon-form, ignoring // conflicts with [/api/v2/pokemon/{id}/, /api/v2/pokemon-color/, /api/v2/pokemon-color/{id}/, /api/v2/pokemon-form/, /api/v2/pokemon-form/{id}/, /api/v2/pokemon-habitat/, /api/v2/pokemon-habitat/{id}/, /api/v2/pokemon/, /api/v2/pokemon-shape/, /api/v2/pokemon-shape/{id}/, /api/v2/pokemon-species/, /api/v2/pokemon-species/{id}/] after/api/v2/pokemon, ignoring @cask.get("/api/v2/pokemon-form/:id/")
        def pokemonFormRetrieve(id : String, request: cask.Request) = {
            // auth method basicAuth : http, keyParamName: 
            // auth method cookieAuth : apiKey, keyParamName: sessionid

        def failFast = request.queryParams.keySet.contains("failFast")

        val result =         for {
            id <- Parsed(id)
            resultTry <- Parsed.eval(service.pokemonFormRetrieve(id))
            result <- Parsed.fromTry(resultTry)
        } yield result

        import PokemonFormDetail.{given, *} // this brings in upickle in the case of union (oneOf) types

        (result : @unchecked) match {
          case Left(error) => cask.Response(error, 500)
          case Right(value : PokemonFormDetail) => cask.Response(data = write(value), 200, headers = Seq("Content-Type" -> "application/json"))
          case Right(other) => cask.Response(s"$other", 200)
        }
      }
        /** List pokemom habitas
         * 
         */
        // conflicts with [/api/v2/pokemon-habitat/{id}/, /api/v2/pokemon-habitat/] after/api/v2/pokemon-habitat, ignoring // conflicts with [/api/v2/pokemon/{id}/, /api/v2/pokemon-color/, /api/v2/pokemon-color/{id}/, /api/v2/pokemon-form/, /api/v2/pokemon-form/{id}/, /api/v2/pokemon-habitat/, /api/v2/pokemon-habitat/{id}/, /api/v2/pokemon/, /api/v2/pokemon-shape/, /api/v2/pokemon-shape/{id}/, /api/v2/pokemon-species/, /api/v2/pokemon-species/{id}/] after/api/v2/pokemon, ignoring @cask.get("/api/v2/pokemon-habitat/")
        def pokemonHabitatList(request: cask.Request, limit : Option[Int], offset : Option[Int], q : Option[String]) = {
            // auth method basicAuth : http, keyParamName: 
            // auth method cookieAuth : apiKey, keyParamName: sessionid

        def failFast = request.queryParams.keySet.contains("failFast")

        val result =         for {
            resultTry <- Parsed.eval(service.pokemonHabitatList(limit, offset, q))
            result <- Parsed.fromTry(resultTry)
        } yield result

        import PaginatedPokemonHabitatSummaryList.{given, *} // this brings in upickle in the case of union (oneOf) types

        (result : @unchecked) match {
          case Left(error) => cask.Response(error, 500)
          case Right(value : PaginatedPokemonHabitatSummaryList) => cask.Response(data = write(value), 200, headers = Seq("Content-Type" -> "application/json"))
          case Right(other) => cask.Response(s"$other", 200)
        }
      }
        /** Get pokemom habita
         * 
         */
        // conflicts with [/api/v2/pokemon-habitat/{id}/, /api/v2/pokemon-habitat/] after/api/v2/pokemon-habitat, ignoring // conflicts with [/api/v2/pokemon/{id}/, /api/v2/pokemon-color/, /api/v2/pokemon-color/{id}/, /api/v2/pokemon-form/, /api/v2/pokemon-form/{id}/, /api/v2/pokemon-habitat/, /api/v2/pokemon-habitat/{id}/, /api/v2/pokemon/, /api/v2/pokemon-shape/, /api/v2/pokemon-shape/{id}/, /api/v2/pokemon-species/, /api/v2/pokemon-species/{id}/] after/api/v2/pokemon, ignoring @cask.get("/api/v2/pokemon-habitat/:id/")
        def pokemonHabitatRetrieve(id : String, request: cask.Request) = {
            // auth method basicAuth : http, keyParamName: 
            // auth method cookieAuth : apiKey, keyParamName: sessionid

        def failFast = request.queryParams.keySet.contains("failFast")

        val result =         for {
            id <- Parsed(id)
            resultTry <- Parsed.eval(service.pokemonHabitatRetrieve(id))
            result <- Parsed.fromTry(resultTry)
        } yield result

        import PokemonHabitatDetail.{given, *} // this brings in upickle in the case of union (oneOf) types

        (result : @unchecked) match {
          case Left(error) => cask.Response(error, 500)
          case Right(value : PokemonHabitatDetail) => cask.Response(data = write(value), 200, headers = Seq("Content-Type" -> "application/json"))
          case Right(other) => cask.Response(s"$other", 200)
        }
      }
        /** List pokemon
         * 
         */
        // conflicts with [/api/v2/pokemon/{id}/, /api/v2/pokemon-color/, /api/v2/pokemon-color/{id}/, /api/v2/pokemon-form/, /api/v2/pokemon-form/{id}/, /api/v2/pokemon-habitat/, /api/v2/pokemon-habitat/{id}/, /api/v2/pokemon/, /api/v2/pokemon-shape/, /api/v2/pokemon-shape/{id}/, /api/v2/pokemon-species/, /api/v2/pokemon-species/{id}/] after/api/v2/pokemon, ignoring @cask.get("/api/v2/pokemon/")
        def pokemonList(request: cask.Request, limit : Option[Int], offset : Option[Int], q : Option[String]) = {
            // auth method basicAuth : http, keyParamName: 
            // auth method cookieAuth : apiKey, keyParamName: sessionid

        def failFast = request.queryParams.keySet.contains("failFast")

        val result =         for {
            resultTry <- Parsed.eval(service.pokemonList(limit, offset, q))
            result <- Parsed.fromTry(resultTry)
        } yield result

        import PaginatedPokemonSummaryList.{given, *} // this brings in upickle in the case of union (oneOf) types

        (result : @unchecked) match {
          case Left(error) => cask.Response(error, 500)
          case Right(value : PaginatedPokemonSummaryList) => cask.Response(data = write(value), 200, headers = Seq("Content-Type" -> "application/json"))
          case Right(other) => cask.Response(s"$other", 200)
        }
      }
        /** Get pokemon
         * 
         */
        // conflicts with [/api/v2/pokemon/{id}/, /api/v2/pokemon-color/, /api/v2/pokemon-color/{id}/, /api/v2/pokemon-form/, /api/v2/pokemon-form/{id}/, /api/v2/pokemon-habitat/, /api/v2/pokemon-habitat/{id}/, /api/v2/pokemon/, /api/v2/pokemon-shape/, /api/v2/pokemon-shape/{id}/, /api/v2/pokemon-species/, /api/v2/pokemon-species/{id}/] after/api/v2/pokemon, ignoring @cask.get("/api/v2/pokemon/:id/")
        def pokemonRetrieve(id : String, request: cask.Request) = {
            // auth method basicAuth : http, keyParamName: 
            // auth method cookieAuth : apiKey, keyParamName: sessionid

        def failFast = request.queryParams.keySet.contains("failFast")

        val result =         for {
            id <- Parsed(id)
            resultTry <- Parsed.eval(service.pokemonRetrieve(id))
            result <- Parsed.fromTry(resultTry)
        } yield result

        import PokemonDetail.{given, *} // this brings in upickle in the case of union (oneOf) types

        (result : @unchecked) match {
          case Left(error) => cask.Response(error, 500)
          case Right(value : PokemonDetail) => cask.Response(data = write(value), 200, headers = Seq("Content-Type" -> "application/json"))
          case Right(other) => cask.Response(s"$other", 200)
        }
      }
        /** List pokemon shapes
         * 
         */
        // conflicts with [/api/v2/pokemon-shape/{id}/, /api/v2/pokemon-shape/] after/api/v2/pokemon-shape, ignoring // conflicts with [/api/v2/pokemon/{id}/, /api/v2/pokemon-color/, /api/v2/pokemon-color/{id}/, /api/v2/pokemon-form/, /api/v2/pokemon-form/{id}/, /api/v2/pokemon-habitat/, /api/v2/pokemon-habitat/{id}/, /api/v2/pokemon/, /api/v2/pokemon-shape/, /api/v2/pokemon-shape/{id}/, /api/v2/pokemon-species/, /api/v2/pokemon-species/{id}/] after/api/v2/pokemon, ignoring @cask.get("/api/v2/pokemon-shape/")
        def pokemonShapeList(request: cask.Request, limit : Option[Int], offset : Option[Int], q : Option[String]) = {
            // auth method basicAuth : http, keyParamName: 
            // auth method cookieAuth : apiKey, keyParamName: sessionid

        def failFast = request.queryParams.keySet.contains("failFast")

        val result =         for {
            resultTry <- Parsed.eval(service.pokemonShapeList(limit, offset, q))
            result <- Parsed.fromTry(resultTry)
        } yield result

        import PaginatedPokemonShapeSummaryList.{given, *} // this brings in upickle in the case of union (oneOf) types

        (result : @unchecked) match {
          case Left(error) => cask.Response(error, 500)
          case Right(value : PaginatedPokemonShapeSummaryList) => cask.Response(data = write(value), 200, headers = Seq("Content-Type" -> "application/json"))
          case Right(other) => cask.Response(s"$other", 200)
        }
      }
        /** Get pokemon shape
         * 
         */
        // conflicts with [/api/v2/pokemon-shape/{id}/, /api/v2/pokemon-shape/] after/api/v2/pokemon-shape, ignoring // conflicts with [/api/v2/pokemon/{id}/, /api/v2/pokemon-color/, /api/v2/pokemon-color/{id}/, /api/v2/pokemon-form/, /api/v2/pokemon-form/{id}/, /api/v2/pokemon-habitat/, /api/v2/pokemon-habitat/{id}/, /api/v2/pokemon/, /api/v2/pokemon-shape/, /api/v2/pokemon-shape/{id}/, /api/v2/pokemon-species/, /api/v2/pokemon-species/{id}/] after/api/v2/pokemon, ignoring @cask.get("/api/v2/pokemon-shape/:id/")
        def pokemonShapeRetrieve(id : String, request: cask.Request) = {
            // auth method basicAuth : http, keyParamName: 
            // auth method cookieAuth : apiKey, keyParamName: sessionid

        def failFast = request.queryParams.keySet.contains("failFast")

        val result =         for {
            id <- Parsed(id)
            resultTry <- Parsed.eval(service.pokemonShapeRetrieve(id))
            result <- Parsed.fromTry(resultTry)
        } yield result

        import PokemonShapeDetail.{given, *} // this brings in upickle in the case of union (oneOf) types

        (result : @unchecked) match {
          case Left(error) => cask.Response(error, 500)
          case Right(value : PokemonShapeDetail) => cask.Response(data = write(value), 200, headers = Seq("Content-Type" -> "application/json"))
          case Right(other) => cask.Response(s"$other", 200)
        }
      }
        /** List pokemon species
         * 
         */
        // conflicts with [/api/v2/pokemon/{id}/, /api/v2/pokemon-color/, /api/v2/pokemon-color/{id}/, /api/v2/pokemon-form/, /api/v2/pokemon-form/{id}/, /api/v2/pokemon-habitat/, /api/v2/pokemon-habitat/{id}/, /api/v2/pokemon/, /api/v2/pokemon-shape/, /api/v2/pokemon-shape/{id}/, /api/v2/pokemon-species/, /api/v2/pokemon-species/{id}/] after/api/v2/pokemon, ignoring // conflicts with [/api/v2/pokemon-species/{id}/, /api/v2/pokemon-species/] after/api/v2/pokemon-species, ignoring @cask.get("/api/v2/pokemon-species/")
        def pokemonSpeciesList(request: cask.Request, limit : Option[Int], offset : Option[Int], q : Option[String]) = {
            // auth method basicAuth : http, keyParamName: 
            // auth method cookieAuth : apiKey, keyParamName: sessionid

        def failFast = request.queryParams.keySet.contains("failFast")

        val result =         for {
            resultTry <- Parsed.eval(service.pokemonSpeciesList(limit, offset, q))
            result <- Parsed.fromTry(resultTry)
        } yield result

        import PaginatedPokemonSpeciesSummaryList.{given, *} // this brings in upickle in the case of union (oneOf) types

        (result : @unchecked) match {
          case Left(error) => cask.Response(error, 500)
          case Right(value : PaginatedPokemonSpeciesSummaryList) => cask.Response(data = write(value), 200, headers = Seq("Content-Type" -> "application/json"))
          case Right(other) => cask.Response(s"$other", 200)
        }
      }
        /** Get pokemon species
         * 
         */
        // conflicts with [/api/v2/pokemon/{id}/, /api/v2/pokemon-color/, /api/v2/pokemon-color/{id}/, /api/v2/pokemon-form/, /api/v2/pokemon-form/{id}/, /api/v2/pokemon-habitat/, /api/v2/pokemon-habitat/{id}/, /api/v2/pokemon/, /api/v2/pokemon-shape/, /api/v2/pokemon-shape/{id}/, /api/v2/pokemon-species/, /api/v2/pokemon-species/{id}/] after/api/v2/pokemon, ignoring // conflicts with [/api/v2/pokemon-species/{id}/, /api/v2/pokemon-species/] after/api/v2/pokemon-species, ignoring @cask.get("/api/v2/pokemon-species/:id/")
        def pokemonSpeciesRetrieve(id : String, request: cask.Request) = {
            // auth method basicAuth : http, keyParamName: 
            // auth method cookieAuth : apiKey, keyParamName: sessionid

        def failFast = request.queryParams.keySet.contains("failFast")

        val result =         for {
            id <- Parsed(id)
            resultTry <- Parsed.eval(service.pokemonSpeciesRetrieve(id))
            result <- Parsed.fromTry(resultTry)
        } yield result

        import PokemonSpeciesDetail.{given, *} // this brings in upickle in the case of union (oneOf) types

        (result : @unchecked) match {
          case Left(error) => cask.Response(error, 500)
          case Right(value : PokemonSpeciesDetail) => cask.Response(data = write(value), 200, headers = Seq("Content-Type" -> "application/json"))
          case Right(other) => cask.Response(s"$other", 200)
        }
      }
        /** List stats
         * 
         */
        // conflicts with [/api/v2/stat/{id}/, /api/v2/stat/] after/api/v2/stat, ignoring @cask.get("/api/v2/stat/")
        def statList(request: cask.Request, limit : Option[Int], offset : Option[Int], q : Option[String]) = {
            // auth method basicAuth : http, keyParamName: 
            // auth method cookieAuth : apiKey, keyParamName: sessionid

        def failFast = request.queryParams.keySet.contains("failFast")

        val result =         for {
            resultTry <- Parsed.eval(service.statList(limit, offset, q))
            result <- Parsed.fromTry(resultTry)
        } yield result

        import PaginatedStatSummaryList.{given, *} // this brings in upickle in the case of union (oneOf) types

        (result : @unchecked) match {
          case Left(error) => cask.Response(error, 500)
          case Right(value : PaginatedStatSummaryList) => cask.Response(data = write(value), 200, headers = Seq("Content-Type" -> "application/json"))
          case Right(other) => cask.Response(s"$other", 200)
        }
      }
        /** Get stat
         * 
         */
        // conflicts with [/api/v2/stat/{id}/, /api/v2/stat/] after/api/v2/stat, ignoring @cask.get("/api/v2/stat/:id/")
        def statRetrieve(id : String, request: cask.Request) = {
            // auth method basicAuth : http, keyParamName: 
            // auth method cookieAuth : apiKey, keyParamName: sessionid

        def failFast = request.queryParams.keySet.contains("failFast")

        val result =         for {
            id <- Parsed(id)
            resultTry <- Parsed.eval(service.statRetrieve(id))
            result <- Parsed.fromTry(resultTry)
        } yield result

        import StatDetail.{given, *} // this brings in upickle in the case of union (oneOf) types

        (result : @unchecked) match {
          case Left(error) => cask.Response(error, 500)
          case Right(value : StatDetail) => cask.Response(data = write(value), 200, headers = Seq("Content-Type" -> "application/json"))
          case Right(other) => cask.Response(s"$other", 200)
        }
      }
        /** List types
         * 
         */
        // conflicts with [/api/v2/type/{id}/, /api/v2/type/] after/api/v2/type, ignoring @cask.get("/api/v2/type/")
        def typeList(request: cask.Request, limit : Option[Int], offset : Option[Int], q : Option[String]) = {
            // auth method basicAuth : http, keyParamName: 
            // auth method cookieAuth : apiKey, keyParamName: sessionid

        def failFast = request.queryParams.keySet.contains("failFast")

        val result =         for {
            resultTry <- Parsed.eval(service.typeList(limit, offset, q))
            result <- Parsed.fromTry(resultTry)
        } yield result

        import PaginatedTypeSummaryList.{given, *} // this brings in upickle in the case of union (oneOf) types

        (result : @unchecked) match {
          case Left(error) => cask.Response(error, 500)
          case Right(value : PaginatedTypeSummaryList) => cask.Response(data = write(value), 200, headers = Seq("Content-Type" -> "application/json"))
          case Right(other) => cask.Response(s"$other", 200)
        }
      }
        /** Get types
         * 
         */
        // conflicts with [/api/v2/type/{id}/, /api/v2/type/] after/api/v2/type, ignoring @cask.get("/api/v2/type/:id/")
        def typeRetrieve(id : String, request: cask.Request) = {
            // auth method basicAuth : http, keyParamName: 
            // auth method cookieAuth : apiKey, keyParamName: sessionid

        def failFast = request.queryParams.keySet.contains("failFast")

        val result =         for {
            id <- Parsed(id)
            resultTry <- Parsed.eval(service.typeRetrieve(id))
            result <- Parsed.fromTry(resultTry)
        } yield result

        import TypeDetail.{given, *} // this brings in upickle in the case of union (oneOf) types

        (result : @unchecked) match {
          case Left(error) => cask.Response(error, 500)
          case Right(value : TypeDetail) => cask.Response(data = write(value), 200, headers = Seq("Content-Type" -> "application/json"))
          case Right(other) => cask.Response(s"$other", 200)
        }
      }

    initialize()
}
