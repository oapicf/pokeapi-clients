//> using scala "3.3.1"
//> using lib "com.lihaoyi::cask:0.8.3"
//> using lib "com.lihaoyi::scalatags:0.12.0"
/**
 * PokéAPI
 * All the Pokémon data you'll ever need in one place, easily accessible through a modern free open-source RESTful API.  ## What is this?  This is a full RESTful API linked to an extensive database detailing everything about the Pokémon main game series.  We've covered everything from Pokémon to Berry Flavors.  ## Where do I start?  We have awesome [documentation](https://pokeapi.co/docs/v2) on how to use this API. It takes minutes to get started.  This API will always be publicly available and will never require any extensive setup process to consume.  Created by [**Paul Hallett**(]https://github.com/phalt) and other [**PokéAPI contributors***](https://github.com/PokeAPI/pokeapi#contributing) around the world. Pokémon and Pokémon character names are trademarks of Nintendo.     
 *
 * OpenAPI spec version: 2.7.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */


// generated from apiService.mustache
package org.openapitools.server.api

import _root_.org.openapitools.server.model.MachineDetail
import _root_.org.openapitools.server.model.PaginatedMachineSummaryList
import scala.util.Failure
import scala.util.Try
import _root_.org.openapitools.server.model.*

/**
 * The MachinesService companion object.
 *
 * Use the MachinesService() companion object to create an instance which returns a 'not implemented' error
 * for each operation.
 *
 */
object MachinesService {

  /**
   * The 'Handler' is an implementation of MachinesService convenient for delegating or overriding individual functions
   */
  case class Handler[F[_]](
        machineListHandler : (limit : Option[Int], offset : Option[Int], q : Option[String]) => F[PaginatedMachineSummaryList], 
        machineRetrieveHandler : (id : String) => F[MachineDetail]
  ) extends MachinesService[F] {

        override def machineList(limit : Option[Int], offset : Option[Int], q : Option[String]) : F[PaginatedMachineSummaryList] = {
          machineListHandler(limit, offset, q)
        }

        override def machineRetrieve(id : String) : F[MachineDetail] = {
          machineRetrieveHandler(id)
        }
  }

  def apply() : MachinesService[Try] = MachinesService.Handler[Try](
        (_, _, _) => notImplemented("machineList"), 
        (_) => notImplemented("machineRetrieve")
  )

  private def notImplemented(name : String) = Failure(new Exception(s"TODO: $name not implemented"))
}

/**
 * The Machines business-logic
 *
 *
 * The 'asHandler' will return an implementation which allows for easily overriding individual operations.
 *
 * equally there are "on&lt;Function&gt;" helper methods for easily overriding individual functions
 *
 * @tparam F the effect type (Future, Try, IO, ID, etc) of the operations
 */
trait MachinesService[F[_]] {
  /** List machines
   * 
   * @return PaginatedMachineSummaryList
   */
  def machineList(limit : Option[Int], offset : Option[Int], q : Option[String]) : F[PaginatedMachineSummaryList]

  /**
   * override machineList with the given handler
   * @return a new implementation of MachinesService[F] with machineList overridden using the given handler
   */
  final def onMachineList(handler : (limit : Option[Int], offset : Option[Int], q : Option[String]) => F[PaginatedMachineSummaryList]) : MachinesService[F] = {
    asHandler.copy(machineListHandler = handler)
  }
  /** Get machine
   * 
   * @return MachineDetail
   */
  def machineRetrieve(id : String) : F[MachineDetail]

  /**
   * override machineRetrieve with the given handler
   * @return a new implementation of MachinesService[F] with machineRetrieve overridden using the given handler
   */
  final def onMachineRetrieve(handler : (id : String) => F[MachineDetail]) : MachinesService[F] = {
    asHandler.copy(machineRetrieveHandler = handler)
  }

  /**
   * @return a Handler implementation of this service
   */
  final def asHandler : MachinesService.Handler[F] = this match {
      case h : MachinesService.Handler[F] => h
      case _ =>
        MachinesService.Handler[F](
              (limit, offset, q) => machineList(limit, offset, q), 
              (id) => machineRetrieve(id)
        )
  }

  /**
   * This function will change the effect type of this service.
   *
   * It's not unlike a typical map operation from A => B, except we're not mapping
   * a type from A to B, but rather from F[A] => G[A] using the 'changeEffect' function.
   *
   * For, this could turn an asynchronous service (one which returns Future[_] types) into
   * a synchronous one (one which returns Try[_] types) by awaiting on the Future.
   *
   * It could change an IO type (like cats effect or ZIO) into an ID[A] which is just:
   * ```
   * type ID[A] => A
   * ```
   *
   * @tparam G the new "polymorphic" effect type
   * @param changeEffect the "natural transformation" which can change one effect type into another
   * @return a new MachinesService service implementation with effect type [G]
   */
  final def mapEffect[G[_]](changeEffect : [A] => F[A] => G[A]) : MachinesService[G] = {
    val self = this

    new MachinesService[G] {
        override def machineList(limit : Option[Int], offset : Option[Int], q : Option[String]) : G[PaginatedMachineSummaryList] = changeEffect {
          self.machineList(limit, offset, q)
        }
        override def machineRetrieve(id : String) : G[MachineDetail] = changeEffect {
          self.machineRetrieve(id)
        }
    }
  }
}
