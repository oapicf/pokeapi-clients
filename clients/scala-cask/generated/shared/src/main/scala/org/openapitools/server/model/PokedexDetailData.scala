/**
 * PokéAPI
 * All the Pokémon data you'll ever need in one place, easily accessible through a modern free open-source RESTful API.  ## What is this?  This is a full RESTful API linked to an extensive database detailing everything about the Pokémon main game series.  We've covered everything from Pokémon to Berry Flavors.  ## Where do I start?  We have awesome [documentation](https://pokeapi.co/docs/v2) on how to use this API. It takes minutes to get started.  This API will always be publicly available and will never require any extensive setup process to consume.  Created by [**Paul Hallett**(]https://github.com/phalt) and other [**PokéAPI contributors***](https://github.com/PokeAPI/pokeapi#contributing) around the world. Pokémon and Pokémon character names are trademarks of Nintendo.     
 *
 * OpenAPI spec version: 2.7.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** PokedexDetailData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class PokedexDetailData(
  id: Int,
  name: String,
  isMainSeries: Boolean = false ,
  descriptions: Seq[PokedexDescriptionData],
  names: Seq[PokedexNameData],
  pokemonEntries: Seq[PokedexDetailPokemonEntriesInnerData],
  region: RegionSummaryData,
  versionGroups: Seq[AbilityDetailPokemonInnerPokemonData]
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== id validation ==================
        
        
        
        

        // ================== name validation ==================
        // validate max length 100
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if name == null then 0 else name.length
            if (len < 100) {
               _allValidationErrors += ValidationError(path :+ PokedexDetail.Fields.name, s"length $len is longer than the max length 100")
            }
        }
        
        
        
        

        // ================== isMainSeries validation ==================
        
        
        
        

        // ================== descriptions validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (descriptions == null) {
            _allValidationErrors += ValidationError(path :+ PokedexDetail.Fields.descriptions, "descriptions is a required field and cannot be null")
            }
        }
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (descriptions != null) {
                descriptions.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ PokedexDetail.Fields.descriptions :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== names validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (names == null) {
            _allValidationErrors += ValidationError(path :+ PokedexDetail.Fields.names, "names is a required field and cannot be null")
            }
        }
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (names != null) {
                names.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ PokedexDetail.Fields.names :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== pokemonEntries validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (pokemonEntries == null) {
            _allValidationErrors += ValidationError(path :+ PokedexDetail.Fields.pokemonEntries, "pokemonEntries is a required field and cannot be null")
            }
        }
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (pokemonEntries != null) {
                pokemonEntries.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ PokedexDetail.Fields.pokemonEntries :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== region validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (region == null) {
            _allValidationErrors += ValidationError(path :+ PokedexDetail.Fields.region, "region is a required field and cannot be null")
            }
        }
        
        
        
        
        // validating region
        if (_allValidationErrors.isEmpty || !failFast) {
            if region != null then _allValidationErrors ++= region.validationErrors(path :+ PokedexDetail.Fields.region, failFast)
        }

        // ================== versionGroups validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (versionGroups == null) {
            _allValidationErrors += ValidationError(path :+ PokedexDetail.Fields.versionGroups, "versionGroups is a required field and cannot be null")
            }
        }
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (versionGroups != null) {
                versionGroups.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ PokedexDetail.Fields.versionGroups :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[PokedexDetail] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : PokedexDetail = {
    PokedexDetail(
        id = id /* 2 */,
        name = name /* 2 */,
        isMainSeries = Option(isMainSeries) /* 1 */,
        descriptions = descriptions.map(_.asModel) /* 5 */,
        names = names.map(_.asModel) /* 5 */,
        pokemonEntries = pokemonEntries.map(_.asModel) /* 5 */,
        region = region.asModel /* 6 */,
        versionGroups = versionGroups.map(_.asModel) /* 5 */
    
    )
  }
}

object PokedexDetailData {

  def validated(d8a : PokedexDetailData, failFast : Boolean) : scala.util.Try[PokedexDetail] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : PokedexDetailData = try {
        val data = read[PokedexDetailData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating PokedexDetailData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : PokedexDetailData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[PokedexDetailData] = try {
        read[List[PokedexDetailData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[PokedexDetail]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[PokedexDetail]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, PokedexDetailData] = try {
        read[Map[String, PokedexDetailData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, PokedexDetail]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, PokedexDetail]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

