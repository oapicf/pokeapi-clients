/**
 * PokéAPI
 * All the Pokémon data you'll ever need in one place, easily accessible through a modern free open-source RESTful API.  ## What is this?  This is a full RESTful API linked to an extensive database detailing everything about the Pokémon main game series.  We've covered everything from Pokémon to Berry Flavors.  ## Where do I start?  We have awesome [documentation](https://pokeapi.co/docs/v2) on how to use this API. It takes minutes to get started.  This API will always be publicly available and will never require any extensive setup process to consume.  Created by [**Paul Hallett**(]https://github.com/phalt) and other [**PokéAPI contributors***](https://github.com/PokeAPI/pokeapi#contributing) around the world. Pokémon and Pokémon character names are trademarks of Nintendo.     
 *
 * OpenAPI spec version: 2.7.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** PokemonSpeciesDetailData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class PokemonSpeciesDetailData(
  id: Int,
  name: String,
  order: Int = 0 ,
  genderRate: Int = 0 ,
  captureRate: Int = 0 ,
  baseHappiness: Int = 0 ,
  isBaby: Boolean = false ,
  isLegendary: Boolean = false ,
  isMythical: Boolean = false ,
  hatchCounter: Int = 0 ,
  hasGenderDifferences: Boolean = false ,
  formsSwitchable: Boolean = false ,
  growthRate: GrowthRateSummaryData,
  pokedexNumbers: Seq[PokemonDexEntryData],
  eggGroups: Seq[AbilityDetailPokemonInnerPokemonData],
  color: PokemonColorSummaryData,
  shape: PokemonShapeSummaryData,
  evolvesFromSpecies: PokemonSpeciesSummaryData,
  evolutionChain: EvolutionChainSummaryData,
  habitat: PokemonHabitatSummaryData,
  generation: GenerationSummaryData,
  names: Seq[PokemonFormDetailFormNamesInnerData],
  palParkEncounters: Seq[PokemonSpeciesDetailPalParkEncountersInnerData],
  formDescriptions: Seq[PokemonSpeciesDescriptionData],
  flavorTextEntries: Seq[PokemonSpeciesFlavorTextData],
  genera: Seq[PokemonSpeciesDetailGeneraInnerData],
  varieties: Seq[PokemonSpeciesDetailVarietiesInnerData]
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== id validation ==================
        
        
        
        

        // ================== name validation ==================
        // validate max length 100
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if name == null then 0 else name.length
            if (len < 100) {
               _allValidationErrors += ValidationError(path :+ PokemonSpeciesDetail.Fields.name, s"length $len is longer than the max length 100")
            }
        }
        
        
        
        

        // ================== order validation ==================
        
        
        
        

        // ================== genderRate validation ==================
        
        
        
        

        // ================== captureRate validation ==================
        
        
        
        

        // ================== baseHappiness validation ==================
        
        
        
        

        // ================== isBaby validation ==================
        
        
        
        

        // ================== isLegendary validation ==================
        
        
        
        

        // ================== isMythical validation ==================
        
        
        
        

        // ================== hatchCounter validation ==================
        
        
        
        

        // ================== hasGenderDifferences validation ==================
        
        
        
        

        // ================== formsSwitchable validation ==================
        
        
        
        

        // ================== growthRate validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (growthRate == null) {
            _allValidationErrors += ValidationError(path :+ PokemonSpeciesDetail.Fields.growthRate, "growthRate is a required field and cannot be null")
            }
        }
        
        
        
        
        // validating growthRate
        if (_allValidationErrors.isEmpty || !failFast) {
            if growthRate != null then _allValidationErrors ++= growthRate.validationErrors(path :+ PokemonSpeciesDetail.Fields.growthRate, failFast)
        }

        // ================== pokedexNumbers validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (pokedexNumbers == null) {
            _allValidationErrors += ValidationError(path :+ PokemonSpeciesDetail.Fields.pokedexNumbers, "pokedexNumbers is a required field and cannot be null")
            }
        }
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (pokedexNumbers != null) {
                pokedexNumbers.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ PokemonSpeciesDetail.Fields.pokedexNumbers :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== eggGroups validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (eggGroups == null) {
            _allValidationErrors += ValidationError(path :+ PokemonSpeciesDetail.Fields.eggGroups, "eggGroups is a required field and cannot be null")
            }
        }
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (eggGroups != null) {
                eggGroups.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ PokemonSpeciesDetail.Fields.eggGroups :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== color validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (color == null) {
            _allValidationErrors += ValidationError(path :+ PokemonSpeciesDetail.Fields.color, "color is a required field and cannot be null")
            }
        }
        
        
        
        
        // validating color
        if (_allValidationErrors.isEmpty || !failFast) {
            if color != null then _allValidationErrors ++= color.validationErrors(path :+ PokemonSpeciesDetail.Fields.color, failFast)
        }

        // ================== shape validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (shape == null) {
            _allValidationErrors += ValidationError(path :+ PokemonSpeciesDetail.Fields.shape, "shape is a required field and cannot be null")
            }
        }
        
        
        
        
        // validating shape
        if (_allValidationErrors.isEmpty || !failFast) {
            if shape != null then _allValidationErrors ++= shape.validationErrors(path :+ PokemonSpeciesDetail.Fields.shape, failFast)
        }

        // ================== evolvesFromSpecies validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (evolvesFromSpecies == null) {
            _allValidationErrors += ValidationError(path :+ PokemonSpeciesDetail.Fields.evolvesFromSpecies, "evolvesFromSpecies is a required field and cannot be null")
            }
        }
        
        
        
        
        // validating evolvesFromSpecies
        if (_allValidationErrors.isEmpty || !failFast) {
            if evolvesFromSpecies != null then _allValidationErrors ++= evolvesFromSpecies.validationErrors(path :+ PokemonSpeciesDetail.Fields.evolvesFromSpecies, failFast)
        }

        // ================== evolutionChain validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (evolutionChain == null) {
            _allValidationErrors += ValidationError(path :+ PokemonSpeciesDetail.Fields.evolutionChain, "evolutionChain is a required field and cannot be null")
            }
        }
        
        
        
        
        // validating evolutionChain
        if (_allValidationErrors.isEmpty || !failFast) {
            if evolutionChain != null then _allValidationErrors ++= evolutionChain.validationErrors(path :+ PokemonSpeciesDetail.Fields.evolutionChain, failFast)
        }

        // ================== habitat validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (habitat == null) {
            _allValidationErrors += ValidationError(path :+ PokemonSpeciesDetail.Fields.habitat, "habitat is a required field and cannot be null")
            }
        }
        
        
        
        
        // validating habitat
        if (_allValidationErrors.isEmpty || !failFast) {
            if habitat != null then _allValidationErrors ++= habitat.validationErrors(path :+ PokemonSpeciesDetail.Fields.habitat, failFast)
        }

        // ================== generation validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (generation == null) {
            _allValidationErrors += ValidationError(path :+ PokemonSpeciesDetail.Fields.generation, "generation is a required field and cannot be null")
            }
        }
        
        
        
        
        // validating generation
        if (_allValidationErrors.isEmpty || !failFast) {
            if generation != null then _allValidationErrors ++= generation.validationErrors(path :+ PokemonSpeciesDetail.Fields.generation, failFast)
        }

        // ================== names validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (names == null) {
            _allValidationErrors += ValidationError(path :+ PokemonSpeciesDetail.Fields.names, "names is a required field and cannot be null")
            }
        }
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (names != null) {
                names.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ PokemonSpeciesDetail.Fields.names :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== palParkEncounters validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (palParkEncounters == null) {
            _allValidationErrors += ValidationError(path :+ PokemonSpeciesDetail.Fields.palParkEncounters, "palParkEncounters is a required field and cannot be null")
            }
        }
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (palParkEncounters != null) {
                palParkEncounters.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ PokemonSpeciesDetail.Fields.palParkEncounters :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== formDescriptions validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (formDescriptions == null) {
            _allValidationErrors += ValidationError(path :+ PokemonSpeciesDetail.Fields.formDescriptions, "formDescriptions is a required field and cannot be null")
            }
        }
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (formDescriptions != null) {
                formDescriptions.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ PokemonSpeciesDetail.Fields.formDescriptions :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== flavorTextEntries validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (flavorTextEntries == null) {
            _allValidationErrors += ValidationError(path :+ PokemonSpeciesDetail.Fields.flavorTextEntries, "flavorTextEntries is a required field and cannot be null")
            }
        }
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (flavorTextEntries != null) {
                flavorTextEntries.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ PokemonSpeciesDetail.Fields.flavorTextEntries :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== genera validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (genera == null) {
            _allValidationErrors += ValidationError(path :+ PokemonSpeciesDetail.Fields.genera, "genera is a required field and cannot be null")
            }
        }
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (genera != null) {
                genera.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ PokemonSpeciesDetail.Fields.genera :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== varieties validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (varieties == null) {
            _allValidationErrors += ValidationError(path :+ PokemonSpeciesDetail.Fields.varieties, "varieties is a required field and cannot be null")
            }
        }
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (varieties != null) {
                varieties.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ PokemonSpeciesDetail.Fields.varieties :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[PokemonSpeciesDetail] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : PokemonSpeciesDetail = {
    PokemonSpeciesDetail(
        id = id /* 2 */,
        name = name /* 2 */,
        order = Option(order) /* 1 */,
        genderRate = Option(genderRate) /* 1 */,
        captureRate = Option(captureRate) /* 1 */,
        baseHappiness = Option(baseHappiness) /* 1 */,
        isBaby = Option(isBaby) /* 1 */,
        isLegendary = Option(isLegendary) /* 1 */,
        isMythical = Option(isMythical) /* 1 */,
        hatchCounter = Option(hatchCounter) /* 1 */,
        hasGenderDifferences = Option(hasGenderDifferences) /* 1 */,
        formsSwitchable = Option(formsSwitchable) /* 1 */,
        growthRate = growthRate.asModel /* 6 */,
        pokedexNumbers = pokedexNumbers.map(_.asModel) /* 5 */,
        eggGroups = eggGroups.map(_.asModel) /* 5 */,
        color = color.asModel /* 6 */,
        shape = shape.asModel /* 6 */,
        evolvesFromSpecies = evolvesFromSpecies.asModel /* 6 */,
        evolutionChain = evolutionChain.asModel /* 6 */,
        habitat = habitat.asModel /* 6 */,
        generation = generation.asModel /* 6 */,
        names = names.map(_.asModel) /* 5 */,
        palParkEncounters = palParkEncounters.map(_.asModel) /* 5 */,
        formDescriptions = formDescriptions.map(_.asModel) /* 5 */,
        flavorTextEntries = flavorTextEntries.map(_.asModel) /* 5 */,
        genera = genera.map(_.asModel) /* 5 */,
        varieties = varieties.map(_.asModel) /* 5 */
    
    )
  }
}

object PokemonSpeciesDetailData {

  def validated(d8a : PokemonSpeciesDetailData, failFast : Boolean) : scala.util.Try[PokemonSpeciesDetail] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : PokemonSpeciesDetailData = try {
        val data = read[PokemonSpeciesDetailData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating PokemonSpeciesDetailData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : PokemonSpeciesDetailData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[PokemonSpeciesDetailData] = try {
        read[List[PokemonSpeciesDetailData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[PokemonSpeciesDetail]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[PokemonSpeciesDetail]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, PokemonSpeciesDetailData] = try {
        read[Map[String, PokemonSpeciesDetailData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, PokemonSpeciesDetail]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, PokemonSpeciesDetail]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

