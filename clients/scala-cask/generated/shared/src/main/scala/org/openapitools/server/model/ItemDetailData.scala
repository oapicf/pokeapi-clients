/**
 * PokéAPI
 * All the Pokémon data you'll ever need in one place, easily accessible through a modern free open-source RESTful API.  ## What is this?  This is a full RESTful API linked to an extensive database detailing everything about the Pokémon main game series.  We've covered everything from Pokémon to Berry Flavors.  ## Where do I start?  We have awesome [documentation](https://pokeapi.co/docs/v2) on how to use this API. It takes minutes to get started.  This API will always be publicly available and will never require any extensive setup process to consume.  Created by [**Paul Hallett**(]https://github.com/phalt) and other [**PokéAPI contributors***](https://github.com/PokeAPI/pokeapi#contributing) around the world. Pokémon and Pokémon character names are trademarks of Nintendo.     
 *
 * OpenAPI spec version: 2.7.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** ItemDetailData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class ItemDetailData(
  id: Int,
  name: String,
  cost: Int = 0 ,
  flingPower: Int = 0 ,
  flingEffect: ItemFlingEffectSummaryData,
  attributes: Seq[AbilityDetailPokemonInnerPokemonData],
  category: ItemCategorySummaryData,
  effectEntries: Seq[ItemEffectTextData],
  flavorTextEntries: Seq[ItemFlavorTextData],
  gameIndices: Seq[ItemGameIndexData],
  names: Seq[ItemNameData],
  heldByPokemon: Seq[ItemDetailHeldByPokemonInnerData],
  sprites: ItemDetailSpritesData,
  babyTriggerFor: ItemDetailBabyTriggerForData,
  machines: Seq[ItemDetailMachinesInnerData]
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== id validation ==================
        
        
        
        

        // ================== name validation ==================
        // validate max length 100
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if name == null then 0 else name.length
            if (len < 100) {
               _allValidationErrors += ValidationError(path :+ ItemDetail.Fields.name, s"length $len is longer than the max length 100")
            }
        }
        
        
        
        

        // ================== cost validation ==================
        
        
        
        

        // ================== flingPower validation ==================
        
        
        
        

        // ================== flingEffect validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (flingEffect == null) {
            _allValidationErrors += ValidationError(path :+ ItemDetail.Fields.flingEffect, "flingEffect is a required field and cannot be null")
            }
        }
        
        
        
        
        // validating flingEffect
        if (_allValidationErrors.isEmpty || !failFast) {
            if flingEffect != null then _allValidationErrors ++= flingEffect.validationErrors(path :+ ItemDetail.Fields.flingEffect, failFast)
        }

        // ================== attributes validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (attributes == null) {
            _allValidationErrors += ValidationError(path :+ ItemDetail.Fields.attributes, "attributes is a required field and cannot be null")
            }
        }
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (attributes != null) {
                attributes.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ ItemDetail.Fields.attributes :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== category validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (category == null) {
            _allValidationErrors += ValidationError(path :+ ItemDetail.Fields.category, "category is a required field and cannot be null")
            }
        }
        
        
        
        
        // validating category
        if (_allValidationErrors.isEmpty || !failFast) {
            if category != null then _allValidationErrors ++= category.validationErrors(path :+ ItemDetail.Fields.category, failFast)
        }

        // ================== effectEntries validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (effectEntries == null) {
            _allValidationErrors += ValidationError(path :+ ItemDetail.Fields.effectEntries, "effectEntries is a required field and cannot be null")
            }
        }
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (effectEntries != null) {
                effectEntries.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ ItemDetail.Fields.effectEntries :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== flavorTextEntries validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (flavorTextEntries == null) {
            _allValidationErrors += ValidationError(path :+ ItemDetail.Fields.flavorTextEntries, "flavorTextEntries is a required field and cannot be null")
            }
        }
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (flavorTextEntries != null) {
                flavorTextEntries.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ ItemDetail.Fields.flavorTextEntries :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== gameIndices validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (gameIndices == null) {
            _allValidationErrors += ValidationError(path :+ ItemDetail.Fields.gameIndices, "gameIndices is a required field and cannot be null")
            }
        }
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (gameIndices != null) {
                gameIndices.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ ItemDetail.Fields.gameIndices :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== names validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (names == null) {
            _allValidationErrors += ValidationError(path :+ ItemDetail.Fields.names, "names is a required field and cannot be null")
            }
        }
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (names != null) {
                names.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ ItemDetail.Fields.names :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== heldByPokemon validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (heldByPokemon == null) {
            _allValidationErrors += ValidationError(path :+ ItemDetail.Fields.heldByPokemon, "heldByPokemon is a required field and cannot be null")
            }
        }
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (heldByPokemon != null) {
                heldByPokemon.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ ItemDetail.Fields.heldByPokemon :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== sprites validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (sprites == null) {
            _allValidationErrors += ValidationError(path :+ ItemDetail.Fields.sprites, "sprites is a required field and cannot be null")
            }
        }
        
        
        
        
        // validating sprites
        if (_allValidationErrors.isEmpty || !failFast) {
            if sprites != null then _allValidationErrors ++= sprites.validationErrors(path :+ ItemDetail.Fields.sprites, failFast)
        }

        // ================== babyTriggerFor validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (babyTriggerFor == null) {
            _allValidationErrors += ValidationError(path :+ ItemDetail.Fields.babyTriggerFor, "babyTriggerFor is a required field and cannot be null")
            }
        }
        
        
        
        
        // validating babyTriggerFor
        if (_allValidationErrors.isEmpty || !failFast) {
            if babyTriggerFor != null then _allValidationErrors ++= babyTriggerFor.validationErrors(path :+ ItemDetail.Fields.babyTriggerFor, failFast)
        }

        // ================== machines validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (machines == null) {
            _allValidationErrors += ValidationError(path :+ ItemDetail.Fields.machines, "machines is a required field and cannot be null")
            }
        }
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (machines != null) {
                machines.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ ItemDetail.Fields.machines :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[ItemDetail] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : ItemDetail = {
    ItemDetail(
        id = id /* 2 */,
        name = name /* 2 */,
        cost = Option(cost) /* 1 */,
        flingPower = Option(flingPower) /* 1 */,
        flingEffect = flingEffect.asModel /* 6 */,
        attributes = attributes.map(_.asModel) /* 5 */,
        category = category.asModel /* 6 */,
        effectEntries = effectEntries.map(_.asModel) /* 5 */,
        flavorTextEntries = flavorTextEntries.map(_.asModel) /* 5 */,
        gameIndices = gameIndices.map(_.asModel) /* 5 */,
        names = names.map(_.asModel) /* 5 */,
        heldByPokemon = heldByPokemon.map(_.asModel) /* 5 */,
        sprites = sprites.asModel /* 6 */,
        babyTriggerFor = babyTriggerFor.asModel /* 6 */,
        machines = machines.map(_.asModel) /* 5 */
    
    )
  }
}

object ItemDetailData {

  def validated(d8a : ItemDetailData, failFast : Boolean) : scala.util.Try[ItemDetail] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : ItemDetailData = try {
        val data = read[ItemDetailData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating ItemDetailData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : ItemDetailData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[ItemDetailData] = try {
        read[List[ItemDetailData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[ItemDetail]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[ItemDetail]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, ItemDetailData] = try {
        read[Map[String, ItemDetailData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, ItemDetail]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, ItemDetail]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

