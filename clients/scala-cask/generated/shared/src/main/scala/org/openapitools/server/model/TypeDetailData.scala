/**
 * PokéAPI
 * All the Pokémon data you'll ever need in one place, easily accessible through a modern free open-source RESTful API.  ## What is this?  This is a full RESTful API linked to an extensive database detailing everything about the Pokémon main game series.  We've covered everything from Pokémon to Berry Flavors.  ## Where do I start?  We have awesome [documentation](https://pokeapi.co/docs/v2) on how to use this API. It takes minutes to get started.  This API will always be publicly available and will never require any extensive setup process to consume.  Created by [**Paul Hallett**(]https://github.com/phalt) and other [**PokéAPI contributors***](https://github.com/PokeAPI/pokeapi#contributing) around the world. Pokémon and Pokémon character names are trademarks of Nintendo.     
 *
 * OpenAPI spec version: 2.7.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** TypeDetailData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class TypeDetailData(
  id: Int,
  name: String,
  damageRelations: TypeDetailDamageRelationsData,
  pastDamageRelations: Seq[TypeDetailPastDamageRelationsInnerData],
  gameIndices: Seq[TypeGameIndexData],
  generation: GenerationSummaryData,
  moveDamageClass: MoveDamageClassSummaryData,
  names: Seq[AbilityNameData],
  pokemon: Seq[TypeDetailPokemonInnerData],
  moves: Seq[MoveSummaryData],
  sprites: Map[String, Map[String, TypeDetailSpritesValueValue]]
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== id validation ==================
        
        
        
        

        // ================== name validation ==================
        // validate max length 100
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if name == null then 0 else name.length
            if (len < 100) {
               _allValidationErrors += ValidationError(path :+ TypeDetail.Fields.name, s"length $len is longer than the max length 100")
            }
        }
        
        
        
        

        // ================== damageRelations validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (damageRelations == null) {
            _allValidationErrors += ValidationError(path :+ TypeDetail.Fields.damageRelations, "damageRelations is a required field and cannot be null")
            }
        }
        
        
        
        
        // validating damageRelations
        if (_allValidationErrors.isEmpty || !failFast) {
            if damageRelations != null then _allValidationErrors ++= damageRelations.validationErrors(path :+ TypeDetail.Fields.damageRelations, failFast)
        }

        // ================== pastDamageRelations validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (pastDamageRelations == null) {
            _allValidationErrors += ValidationError(path :+ TypeDetail.Fields.pastDamageRelations, "pastDamageRelations is a required field and cannot be null")
            }
        }
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (pastDamageRelations != null) {
                pastDamageRelations.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ TypeDetail.Fields.pastDamageRelations :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== gameIndices validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (gameIndices == null) {
            _allValidationErrors += ValidationError(path :+ TypeDetail.Fields.gameIndices, "gameIndices is a required field and cannot be null")
            }
        }
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (gameIndices != null) {
                gameIndices.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ TypeDetail.Fields.gameIndices :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== generation validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (generation == null) {
            _allValidationErrors += ValidationError(path :+ TypeDetail.Fields.generation, "generation is a required field and cannot be null")
            }
        }
        
        
        
        
        // validating generation
        if (_allValidationErrors.isEmpty || !failFast) {
            if generation != null then _allValidationErrors ++= generation.validationErrors(path :+ TypeDetail.Fields.generation, failFast)
        }

        // ================== moveDamageClass validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (moveDamageClass == null) {
            _allValidationErrors += ValidationError(path :+ TypeDetail.Fields.moveDamageClass, "moveDamageClass is a required field and cannot be null")
            }
        }
        
        
        
        
        // validating moveDamageClass
        if (_allValidationErrors.isEmpty || !failFast) {
            if moveDamageClass != null then _allValidationErrors ++= moveDamageClass.validationErrors(path :+ TypeDetail.Fields.moveDamageClass, failFast)
        }

        // ================== names validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (names == null) {
            _allValidationErrors += ValidationError(path :+ TypeDetail.Fields.names, "names is a required field and cannot be null")
            }
        }
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (names != null) {
                names.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ TypeDetail.Fields.names :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== pokemon validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (pokemon == null) {
            _allValidationErrors += ValidationError(path :+ TypeDetail.Fields.pokemon, "pokemon is a required field and cannot be null")
            }
        }
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (pokemon != null) {
                pokemon.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ TypeDetail.Fields.pokemon :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== moves validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (moves == null) {
            _allValidationErrors += ValidationError(path :+ TypeDetail.Fields.moves, "moves is a required field and cannot be null")
            }
        }
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (moves != null) {
                moves.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ TypeDetail.Fields.moves :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== sprites validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (sprites == null) {
            _allValidationErrors += ValidationError(path :+ TypeDetail.Fields.sprites, "sprites is a required field and cannot be null")
            }
        }
        
        
        
        

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[TypeDetail] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : TypeDetail = {
    TypeDetail(
        id = id /* 2 */,
        name = name /* 2 */,
        damageRelations = damageRelations.asModel /* 6 */,
        pastDamageRelations = pastDamageRelations.map(_.asModel) /* 5 */,
        gameIndices = gameIndices.map(_.asModel) /* 5 */,
        generation = generation.asModel /* 6 */,
        moveDamageClass = moveDamageClass.asModel /* 6 */,
        names = names.map(_.asModel) /* 5 */,
        pokemon = pokemon.map(_.asModel) /* 5 */,
        moves = moves.map(_.asModel) /* 5 */,
        sprites = sprites /* 5.1 */
    
    )
  }
}

object TypeDetailData {

  def validated(d8a : TypeDetailData, failFast : Boolean) : scala.util.Try[TypeDetail] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : TypeDetailData = try {
        val data = read[TypeDetailData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating TypeDetailData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : TypeDetailData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[TypeDetailData] = try {
        read[List[TypeDetailData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[TypeDetail]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[TypeDetail]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, TypeDetailData] = try {
        read[Map[String, TypeDetailData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, TypeDetail]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, TypeDetail]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

