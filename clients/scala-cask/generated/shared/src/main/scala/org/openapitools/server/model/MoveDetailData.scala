/**
 * PokéAPI
 * All the Pokémon data you'll ever need in one place, easily accessible through a modern free open-source RESTful API.  ## What is this?  This is a full RESTful API linked to an extensive database detailing everything about the Pokémon main game series.  We've covered everything from Pokémon to Berry Flavors.  ## Where do I start?  We have awesome [documentation](https://pokeapi.co/docs/v2) on how to use this API. It takes minutes to get started.  This API will always be publicly available and will never require any extensive setup process to consume.  Created by [**Paul Hallett**(]https://github.com/phalt) and other [**PokéAPI contributors***](https://github.com/PokeAPI/pokeapi#contributing) around the world. Pokémon and Pokémon character names are trademarks of Nintendo.     
 *
 * OpenAPI spec version: 2.7.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** MoveDetailData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class MoveDetailData(
  id: Int,
  name: String,
  accuracy: Int = 0 ,
  effectChance: Int,
  pp: Int = 0 ,
  priority: Int = 0 ,
  power: Int = 0 ,
  contestCombos: MoveDetailContestCombosData,
  contestType: ContestTypeSummaryData,
  contestEffect: ContestEffectSummaryData,
  damageClass: MoveDamageClassSummaryData,
  effectEntries: Seq[MoveChangeEffectEntriesInnerData],
  effectChanges: Seq[MoveDetailEffectChangesInnerData],
  generation: GenerationSummaryData,
  meta: MoveMetaData,
  names: Seq[MoveNameData],
  pastValues: Seq[MoveChangeData],
  statChanges: Seq[MoveDetailStatChangesInnerData],
  superContestEffect: SuperContestEffectSummaryData,
  target: MoveTargetSummaryData,
  `type`: TypeSummaryData,
  machines: Seq[MoveDetailMachinesInnerData],
  flavorTextEntries: Seq[MoveFlavorTextData],
  learnedByPokemon: Seq[AbilityDetailPokemonInnerPokemonData]
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== id validation ==================
        
        
        
        

        // ================== name validation ==================
        // validate max length 100
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if name == null then 0 else name.length
            if (len < 100) {
               _allValidationErrors += ValidationError(path :+ MoveDetail.Fields.name, s"length $len is longer than the max length 100")
            }
        }
        
        
        
        

        // ================== accuracy validation ==================
        
        
        
        

        // ================== effectChance validation ==================
        
        
        
        

        // ================== pp validation ==================
        
        
        
        

        // ================== priority validation ==================
        
        
        
        

        // ================== power validation ==================
        
        
        
        

        // ================== contestCombos validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (contestCombos == null) {
            _allValidationErrors += ValidationError(path :+ MoveDetail.Fields.contestCombos, "contestCombos is a required field and cannot be null")
            }
        }
        
        
        
        
        // validating contestCombos
        if (_allValidationErrors.isEmpty || !failFast) {
            if contestCombos != null then _allValidationErrors ++= contestCombos.validationErrors(path :+ MoveDetail.Fields.contestCombos, failFast)
        }

        // ================== contestType validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (contestType == null) {
            _allValidationErrors += ValidationError(path :+ MoveDetail.Fields.contestType, "contestType is a required field and cannot be null")
            }
        }
        
        
        
        
        // validating contestType
        if (_allValidationErrors.isEmpty || !failFast) {
            if contestType != null then _allValidationErrors ++= contestType.validationErrors(path :+ MoveDetail.Fields.contestType, failFast)
        }

        // ================== contestEffect validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (contestEffect == null) {
            _allValidationErrors += ValidationError(path :+ MoveDetail.Fields.contestEffect, "contestEffect is a required field and cannot be null")
            }
        }
        
        
        
        
        // validating contestEffect
        if (_allValidationErrors.isEmpty || !failFast) {
            if contestEffect != null then _allValidationErrors ++= contestEffect.validationErrors(path :+ MoveDetail.Fields.contestEffect, failFast)
        }

        // ================== damageClass validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (damageClass == null) {
            _allValidationErrors += ValidationError(path :+ MoveDetail.Fields.damageClass, "damageClass is a required field and cannot be null")
            }
        }
        
        
        
        
        // validating damageClass
        if (_allValidationErrors.isEmpty || !failFast) {
            if damageClass != null then _allValidationErrors ++= damageClass.validationErrors(path :+ MoveDetail.Fields.damageClass, failFast)
        }

        // ================== effectEntries validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (effectEntries == null) {
            _allValidationErrors += ValidationError(path :+ MoveDetail.Fields.effectEntries, "effectEntries is a required field and cannot be null")
            }
        }
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (effectEntries != null) {
                effectEntries.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ MoveDetail.Fields.effectEntries :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== effectChanges validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (effectChanges == null) {
            _allValidationErrors += ValidationError(path :+ MoveDetail.Fields.effectChanges, "effectChanges is a required field and cannot be null")
            }
        }
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (effectChanges != null) {
                effectChanges.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ MoveDetail.Fields.effectChanges :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== generation validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (generation == null) {
            _allValidationErrors += ValidationError(path :+ MoveDetail.Fields.generation, "generation is a required field and cannot be null")
            }
        }
        
        
        
        
        // validating generation
        if (_allValidationErrors.isEmpty || !failFast) {
            if generation != null then _allValidationErrors ++= generation.validationErrors(path :+ MoveDetail.Fields.generation, failFast)
        }

        // ================== meta validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (meta == null) {
            _allValidationErrors += ValidationError(path :+ MoveDetail.Fields.meta, "meta is a required field and cannot be null")
            }
        }
        
        
        
        
        // validating meta
        if (_allValidationErrors.isEmpty || !failFast) {
            if meta != null then _allValidationErrors ++= meta.validationErrors(path :+ MoveDetail.Fields.meta, failFast)
        }

        // ================== names validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (names == null) {
            _allValidationErrors += ValidationError(path :+ MoveDetail.Fields.names, "names is a required field and cannot be null")
            }
        }
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (names != null) {
                names.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ MoveDetail.Fields.names :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== pastValues validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (pastValues == null) {
            _allValidationErrors += ValidationError(path :+ MoveDetail.Fields.pastValues, "pastValues is a required field and cannot be null")
            }
        }
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (pastValues != null) {
                pastValues.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ MoveDetail.Fields.pastValues :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== statChanges validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (statChanges == null) {
            _allValidationErrors += ValidationError(path :+ MoveDetail.Fields.statChanges, "statChanges is a required field and cannot be null")
            }
        }
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (statChanges != null) {
                statChanges.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ MoveDetail.Fields.statChanges :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== superContestEffect validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (superContestEffect == null) {
            _allValidationErrors += ValidationError(path :+ MoveDetail.Fields.superContestEffect, "superContestEffect is a required field and cannot be null")
            }
        }
        
        
        
        
        // validating superContestEffect
        if (_allValidationErrors.isEmpty || !failFast) {
            if superContestEffect != null then _allValidationErrors ++= superContestEffect.validationErrors(path :+ MoveDetail.Fields.superContestEffect, failFast)
        }

        // ================== target validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (target == null) {
            _allValidationErrors += ValidationError(path :+ MoveDetail.Fields.target, "target is a required field and cannot be null")
            }
        }
        
        
        
        
        // validating target
        if (_allValidationErrors.isEmpty || !failFast) {
            if target != null then _allValidationErrors ++= target.validationErrors(path :+ MoveDetail.Fields.target, failFast)
        }

        // ================== `type` validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (`type` == null) {
            _allValidationErrors += ValidationError(path :+ MoveDetail.Fields.`type`, "`type` is a required field and cannot be null")
            }
        }
        
        
        
        
        // validating `type`
        if (_allValidationErrors.isEmpty || !failFast) {
            if `type` != null then _allValidationErrors ++= `type`.validationErrors(path :+ MoveDetail.Fields.`type`, failFast)
        }

        // ================== machines validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (machines == null) {
            _allValidationErrors += ValidationError(path :+ MoveDetail.Fields.machines, "machines is a required field and cannot be null")
            }
        }
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (machines != null) {
                machines.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ MoveDetail.Fields.machines :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== flavorTextEntries validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (flavorTextEntries == null) {
            _allValidationErrors += ValidationError(path :+ MoveDetail.Fields.flavorTextEntries, "flavorTextEntries is a required field and cannot be null")
            }
        }
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (flavorTextEntries != null) {
                flavorTextEntries.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ MoveDetail.Fields.flavorTextEntries :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== learnedByPokemon validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (learnedByPokemon == null) {
            _allValidationErrors += ValidationError(path :+ MoveDetail.Fields.learnedByPokemon, "learnedByPokemon is a required field and cannot be null")
            }
        }
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (learnedByPokemon != null) {
                learnedByPokemon.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ MoveDetail.Fields.learnedByPokemon :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[MoveDetail] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : MoveDetail = {
    MoveDetail(
        id = id /* 2 */,
        name = name /* 2 */,
        accuracy = Option(accuracy) /* 1 */,
        effectChance = effectChance /* 2 */,
        pp = Option(pp) /* 1 */,
        priority = Option(priority) /* 1 */,
        power = Option(power) /* 1 */,
        contestCombos = contestCombos.asModel /* 6 */,
        contestType = contestType.asModel /* 6 */,
        contestEffect = contestEffect.asModel /* 6 */,
        damageClass = damageClass.asModel /* 6 */,
        effectEntries = effectEntries.map(_.asModel) /* 5 */,
        effectChanges = effectChanges.map(_.asModel) /* 5 */,
        generation = generation.asModel /* 6 */,
        meta = meta.asModel /* 6 */,
        names = names.map(_.asModel) /* 5 */,
        pastValues = pastValues.map(_.asModel) /* 5 */,
        statChanges = statChanges.map(_.asModel) /* 5 */,
        superContestEffect = superContestEffect.asModel /* 6 */,
        target = target.asModel /* 6 */,
        `type` = `type`.asModel /* 6 */,
        machines = machines.map(_.asModel) /* 5 */,
        flavorTextEntries = flavorTextEntries.map(_.asModel) /* 5 */,
        learnedByPokemon = learnedByPokemon.map(_.asModel) /* 5 */
    
    )
  }
}

object MoveDetailData {

  def validated(d8a : MoveDetailData, failFast : Boolean) : scala.util.Try[MoveDetail] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : MoveDetailData = try {
        val data = read[MoveDetailData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating MoveDetailData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : MoveDetailData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[MoveDetailData] = try {
        read[List[MoveDetailData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[MoveDetail]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[MoveDetail]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, MoveDetailData] = try {
        read[Map[String, MoveDetailData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, MoveDetail]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, MoveDetail]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

