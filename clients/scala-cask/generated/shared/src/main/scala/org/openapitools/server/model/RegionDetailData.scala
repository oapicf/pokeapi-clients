/**
 * PokéAPI
 * All the Pokémon data you'll ever need in one place, easily accessible through a modern free open-source RESTful API.  ## What is this?  This is a full RESTful API linked to an extensive database detailing everything about the Pokémon main game series.  We've covered everything from Pokémon to Berry Flavors.  ## Where do I start?  We have awesome [documentation](https://pokeapi.co/docs/v2) on how to use this API. It takes minutes to get started.  This API will always be publicly available and will never require any extensive setup process to consume.  Created by [**Paul Hallett**(]https://github.com/phalt) and other [**PokéAPI contributors***](https://github.com/PokeAPI/pokeapi#contributing) around the world. Pokémon and Pokémon character names are trademarks of Nintendo.     
 *
 * OpenAPI spec version: 2.7.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** RegionDetailData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class RegionDetailData(
  id: Int,
  name: String,
  locations: Seq[LocationSummaryData],
  mainGeneration: GenerationSummaryData,
  names: Seq[RegionNameData],
  pokedexes: Seq[PokedexSummaryData],
  versionGroups: Seq[AbilityDetailPokemonInnerPokemonData]
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== id validation ==================
        
        
        
        

        // ================== name validation ==================
        // validate max length 100
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if name == null then 0 else name.length
            if (len < 100) {
               _allValidationErrors += ValidationError(path :+ RegionDetail.Fields.name, s"length $len is longer than the max length 100")
            }
        }
        
        
        
        

        // ================== locations validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (locations == null) {
            _allValidationErrors += ValidationError(path :+ RegionDetail.Fields.locations, "locations is a required field and cannot be null")
            }
        }
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (locations != null) {
                locations.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ RegionDetail.Fields.locations :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== mainGeneration validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (mainGeneration == null) {
            _allValidationErrors += ValidationError(path :+ RegionDetail.Fields.mainGeneration, "mainGeneration is a required field and cannot be null")
            }
        }
        
        
        
        
        // validating mainGeneration
        if (_allValidationErrors.isEmpty || !failFast) {
            if mainGeneration != null then _allValidationErrors ++= mainGeneration.validationErrors(path :+ RegionDetail.Fields.mainGeneration, failFast)
        }

        // ================== names validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (names == null) {
            _allValidationErrors += ValidationError(path :+ RegionDetail.Fields.names, "names is a required field and cannot be null")
            }
        }
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (names != null) {
                names.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ RegionDetail.Fields.names :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== pokedexes validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (pokedexes == null) {
            _allValidationErrors += ValidationError(path :+ RegionDetail.Fields.pokedexes, "pokedexes is a required field and cannot be null")
            }
        }
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (pokedexes != null) {
                pokedexes.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ RegionDetail.Fields.pokedexes :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== versionGroups validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (versionGroups == null) {
            _allValidationErrors += ValidationError(path :+ RegionDetail.Fields.versionGroups, "versionGroups is a required field and cannot be null")
            }
        }
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (versionGroups != null) {
                versionGroups.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ RegionDetail.Fields.versionGroups :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[RegionDetail] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : RegionDetail = {
    RegionDetail(
        id = id /* 2 */,
        name = name /* 2 */,
        locations = locations.map(_.asModel) /* 5 */,
        mainGeneration = mainGeneration.asModel /* 6 */,
        names = names.map(_.asModel) /* 5 */,
        pokedexes = pokedexes.map(_.asModel) /* 5 */,
        versionGroups = versionGroups.map(_.asModel) /* 5 */
    
    )
  }
}

object RegionDetailData {

  def validated(d8a : RegionDetailData, failFast : Boolean) : scala.util.Try[RegionDetail] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : RegionDetailData = try {
        val data = read[RegionDetailData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating RegionDetailData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : RegionDetailData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[RegionDetailData] = try {
        read[List[RegionDetailData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[RegionDetail]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[RegionDetail]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, RegionDetailData] = try {
        read[Map[String, RegionDetailData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, RegionDetail]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, RegionDetail]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

