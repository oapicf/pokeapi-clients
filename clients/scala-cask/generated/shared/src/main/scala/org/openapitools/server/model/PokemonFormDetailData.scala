/**
 * PokéAPI
 * All the Pokémon data you'll ever need in one place, easily accessible through a modern free open-source RESTful API.  ## What is this?  This is a full RESTful API linked to an extensive database detailing everything about the Pokémon main game series.  We've covered everything from Pokémon to Berry Flavors.  ## Where do I start?  We have awesome [documentation](https://pokeapi.co/docs/v2) on how to use this API. It takes minutes to get started.  This API will always be publicly available and will never require any extensive setup process to consume.  Created by [**Paul Hallett**(]https://github.com/phalt) and other [**PokéAPI contributors***](https://github.com/PokeAPI/pokeapi#contributing) around the world. Pokémon and Pokémon character names are trademarks of Nintendo.     
 *
 * OpenAPI spec version: 2.7.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** PokemonFormDetailData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class PokemonFormDetailData(
  id: Int,
  name: String,
  order: Int = 0 ,
  formOrder: Int = 0 ,
  isDefault: Boolean = false ,
  isBattleOnly: Boolean = false ,
  isMega: Boolean = false ,
  formName: String,
  pokemon: PokemonSummaryData,
  sprites: PokemonFormDetailSprites,
  versionGroup: VersionGroupSummaryData,
  formNames: Seq[PokemonFormDetailFormNamesInnerData],
  names: Seq[PokemonFormDetailFormNamesInnerData],
  types: Seq[PokemonDetailTypesInnerData]
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== id validation ==================
        
        
        
        

        // ================== name validation ==================
        // validate max length 100
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if name == null then 0 else name.length
            if (len < 100) {
               _allValidationErrors += ValidationError(path :+ PokemonFormDetail.Fields.name, s"length $len is longer than the max length 100")
            }
        }
        
        
        
        

        // ================== order validation ==================
        
        
        
        

        // ================== formOrder validation ==================
        
        
        
        

        // ================== isDefault validation ==================
        
        
        
        

        // ================== isBattleOnly validation ==================
        
        
        
        

        // ================== isMega validation ==================
        
        
        
        

        // ================== formName validation ==================
        // validate max length 30
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if formName == null then 0 else formName.length
            if (len < 30) {
               _allValidationErrors += ValidationError(path :+ PokemonFormDetail.Fields.formName, s"length $len is longer than the max length 30")
            }
        }
        
        
        
        

        // ================== pokemon validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (pokemon == null) {
            _allValidationErrors += ValidationError(path :+ PokemonFormDetail.Fields.pokemon, "pokemon is a required field and cannot be null")
            }
        }
        
        
        
        
        // validating pokemon
        if (_allValidationErrors.isEmpty || !failFast) {
            if pokemon != null then _allValidationErrors ++= pokemon.validationErrors(path :+ PokemonFormDetail.Fields.pokemon, failFast)
        }

        // ================== sprites validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (sprites == null) {
            _allValidationErrors += ValidationError(path :+ PokemonFormDetail.Fields.sprites, "sprites is a required field and cannot be null")
            }
        }
        
        
        
        

        // ================== versionGroup validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (versionGroup == null) {
            _allValidationErrors += ValidationError(path :+ PokemonFormDetail.Fields.versionGroup, "versionGroup is a required field and cannot be null")
            }
        }
        
        
        
        
        // validating versionGroup
        if (_allValidationErrors.isEmpty || !failFast) {
            if versionGroup != null then _allValidationErrors ++= versionGroup.validationErrors(path :+ PokemonFormDetail.Fields.versionGroup, failFast)
        }

        // ================== formNames validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (formNames == null) {
            _allValidationErrors += ValidationError(path :+ PokemonFormDetail.Fields.formNames, "formNames is a required field and cannot be null")
            }
        }
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (formNames != null) {
                formNames.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ PokemonFormDetail.Fields.formNames :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== names validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (names == null) {
            _allValidationErrors += ValidationError(path :+ PokemonFormDetail.Fields.names, "names is a required field and cannot be null")
            }
        }
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (names != null) {
                names.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ PokemonFormDetail.Fields.names :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== types validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (types == null) {
            _allValidationErrors += ValidationError(path :+ PokemonFormDetail.Fields.types, "types is a required field and cannot be null")
            }
        }
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (types != null) {
                types.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ PokemonFormDetail.Fields.types :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[PokemonFormDetail] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : PokemonFormDetail = {
    PokemonFormDetail(
        id = id /* 2 */,
        name = name /* 2 */,
        order = Option(order) /* 1 */,
        formOrder = Option(formOrder) /* 1 */,
        isDefault = Option(isDefault) /* 1 */,
        isBattleOnly = Option(isBattleOnly) /* 1 */,
        isMega = Option(isMega) /* 1 */,
        formName = formName /* 2 */,
        pokemon = pokemon.asModel /* 6 */,
        sprites = sprites.asModel /* 6 */,
        versionGroup = versionGroup.asModel /* 6 */,
        formNames = formNames.map(_.asModel) /* 5 */,
        names = names.map(_.asModel) /* 5 */,
        types = types.map(_.asModel) /* 5 */
    
    )
  }
}

object PokemonFormDetailData {

  def validated(d8a : PokemonFormDetailData, failFast : Boolean) : scala.util.Try[PokemonFormDetail] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : PokemonFormDetailData = try {
        val data = read[PokemonFormDetailData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating PokemonFormDetailData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : PokemonFormDetailData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[PokemonFormDetailData] = try {
        read[List[PokemonFormDetailData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[PokemonFormDetail]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[PokemonFormDetail]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, PokemonFormDetailData] = try {
        read[Map[String, PokemonFormDetailData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, PokemonFormDetail]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, PokemonFormDetail]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

