/**
 * PokéAPI
 * All the Pokémon data you'll ever need in one place, easily accessible through a modern free open-source RESTful API.  ## What is this?  This is a full RESTful API linked to an extensive database detailing everything about the Pokémon main game series.  We've covered everything from Pokémon to Berry Flavors.  ## Where do I start?  We have awesome [documentation](https://pokeapi.co/docs/v2) on how to use this API. It takes minutes to get started.  This API will always be publicly available and will never require any extensive setup process to consume.  Created by [**Paul Hallett**(]https://github.com/phalt) and other [**PokéAPI contributors***](https://github.com/PokeAPI/pokeapi#contributing) around the world. Pokémon and Pokémon character names are trademarks of Nintendo.     
 *
 * OpenAPI spec version: 2.7.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** VersionGroupDetailData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class VersionGroupDetailData(
  id: Int,
  name: String,
  order: Int = 0 ,
  generation: GenerationSummaryData,
  moveLearnMethods: Seq[AbilityDetailPokemonInnerPokemonData],
  pokedexes: Seq[AbilityDetailPokemonInnerPokemonData],
  regions: Seq[AbilityDetailPokemonInnerPokemonData],
  versions: Seq[VersionSummaryData]
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== id validation ==================
        
        
        
        

        // ================== name validation ==================
        // validate max length 100
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if name == null then 0 else name.length
            if (len < 100) {
               _allValidationErrors += ValidationError(path :+ VersionGroupDetail.Fields.name, s"length $len is longer than the max length 100")
            }
        }
        
        
        
        

        // ================== order validation ==================
        
        
        
        

        // ================== generation validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (generation == null) {
            _allValidationErrors += ValidationError(path :+ VersionGroupDetail.Fields.generation, "generation is a required field and cannot be null")
            }
        }
        
        
        
        
        // validating generation
        if (_allValidationErrors.isEmpty || !failFast) {
            if generation != null then _allValidationErrors ++= generation.validationErrors(path :+ VersionGroupDetail.Fields.generation, failFast)
        }

        // ================== moveLearnMethods validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (moveLearnMethods == null) {
            _allValidationErrors += ValidationError(path :+ VersionGroupDetail.Fields.moveLearnMethods, "moveLearnMethods is a required field and cannot be null")
            }
        }
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (moveLearnMethods != null) {
                moveLearnMethods.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ VersionGroupDetail.Fields.moveLearnMethods :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== pokedexes validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (pokedexes == null) {
            _allValidationErrors += ValidationError(path :+ VersionGroupDetail.Fields.pokedexes, "pokedexes is a required field and cannot be null")
            }
        }
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (pokedexes != null) {
                pokedexes.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ VersionGroupDetail.Fields.pokedexes :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== regions validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (regions == null) {
            _allValidationErrors += ValidationError(path :+ VersionGroupDetail.Fields.regions, "regions is a required field and cannot be null")
            }
        }
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (regions != null) {
                regions.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ VersionGroupDetail.Fields.regions :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== versions validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (versions == null) {
            _allValidationErrors += ValidationError(path :+ VersionGroupDetail.Fields.versions, "versions is a required field and cannot be null")
            }
        }
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (versions != null) {
                versions.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ VersionGroupDetail.Fields.versions :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[VersionGroupDetail] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : VersionGroupDetail = {
    VersionGroupDetail(
        id = id /* 2 */,
        name = name /* 2 */,
        order = Option(order) /* 1 */,
        generation = generation.asModel /* 6 */,
        moveLearnMethods = moveLearnMethods.map(_.asModel) /* 5 */,
        pokedexes = pokedexes.map(_.asModel) /* 5 */,
        regions = regions.map(_.asModel) /* 5 */,
        versions = versions.map(_.asModel) /* 5 */
    
    )
  }
}

object VersionGroupDetailData {

  def validated(d8a : VersionGroupDetailData, failFast : Boolean) : scala.util.Try[VersionGroupDetail] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : VersionGroupDetailData = try {
        val data = read[VersionGroupDetailData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating VersionGroupDetailData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : VersionGroupDetailData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[VersionGroupDetailData] = try {
        read[List[VersionGroupDetailData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[VersionGroupDetail]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[VersionGroupDetail]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, VersionGroupDetailData] = try {
        read[Map[String, VersionGroupDetailData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, VersionGroupDetail]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, VersionGroupDetail]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

