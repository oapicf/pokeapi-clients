//> using scala "3.3.1"
//> using lib "com.lihaoyi::cask:0.8.3"
//> using lib "com.lihaoyi::scalatags:0.12.0"
/**
 * PokéAPI
 * All the Pokémon data you'll ever need in one place, easily accessible through a modern free open-source RESTful API.  ## What is this?  This is a full RESTful API linked to an extensive database detailing everything about the Pokémon main game series.  We've covered everything from Pokémon to Berry Flavors.  ## Where do I start?  We have awesome [documentation](https://pokeapi.co/docs/v2) on how to use this API. It takes minutes to get started.  This API will always be publicly available and will never require any extensive setup process to consume.  Created by [**Paul Hallett**(]https://github.com/phalt) and other [**PokéAPI contributors***](https://github.com/PokeAPI/pokeapi#contributing) around the world. Pokémon and Pokémon character names are trademarks of Nintendo.     
 *
 * OpenAPI spec version: 2.7.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */


// generated from apiService.mustache
package org.openapitools.server.api

import _root_.org.openapitools.server.model.LanguageDetail
import _root_.org.openapitools.server.model.PaginatedLanguageSummaryList
import scala.util.Failure
import scala.util.Try
import _root_.org.openapitools.server.model.*

/**
 * The UtilityService companion object.
 *
 * Use the UtilityService() companion object to create an instance which returns a 'not implemented' error
 * for each operation.
 *
 */
object UtilityService {

  /**
   * The 'Handler' is an implementation of UtilityService convenient for delegating or overriding individual functions
   */
  case class Handler[F[_]](
        languageListHandler : (limit : Option[Int], offset : Option[Int], q : Option[String]) => F[PaginatedLanguageSummaryList], 
        languageRetrieveHandler : (id : String) => F[LanguageDetail]
  ) extends UtilityService[F] {

        override def languageList(limit : Option[Int], offset : Option[Int], q : Option[String]) : F[PaginatedLanguageSummaryList] = {
          languageListHandler(limit, offset, q)
        }

        override def languageRetrieve(id : String) : F[LanguageDetail] = {
          languageRetrieveHandler(id)
        }
  }

  def apply() : UtilityService[Try] = UtilityService.Handler[Try](
        (_, _, _) => notImplemented("languageList"), 
        (_) => notImplemented("languageRetrieve")
  )

  private def notImplemented(name : String) = Failure(new Exception(s"TODO: $name not implemented"))
}

/**
 * The Utility business-logic
 *
 *
 * The 'asHandler' will return an implementation which allows for easily overriding individual operations.
 *
 * equally there are "on&lt;Function&gt;" helper methods for easily overriding individual functions
 *
 * @tparam F the effect type (Future, Try, IO, ID, etc) of the operations
 */
trait UtilityService[F[_]] {
  /** List languages
   * 
   * @return PaginatedLanguageSummaryList
   */
  def languageList(limit : Option[Int], offset : Option[Int], q : Option[String]) : F[PaginatedLanguageSummaryList]

  /**
   * override languageList with the given handler
   * @return a new implementation of UtilityService[F] with languageList overridden using the given handler
   */
  final def onLanguageList(handler : (limit : Option[Int], offset : Option[Int], q : Option[String]) => F[PaginatedLanguageSummaryList]) : UtilityService[F] = {
    asHandler.copy(languageListHandler = handler)
  }
  /** Get language
   * 
   * @return LanguageDetail
   */
  def languageRetrieve(id : String) : F[LanguageDetail]

  /**
   * override languageRetrieve with the given handler
   * @return a new implementation of UtilityService[F] with languageRetrieve overridden using the given handler
   */
  final def onLanguageRetrieve(handler : (id : String) => F[LanguageDetail]) : UtilityService[F] = {
    asHandler.copy(languageRetrieveHandler = handler)
  }

  /**
   * @return a Handler implementation of this service
   */
  final def asHandler : UtilityService.Handler[F] = this match {
      case h : UtilityService.Handler[F] => h
      case _ =>
        UtilityService.Handler[F](
              (limit, offset, q) => languageList(limit, offset, q), 
              (id) => languageRetrieve(id)
        )
  }

  /**
   * This function will change the effect type of this service.
   *
   * It's not unlike a typical map operation from A => B, except we're not mapping
   * a type from A to B, but rather from F[A] => G[A] using the 'changeEffect' function.
   *
   * For, this could turn an asynchronous service (one which returns Future[_] types) into
   * a synchronous one (one which returns Try[_] types) by awaiting on the Future.
   *
   * It could change an IO type (like cats effect or ZIO) into an ID[A] which is just:
   * ```
   * type ID[A] => A
   * ```
   *
   * @tparam G the new "polymorphic" effect type
   * @param changeEffect the "natural transformation" which can change one effect type into another
   * @return a new UtilityService service implementation with effect type [G]
   */
  final def mapEffect[G[_]](changeEffect : [A] => F[A] => G[A]) : UtilityService[G] = {
    val self = this

    new UtilityService[G] {
        override def languageList(limit : Option[Int], offset : Option[Int], q : Option[String]) : G[PaginatedLanguageSummaryList] = changeEffect {
          self.languageList(limit, offset, q)
        }
        override def languageRetrieve(id : String) : G[LanguageDetail] = changeEffect {
          self.languageRetrieve(id)
        }
    }
  }
}
