/**
 * PokéAPI
 * All the Pokémon data you'll ever need in one place, easily accessible through a modern free open-source RESTful API.  ## What is this?  This is a full RESTful API linked to an extensive database detailing everything about the Pokémon main game series.  We've covered everything from Pokémon to Berry Flavors.  ## Where do I start?  We have awesome [documentation](https://pokeapi.co/docs/v2) on how to use this API. It takes minutes to get started.  This API will always be publicly available and will never require any extensive setup process to consume.  Created by [**Paul Hallett**(]https://github.com/phalt) and other [**PokéAPI contributors***](https://github.com/PokeAPI/pokeapi#contributing) around the world. Pokémon and Pokémon character names are trademarks of Nintendo.     
 *
 * OpenAPI spec version: 2.7.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** PokemonDetailData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class PokemonDetailData(
  id: Int,
  name: String,
  baseExperience: Int = 0 ,
  height: Int = 0 ,
  isDefault: Boolean = false ,
  order: Int = 0 ,
  weight: Int = 0 ,
  abilities: Seq[PokemonDetailAbilitiesInnerData],
  pastAbilities: Seq[PokemonDetailPastAbilitiesInnerData],
  forms: Seq[PokemonFormSummaryData],
  gameIndices: Seq[PokemonGameIndexData],
  heldItems: PokemonDetailHeldItemsData,
  locationAreaEncounters: String,
  moves: Seq[PokemonDetailMovesInnerData],
  species: PokemonSpeciesSummaryData,
  sprites: PokemonDetailSprites,
  cries: PokemonDetailCriesData,
  stats: Seq[PokemonStatData],
  types: Seq[PokemonDetailTypesInnerData],
  pastTypes: Seq[PokemonDetailPastTypesInnerData]
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== id validation ==================
        
        
        
        

        // ================== name validation ==================
        // validate max length 100
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if name == null then 0 else name.length
            if (len < 100) {
               _allValidationErrors += ValidationError(path :+ PokemonDetail.Fields.name, s"length $len is longer than the max length 100")
            }
        }
        
        
        
        

        // ================== baseExperience validation ==================
        
        
        
        

        // ================== height validation ==================
        
        
        
        

        // ================== isDefault validation ==================
        
        
        
        

        // ================== order validation ==================
        
        
        
        

        // ================== weight validation ==================
        
        
        
        

        // ================== abilities validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (abilities == null) {
            _allValidationErrors += ValidationError(path :+ PokemonDetail.Fields.abilities, "abilities is a required field and cannot be null")
            }
        }
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (abilities != null) {
                abilities.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ PokemonDetail.Fields.abilities :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== pastAbilities validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (pastAbilities == null) {
            _allValidationErrors += ValidationError(path :+ PokemonDetail.Fields.pastAbilities, "pastAbilities is a required field and cannot be null")
            }
        }
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (pastAbilities != null) {
                pastAbilities.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ PokemonDetail.Fields.pastAbilities :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== forms validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (forms == null) {
            _allValidationErrors += ValidationError(path :+ PokemonDetail.Fields.forms, "forms is a required field and cannot be null")
            }
        }
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (forms != null) {
                forms.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ PokemonDetail.Fields.forms :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== gameIndices validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (gameIndices == null) {
            _allValidationErrors += ValidationError(path :+ PokemonDetail.Fields.gameIndices, "gameIndices is a required field and cannot be null")
            }
        }
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (gameIndices != null) {
                gameIndices.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ PokemonDetail.Fields.gameIndices :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== heldItems validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (heldItems == null) {
            _allValidationErrors += ValidationError(path :+ PokemonDetail.Fields.heldItems, "heldItems is a required field and cannot be null")
            }
        }
        
        
        
        
        // validating heldItems
        if (_allValidationErrors.isEmpty || !failFast) {
            if heldItems != null then _allValidationErrors ++= heldItems.validationErrors(path :+ PokemonDetail.Fields.heldItems, failFast)
        }

        // ================== locationAreaEncounters validation ==================
        
        
        
        

        // ================== moves validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (moves == null) {
            _allValidationErrors += ValidationError(path :+ PokemonDetail.Fields.moves, "moves is a required field and cannot be null")
            }
        }
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (moves != null) {
                moves.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ PokemonDetail.Fields.moves :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== species validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (species == null) {
            _allValidationErrors += ValidationError(path :+ PokemonDetail.Fields.species, "species is a required field and cannot be null")
            }
        }
        
        
        
        
        // validating species
        if (_allValidationErrors.isEmpty || !failFast) {
            if species != null then _allValidationErrors ++= species.validationErrors(path :+ PokemonDetail.Fields.species, failFast)
        }

        // ================== sprites validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (sprites == null) {
            _allValidationErrors += ValidationError(path :+ PokemonDetail.Fields.sprites, "sprites is a required field and cannot be null")
            }
        }
        
        
        
        

        // ================== cries validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (cries == null) {
            _allValidationErrors += ValidationError(path :+ PokemonDetail.Fields.cries, "cries is a required field and cannot be null")
            }
        }
        
        
        
        
        // validating cries
        if (_allValidationErrors.isEmpty || !failFast) {
            if cries != null then _allValidationErrors ++= cries.validationErrors(path :+ PokemonDetail.Fields.cries, failFast)
        }

        // ================== stats validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (stats == null) {
            _allValidationErrors += ValidationError(path :+ PokemonDetail.Fields.stats, "stats is a required field and cannot be null")
            }
        }
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (stats != null) {
                stats.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ PokemonDetail.Fields.stats :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== types validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (types == null) {
            _allValidationErrors += ValidationError(path :+ PokemonDetail.Fields.types, "types is a required field and cannot be null")
            }
        }
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (types != null) {
                types.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ PokemonDetail.Fields.types :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== pastTypes validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (pastTypes == null) {
            _allValidationErrors += ValidationError(path :+ PokemonDetail.Fields.pastTypes, "pastTypes is a required field and cannot be null")
            }
        }
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (pastTypes != null) {
                pastTypes.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ PokemonDetail.Fields.pastTypes :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[PokemonDetail] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : PokemonDetail = {
    PokemonDetail(
        id = id /* 2 */,
        name = name /* 2 */,
        baseExperience = Option(baseExperience) /* 1 */,
        height = Option(height) /* 1 */,
        isDefault = Option(isDefault) /* 1 */,
        order = Option(order) /* 1 */,
        weight = Option(weight) /* 1 */,
        abilities = abilities.map(_.asModel) /* 5 */,
        pastAbilities = pastAbilities.map(_.asModel) /* 5 */,
        forms = forms.map(_.asModel) /* 5 */,
        gameIndices = gameIndices.map(_.asModel) /* 5 */,
        heldItems = heldItems.asModel /* 6 */,
        locationAreaEncounters = locationAreaEncounters /* 2 */,
        moves = moves.map(_.asModel) /* 5 */,
        species = species.asModel /* 6 */,
        sprites = sprites.asModel /* 6 */,
        cries = cries.asModel /* 6 */,
        stats = stats.map(_.asModel) /* 5 */,
        types = types.map(_.asModel) /* 5 */,
        pastTypes = pastTypes.map(_.asModel) /* 5 */
    
    )
  }
}

object PokemonDetailData {

  def validated(d8a : PokemonDetailData, failFast : Boolean) : scala.util.Try[PokemonDetail] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : PokemonDetailData = try {
        val data = read[PokemonDetailData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating PokemonDetailData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : PokemonDetailData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[PokemonDetailData] = try {
        read[List[PokemonDetailData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[PokemonDetail]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[PokemonDetail]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, PokemonDetailData] = try {
        read[Map[String, PokemonDetailData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, PokemonDetail]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, PokemonDetail]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

