/**
 * PokéAPI
 * All the Pokémon data you'll ever need in one place, easily accessible through a modern free open-source RESTful API.  ## What is this?  This is a full RESTful API linked to an extensive database detailing everything about the Pokémon main game series.  We've covered everything from Pokémon to Berry Flavors.  ## Where do I start?  We have awesome [documentation](https://pokeapi.co/docs/v2) on how to use this API. It takes minutes to get started.  This API will always be publicly available and will never require any extensive setup process to consume.  Created by [**Paul Hallett**(]https://github.com/phalt) and other [**PokéAPI contributors***](https://github.com/PokeAPI/pokeapi#contributing) around the world. Pokémon and Pokémon character names are trademarks of Nintendo.     
 *
 * OpenAPI spec version: 2.7.0
 *
 * Contact: blah+oapicf@cliffano.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 *
 * https://openapi-generator.tech
 */

// this model was generated using modelData.mustache
package org.openapitools.server.model
import scala.util.control.NonFatal
import scala.util.*

// see https://com-lihaoyi.github.io/upickle/
import upickle.default.{ReadWriter => RW, macroRW}
import upickle.default.*


        /** NatureDetailData a data transfer object, primarily for simple json serialisation.
  * It has no validation - there may be nulls, values out of range, etc
  */
case class NatureDetailData(
  id: Int,
  name: String,
  decreasedStat: StatSummaryData,
  increasedStat: StatSummaryData,
  likesFlavor: BerryFlavorSummaryData,
  hatesFlavor: BerryFlavorSummaryData,
  berries: Seq[BerrySummaryData],
  pokeathlonStatChanges: Seq[NatureDetailPokeathlonStatChangesInnerData],
  moveBattleStylePreferences: Seq[NatureBattleStylePreferenceData],
  names: Seq[NatureNameData]
  

) derives RW {

  def asJsonString: String = asJson.toString()

  def asJson : ujson.Value = {
    val jason = writeJs(this)
    jason
  }

  def validationErrors(path : Seq[Field], failFast : Boolean) : Seq[ValidationError] = {
    val _allValidationErrors = scala.collection.mutable.ListBuffer[ValidationError]()
        // ================== id validation ==================
        
        
        
        

        // ================== name validation ==================
        // validate max length 100
        if (_allValidationErrors.isEmpty || !failFast)  {
          val len = if name == null then 0 else name.length
            if (len < 100) {
               _allValidationErrors += ValidationError(path :+ NatureDetail.Fields.name, s"length $len is longer than the max length 100")
            }
        }
        
        
        
        

        // ================== decreasedStat validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (decreasedStat == null) {
            _allValidationErrors += ValidationError(path :+ NatureDetail.Fields.decreasedStat, "decreasedStat is a required field and cannot be null")
            }
        }
        
        
        
        
        // validating decreasedStat
        if (_allValidationErrors.isEmpty || !failFast) {
            if decreasedStat != null then _allValidationErrors ++= decreasedStat.validationErrors(path :+ NatureDetail.Fields.decreasedStat, failFast)
        }

        // ================== increasedStat validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (increasedStat == null) {
            _allValidationErrors += ValidationError(path :+ NatureDetail.Fields.increasedStat, "increasedStat is a required field and cannot be null")
            }
        }
        
        
        
        
        // validating increasedStat
        if (_allValidationErrors.isEmpty || !failFast) {
            if increasedStat != null then _allValidationErrors ++= increasedStat.validationErrors(path :+ NatureDetail.Fields.increasedStat, failFast)
        }

        // ================== likesFlavor validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (likesFlavor == null) {
            _allValidationErrors += ValidationError(path :+ NatureDetail.Fields.likesFlavor, "likesFlavor is a required field and cannot be null")
            }
        }
        
        
        
        
        // validating likesFlavor
        if (_allValidationErrors.isEmpty || !failFast) {
            if likesFlavor != null then _allValidationErrors ++= likesFlavor.validationErrors(path :+ NatureDetail.Fields.likesFlavor, failFast)
        }

        // ================== hatesFlavor validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (hatesFlavor == null) {
            _allValidationErrors += ValidationError(path :+ NatureDetail.Fields.hatesFlavor, "hatesFlavor is a required field and cannot be null")
            }
        }
        
        
        
        
        // validating hatesFlavor
        if (_allValidationErrors.isEmpty || !failFast) {
            if hatesFlavor != null then _allValidationErrors ++= hatesFlavor.validationErrors(path :+ NatureDetail.Fields.hatesFlavor, failFast)
        }

        // ================== berries validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (berries == null) {
            _allValidationErrors += ValidationError(path :+ NatureDetail.Fields.berries, "berries is a required field and cannot be null")
            }
        }
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (berries != null) {
                berries.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ NatureDetail.Fields.berries :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== pokeathlonStatChanges validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (pokeathlonStatChanges == null) {
            _allValidationErrors += ValidationError(path :+ NatureDetail.Fields.pokeathlonStatChanges, "pokeathlonStatChanges is a required field and cannot be null")
            }
        }
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (pokeathlonStatChanges != null) {
                pokeathlonStatChanges.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ NatureDetail.Fields.pokeathlonStatChanges :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== moveBattleStylePreferences validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (moveBattleStylePreferences == null) {
            _allValidationErrors += ValidationError(path :+ NatureDetail.Fields.moveBattleStylePreferences, "moveBattleStylePreferences is a required field and cannot be null")
            }
        }
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (moveBattleStylePreferences != null) {
                moveBattleStylePreferences.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ NatureDetail.Fields.moveBattleStylePreferences :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

        // ================== names validation ==================
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (names == null) {
            _allValidationErrors += ValidationError(path :+ NatureDetail.Fields.names, "names is a required field and cannot be null")
            }
        }
        
        
        
        
        if (_allValidationErrors.isEmpty || !failFast) {
            if (names != null) {
                names.zipWithIndex.foreach {
                    case (value, i) if _allValidationErrors.isEmpty || !failFast =>
                      _allValidationErrors ++= value.validationErrors(
                        path :+ NatureDetail.Fields.names :+ Field(i.toString),
                        failFast)
                    case (value, i) =>
                }
            }
        }
        

    _allValidationErrors.toSeq
  }

  /**
   * @return the validated model within a Try (if successful)
   */
  def validated(failFast : Boolean = false) : scala.util.Try[NatureDetail] = {
    validationErrors(Vector(), failFast) match {
      case Seq() => Success(asModel)
      case first +: theRest => Failure(ValidationErrors(first, theRest))
    }
  }

  /** use 'validated' to check validation */
  def asModel : NatureDetail = {
    NatureDetail(
        id = id /* 2 */,
        name = name /* 2 */,
        decreasedStat = decreasedStat.asModel /* 6 */,
        increasedStat = increasedStat.asModel /* 6 */,
        likesFlavor = likesFlavor.asModel /* 6 */,
        hatesFlavor = hatesFlavor.asModel /* 6 */,
        berries = berries.map(_.asModel) /* 5 */,
        pokeathlonStatChanges = pokeathlonStatChanges.map(_.asModel) /* 5 */,
        moveBattleStylePreferences = moveBattleStylePreferences.map(_.asModel) /* 5 */,
        names = names.map(_.asModel) /* 5 */
    
    )
  }
}

object NatureDetailData {

  def validated(d8a : NatureDetailData, failFast : Boolean) : scala.util.Try[NatureDetail] = d8a.validated(failFast)

  def fromJson(jason : ujson.Value) : NatureDetailData = try {
        val data = read[NatureDetailData](jason)
        data
    } catch {
      case NonFatal(e) => sys.error(s"Error creating NatureDetailData from json '$jason': $e")
  }

  def fromJsonString(jason : String) : NatureDetailData = {
        val parsed = try {
           read[ujson.Value](jason)
        } catch {
          case NonFatal(e) => sys.error(s"Error parsing json '$jason': $e")
        }
        fromJson(parsed)
  }

  def manyFromJsonString(jason : String) : Seq[NatureDetailData] = try {
        read[List[NatureDetailData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as list: $e")
    }

  def manyFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Seq[NatureDetail]] = {
      Try(manyFromJsonString(jason)).flatMap { list =>
        list.zipWithIndex.foldLeft(Try(Vector[NatureDetail]())) {
          case (Success(list), (next, i)) => 
            next.validated(failFast) match {
              case Success(ok) => Success(list :+ ok)
              case Failure(err) => Failure(new Exception(s"Validation error on element $i: ${err.getMessage}", err))
            }
          case (fail, _)  => fail
        }
      }
    }

  def mapFromJsonString(jason : String) : Map[String, NatureDetailData] = try {
        read[Map[String, NatureDetailData]](jason)
    } catch {
        case NonFatal(e) => sys.error(s"Error parsing json '$jason' as map: $e")
    }


  def mapFromJsonStringValidated(jason : String, failFast : Boolean = false) : Try[Map[String, NatureDetail]] = {
     Try(mapFromJsonString(jason)).flatMap { map =>
       map.foldLeft(Try(Map[String, NatureDetail]())) {
         case (Success(map), (key, next)) =>
           next.validated(failFast) match {
             case Success(ok) => Success(map.updated(key, ok))
             case Failure(err) => Failure(new Exception(s"Validation error on element $key: ${err.getMessage}", err))
           }
         case (fail, _) => fail
       }
     }
  }
}

