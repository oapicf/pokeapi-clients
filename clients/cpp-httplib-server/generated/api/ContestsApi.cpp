/**
* This file is auto generated by OpenAPI Generator (https://openapi-generator.tech).
* https://openapi-generator.tech
* Do not edit the class manually.
*/

// System headers
#include <algorithm>
#include <cctype>
#include <locale>

// Project headers
#include "ContestsApi.h"
#include "AuthenticationManager.h"

constexpr int HTTP_RESPONSE_CODE_PAGINATED_CONTEST_EFFECT_SUMMARY_LIST = 200;
constexpr int HTTP_RESPONSE_CODE_CONTEST_EFFECT_DETAIL = 200;
constexpr int HTTP_RESPONSE_CODE_PAGINATED_CONTEST_TYPE_SUMMARY_LIST = 200;
constexpr int HTTP_RESPONSE_CODE_CONTEST_TYPE_DETAIL = 200;
constexpr int HTTP_RESPONSE_CODE_PAGINATED_SUPER_CONTEST_EFFECT_SUMMARY_LIST = 200;
constexpr int HTTP_RESPONSE_CODE_SUPER_CONTEST_EFFECT_DETAIL = 200;
constexpr int HTTP_RESPONSE_CODE_BAD_REQUEST = 400;
constexpr int HTTP_RESPONSE_CODE_UNAUTHORIZED = 401;
constexpr int HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR = 500;

namespace Api {

using namespace models;

bool Contests::parseApiv2contestEffectGetParams(const httplib::Request& req, Contests::Apiv2contestEffectGetRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;

    // Query Parameters - limit
    if (req.has_param("limit"))
    {
        try
        {
            params.m_limit = std::stoi(req.get_param_value("limit"));
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'limit': " + std::string(e.what()));
        }
    }
    else
    {
        // Use default value for optional parameter
        params.m_limit = 0;
    }
    // Query Parameters - offset
    if (req.has_param("offset"))
    {
        try
        {
            params.m_offset = std::stoi(req.get_param_value("offset"));
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'offset': " + std::string(e.what()));
        }
    }
    else
    {
        // Use default value for optional parameter
        params.m_offset = 0;
    }
    // Query Parameters - q
    if (req.has_param("q"))
    {
        params.m_q = req.get_param_value("q");
    }
    else
    {
        // Use default value for optional parameter
        params.m_q = "";
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void Contests::handleApiv2contestEffectGetResponse(const Apiv2contestEffectGetResponse& result, httplib::Response& res)
{
    // Single response type
    res.status = HTTP_RESPONSE_CODE_PAGINATED_CONTEST_EFFECT_SUMMARY_LIST;
    nlohmann::json responseJson;
    to_json(responseJson, result);
    res.set_content(responseJson.dump(), "application/json");
}
bool Contests::parseApiv2contestEffectidGetParams(const httplib::Request& req, Contests::Apiv2contestEffectidGetRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;

    // Path Parameters - id (index: 1)
    if (req.matches.size() < 1 + 1)
    {
        errors.push_back("Missing path parameter 'id'");
    }
    else
    {
        try
        {
            params.m_id = req.matches[1];
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid path parameter 'id': " + std::string(e.what()));
        }
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void Contests::handleApiv2contestEffectidGetResponse(const Apiv2contestEffectidGetResponse& result, httplib::Response& res)
{
    // Single response type
    res.status = HTTP_RESPONSE_CODE_CONTEST_EFFECT_DETAIL;
    nlohmann::json responseJson;
    to_json(responseJson, result);
    res.set_content(responseJson.dump(), "application/json");
}
bool Contests::parseApiv2contestTypeGetParams(const httplib::Request& req, Contests::Apiv2contestTypeGetRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;

    // Query Parameters - limit
    if (req.has_param("limit"))
    {
        try
        {
            params.m_limit = std::stoi(req.get_param_value("limit"));
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'limit': " + std::string(e.what()));
        }
    }
    else
    {
        // Use default value for optional parameter
        params.m_limit = 0;
    }
    // Query Parameters - offset
    if (req.has_param("offset"))
    {
        try
        {
            params.m_offset = std::stoi(req.get_param_value("offset"));
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'offset': " + std::string(e.what()));
        }
    }
    else
    {
        // Use default value for optional parameter
        params.m_offset = 0;
    }
    // Query Parameters - q
    if (req.has_param("q"))
    {
        params.m_q = req.get_param_value("q");
    }
    else
    {
        // Use default value for optional parameter
        params.m_q = "";
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void Contests::handleApiv2contestTypeGetResponse(const Apiv2contestTypeGetResponse& result, httplib::Response& res)
{
    // Single response type
    res.status = HTTP_RESPONSE_CODE_PAGINATED_CONTEST_TYPE_SUMMARY_LIST;
    nlohmann::json responseJson;
    to_json(responseJson, result);
    res.set_content(responseJson.dump(), "application/json");
}
bool Contests::parseApiv2contestTypeidGetParams(const httplib::Request& req, Contests::Apiv2contestTypeidGetRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;

    // Path Parameters - id (index: 1)
    if (req.matches.size() < 1 + 1)
    {
        errors.push_back("Missing path parameter 'id'");
    }
    else
    {
        try
        {
            params.m_id = req.matches[1];
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid path parameter 'id': " + std::string(e.what()));
        }
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void Contests::handleApiv2contestTypeidGetResponse(const Apiv2contestTypeidGetResponse& result, httplib::Response& res)
{
    // Single response type
    res.status = HTTP_RESPONSE_CODE_CONTEST_TYPE_DETAIL;
    nlohmann::json responseJson;
    to_json(responseJson, result);
    res.set_content(responseJson.dump(), "application/json");
}
bool Contests::parseApiv2superContestEffectGetParams(const httplib::Request& req, Contests::Apiv2superContestEffectGetRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;

    // Query Parameters - limit
    if (req.has_param("limit"))
    {
        try
        {
            params.m_limit = std::stoi(req.get_param_value("limit"));
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'limit': " + std::string(e.what()));
        }
    }
    else
    {
        // Use default value for optional parameter
        params.m_limit = 0;
    }
    // Query Parameters - offset
    if (req.has_param("offset"))
    {
        try
        {
            params.m_offset = std::stoi(req.get_param_value("offset"));
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'offset': " + std::string(e.what()));
        }
    }
    else
    {
        // Use default value for optional parameter
        params.m_offset = 0;
    }
    // Query Parameters - q
    if (req.has_param("q"))
    {
        params.m_q = req.get_param_value("q");
    }
    else
    {
        // Use default value for optional parameter
        params.m_q = "";
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void Contests::handleApiv2superContestEffectGetResponse(const Apiv2superContestEffectGetResponse& result, httplib::Response& res)
{
    // Single response type
    res.status = HTTP_RESPONSE_CODE_PAGINATED_SUPER_CONTEST_EFFECT_SUMMARY_LIST;
    nlohmann::json responseJson;
    to_json(responseJson, result);
    res.set_content(responseJson.dump(), "application/json");
}
bool Contests::parseApiv2superContestEffectidGetParams(const httplib::Request& req, Contests::Apiv2superContestEffectidGetRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;

    // Path Parameters - id (index: 1)
    if (req.matches.size() < 1 + 1)
    {
        errors.push_back("Missing path parameter 'id'");
    }
    else
    {
        try
        {
            params.m_id = req.matches[1];
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid path parameter 'id': " + std::string(e.what()));
        }
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void Contests::handleApiv2superContestEffectidGetResponse(const Apiv2superContestEffectidGetResponse& result, httplib::Response& res)
{
    // Single response type
    res.status = HTTP_RESPONSE_CODE_SUPER_CONTEST_EFFECT_DETAIL;
    nlohmann::json responseJson;
    to_json(responseJson, result);
    res.set_content(responseJson.dump(), "application/json");
}

bool Contests::performAuthentication(
    const httplib::Request& req,
    std::shared_ptr<AuthenticationManager> auth,
    httplib::Response& res)
{
    if (!auth)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "AuthenticationManager not configured";
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
        return false;
    }

    {
        auto cookieHeader = req.get_header_value("Cookie");
        std::string key = "sessionid=";
        size_t start = cookieHeader.find(key);
        if (start != std::string::npos)
        {
            start += key.length();
            size_t end = cookieHeader.find(";", start);
            if (end == std::string::npos) end = cookieHeader.length();
            std::string cookieValue = cookieHeader.substr(start, end - start);
            if (auth->validateApiKey(cookieValue)) return true;
        }
    }

    if (req.has_header("Authorization"))
    {
        auto authHeader = req.get_header_value("Authorization");
        if (authHeader.find("Basic ") == 0)
        {
            std::string credentials = authHeader.substr(6);
            if (auth->validateBasicAuth(credentials, "")) return true;
        }
    }



    return false;
}

void Contests::handleApiv2contestEffectGetRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        Apiv2contestEffectGetRequest params;
        std::vector<std::string> paramErrors;
        if (!parseApiv2contestEffectGetParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_BAD_REQUEST;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handleGetForApiv2contestEffect(params);
        handleApiv2contestEffectGetResponse(result, res);

    }
    catch (const std::exception& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Internal error: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}

void Contests::handleApiv2contestEffectidGetRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        Apiv2contestEffectidGetRequest params;
        std::vector<std::string> paramErrors;
        if (!parseApiv2contestEffectidGetParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_BAD_REQUEST;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handleGetForApiv2contestEffectid(params);
        handleApiv2contestEffectidGetResponse(result, res);

    }
    catch (const std::exception& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Internal error: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}

void Contests::handleApiv2contestTypeGetRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        Apiv2contestTypeGetRequest params;
        std::vector<std::string> paramErrors;
        if (!parseApiv2contestTypeGetParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_BAD_REQUEST;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handleGetForApiv2contestType(params);
        handleApiv2contestTypeGetResponse(result, res);

    }
    catch (const std::exception& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Internal error: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}

void Contests::handleApiv2contestTypeidGetRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        Apiv2contestTypeidGetRequest params;
        std::vector<std::string> paramErrors;
        if (!parseApiv2contestTypeidGetParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_BAD_REQUEST;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handleGetForApiv2contestTypeid(params);
        handleApiv2contestTypeidGetResponse(result, res);

    }
    catch (const std::exception& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Internal error: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}

void Contests::handleApiv2superContestEffectGetRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        Apiv2superContestEffectGetRequest params;
        std::vector<std::string> paramErrors;
        if (!parseApiv2superContestEffectGetParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_BAD_REQUEST;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handleGetForApiv2superContestEffect(params);
        handleApiv2superContestEffectGetResponse(result, res);

    }
    catch (const std::exception& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Internal error: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}

void Contests::handleApiv2superContestEffectidGetRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        Apiv2superContestEffectidGetRequest params;
        std::vector<std::string> paramErrors;
        if (!parseApiv2superContestEffectidGetParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_BAD_REQUEST;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handleGetForApiv2superContestEffectid(params);
        handleApiv2superContestEffectidGetResponse(result, res);

    }
    catch (const std::exception& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Internal error: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}


void Contests::registerRoutes(httplib::Server& svr, std::shared_ptr<AuthenticationManager> auth)
{
    svr.Get("/api/v2/contest-effect/", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleApiv2contestEffectGetRequest(req, res, auth);
    });
    svr.Get("/api/v2/contest-effect/{id}/", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleApiv2contestEffectidGetRequest(req, res, auth);
    });
    svr.Get("/api/v2/contest-type/", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleApiv2contestTypeGetRequest(req, res, auth);
    });
    svr.Get("/api/v2/contest-type/{id}/", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleApiv2contestTypeidGetRequest(req, res, auth);
    });
    svr.Get("/api/v2/super-contest-effect/", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleApiv2superContestEffectGetRequest(req, res, auth);
    });
    svr.Get("/api/v2/super-contest-effect/{id}/", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleApiv2superContestEffectidGetRequest(req, res, auth);
    });
}

} // namespace Api