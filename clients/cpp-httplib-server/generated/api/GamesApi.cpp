/**
* This file is auto generated by OpenAPI Generator (https://openapi-generator.tech).
* https://openapi-generator.tech
* Do not edit the class manually.
*/

// System headers
#include <algorithm>
#include <cctype>
#include <locale>

// Project headers
#include "GamesApi.h"
#include "AuthenticationManager.h"

constexpr int HTTP_RESPONSE_CODE_PAGINATED_GENERATION_SUMMARY_LIST = 200;
constexpr int HTTP_RESPONSE_CODE_GENERATION_DETAIL = 200;
constexpr int HTTP_RESPONSE_CODE_PAGINATED_POKEDEX_SUMMARY_LIST = 200;
constexpr int HTTP_RESPONSE_CODE_POKEDEX_DETAIL = 200;
constexpr int HTTP_RESPONSE_CODE_PAGINATED_VERSION_GROUP_SUMMARY_LIST = 200;
constexpr int HTTP_RESPONSE_CODE_VERSION_GROUP_DETAIL = 200;
constexpr int HTTP_RESPONSE_CODE_PAGINATED_VERSION_SUMMARY_LIST = 200;
constexpr int HTTP_RESPONSE_CODE_VERSION_DETAIL = 200;
constexpr int HTTP_RESPONSE_CODE_BAD_REQUEST = 400;
constexpr int HTTP_RESPONSE_CODE_UNAUTHORIZED = 401;
constexpr int HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR = 500;

namespace Api {

using namespace models;

bool Games::parseApiv2generationGetParams(const httplib::Request& req, Games::Apiv2generationGetRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;

    // Query Parameters - limit
    if (req.has_param("limit"))
    {
        try
        {
            params.m_limit = std::stoi(req.get_param_value("limit"));
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'limit': " + std::string(e.what()));
        }
    }
    else
    {
        // Use default value for optional parameter
        params.m_limit = 0;
    }
    // Query Parameters - offset
    if (req.has_param("offset"))
    {
        try
        {
            params.m_offset = std::stoi(req.get_param_value("offset"));
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'offset': " + std::string(e.what()));
        }
    }
    else
    {
        // Use default value for optional parameter
        params.m_offset = 0;
    }
    // Query Parameters - q
    if (req.has_param("q"))
    {
        params.m_q = req.get_param_value("q");
    }
    else
    {
        // Use default value for optional parameter
        params.m_q = "";
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void Games::handleApiv2generationGetResponse(const Apiv2generationGetResponse& result, httplib::Response& res)
{
    // Single response type
    res.status = HTTP_RESPONSE_CODE_PAGINATED_GENERATION_SUMMARY_LIST;
    nlohmann::json responseJson;
    to_json(responseJson, result);
    res.set_content(responseJson.dump(), "application/json");
}
bool Games::parseApiv2generationidGetParams(const httplib::Request& req, Games::Apiv2generationidGetRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;

    // Path Parameters - id (index: 1)
    if (req.matches.size() < 1 + 1)
    {
        errors.push_back("Missing path parameter 'id'");
    }
    else
    {
        try
        {
            params.m_id = req.matches[1];
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid path parameter 'id': " + std::string(e.what()));
        }
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void Games::handleApiv2generationidGetResponse(const Apiv2generationidGetResponse& result, httplib::Response& res)
{
    // Single response type
    res.status = HTTP_RESPONSE_CODE_GENERATION_DETAIL;
    nlohmann::json responseJson;
    to_json(responseJson, result);
    res.set_content(responseJson.dump(), "application/json");
}
bool Games::parseApiv2pokedexGetParams(const httplib::Request& req, Games::Apiv2pokedexGetRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;

    // Query Parameters - limit
    if (req.has_param("limit"))
    {
        try
        {
            params.m_limit = std::stoi(req.get_param_value("limit"));
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'limit': " + std::string(e.what()));
        }
    }
    else
    {
        // Use default value for optional parameter
        params.m_limit = 0;
    }
    // Query Parameters - offset
    if (req.has_param("offset"))
    {
        try
        {
            params.m_offset = std::stoi(req.get_param_value("offset"));
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'offset': " + std::string(e.what()));
        }
    }
    else
    {
        // Use default value for optional parameter
        params.m_offset = 0;
    }
    // Query Parameters - q
    if (req.has_param("q"))
    {
        params.m_q = req.get_param_value("q");
    }
    else
    {
        // Use default value for optional parameter
        params.m_q = "";
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void Games::handleApiv2pokedexGetResponse(const Apiv2pokedexGetResponse& result, httplib::Response& res)
{
    // Single response type
    res.status = HTTP_RESPONSE_CODE_PAGINATED_POKEDEX_SUMMARY_LIST;
    nlohmann::json responseJson;
    to_json(responseJson, result);
    res.set_content(responseJson.dump(), "application/json");
}
bool Games::parseApiv2pokedexidGetParams(const httplib::Request& req, Games::Apiv2pokedexidGetRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;

    // Path Parameters - id (index: 1)
    if (req.matches.size() < 1 + 1)
    {
        errors.push_back("Missing path parameter 'id'");
    }
    else
    {
        try
        {
            params.m_id = req.matches[1];
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid path parameter 'id': " + std::string(e.what()));
        }
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void Games::handleApiv2pokedexidGetResponse(const Apiv2pokedexidGetResponse& result, httplib::Response& res)
{
    // Single response type
    res.status = HTTP_RESPONSE_CODE_POKEDEX_DETAIL;
    nlohmann::json responseJson;
    to_json(responseJson, result);
    res.set_content(responseJson.dump(), "application/json");
}
bool Games::parseApiv2versionGroupGetParams(const httplib::Request& req, Games::Apiv2versionGroupGetRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;

    // Query Parameters - limit
    if (req.has_param("limit"))
    {
        try
        {
            params.m_limit = std::stoi(req.get_param_value("limit"));
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'limit': " + std::string(e.what()));
        }
    }
    else
    {
        // Use default value for optional parameter
        params.m_limit = 0;
    }
    // Query Parameters - offset
    if (req.has_param("offset"))
    {
        try
        {
            params.m_offset = std::stoi(req.get_param_value("offset"));
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'offset': " + std::string(e.what()));
        }
    }
    else
    {
        // Use default value for optional parameter
        params.m_offset = 0;
    }
    // Query Parameters - q
    if (req.has_param("q"))
    {
        params.m_q = req.get_param_value("q");
    }
    else
    {
        // Use default value for optional parameter
        params.m_q = "";
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void Games::handleApiv2versionGroupGetResponse(const Apiv2versionGroupGetResponse& result, httplib::Response& res)
{
    // Single response type
    res.status = HTTP_RESPONSE_CODE_PAGINATED_VERSION_GROUP_SUMMARY_LIST;
    nlohmann::json responseJson;
    to_json(responseJson, result);
    res.set_content(responseJson.dump(), "application/json");
}
bool Games::parseApiv2versionGroupidGetParams(const httplib::Request& req, Games::Apiv2versionGroupidGetRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;

    // Path Parameters - id (index: 1)
    if (req.matches.size() < 1 + 1)
    {
        errors.push_back("Missing path parameter 'id'");
    }
    else
    {
        try
        {
            params.m_id = req.matches[1];
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid path parameter 'id': " + std::string(e.what()));
        }
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void Games::handleApiv2versionGroupidGetResponse(const Apiv2versionGroupidGetResponse& result, httplib::Response& res)
{
    // Single response type
    res.status = HTTP_RESPONSE_CODE_VERSION_GROUP_DETAIL;
    nlohmann::json responseJson;
    to_json(responseJson, result);
    res.set_content(responseJson.dump(), "application/json");
}
bool Games::parseApiv2versionGetParams(const httplib::Request& req, Games::Apiv2versionGetRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;

    // Query Parameters - limit
    if (req.has_param("limit"))
    {
        try
        {
            params.m_limit = std::stoi(req.get_param_value("limit"));
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'limit': " + std::string(e.what()));
        }
    }
    else
    {
        // Use default value for optional parameter
        params.m_limit = 0;
    }
    // Query Parameters - offset
    if (req.has_param("offset"))
    {
        try
        {
            params.m_offset = std::stoi(req.get_param_value("offset"));
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid query parameter 'offset': " + std::string(e.what()));
        }
    }
    else
    {
        // Use default value for optional parameter
        params.m_offset = 0;
    }
    // Query Parameters - q
    if (req.has_param("q"))
    {
        params.m_q = req.get_param_value("q");
    }
    else
    {
        // Use default value for optional parameter
        params.m_q = "";
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void Games::handleApiv2versionGetResponse(const Apiv2versionGetResponse& result, httplib::Response& res)
{
    // Single response type
    res.status = HTTP_RESPONSE_CODE_PAGINATED_VERSION_SUMMARY_LIST;
    nlohmann::json responseJson;
    to_json(responseJson, result);
    res.set_content(responseJson.dump(), "application/json");
}
bool Games::parseApiv2versionidGetParams(const httplib::Request& req, Games::Apiv2versionidGetRequest& params, std::vector<std::string>& paramErrors)
{
    std::vector<std::string> errors;

    // Path Parameters - id (index: 1)
    if (req.matches.size() < 1 + 1)
    {
        errors.push_back("Missing path parameter 'id'");
    }
    else
    {
        try
        {
            params.m_id = req.matches[1];
        }
        catch (const std::exception& e)
        {
            errors.push_back("Invalid path parameter 'id': " + std::string(e.what()));
        }
    }

    // Return errors via out-parameter, return false if any errors
    if (!errors.empty())
    {
        paramErrors = std::move(errors);
        return false;
    }
    return true;
}
void Games::handleApiv2versionidGetResponse(const Apiv2versionidGetResponse& result, httplib::Response& res)
{
    // Single response type
    res.status = HTTP_RESPONSE_CODE_VERSION_DETAIL;
    nlohmann::json responseJson;
    to_json(responseJson, result);
    res.set_content(responseJson.dump(), "application/json");
}

bool Games::performAuthentication(
    const httplib::Request& req,
    std::shared_ptr<AuthenticationManager> auth,
    httplib::Response& res)
{
    if (!auth)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "AuthenticationManager not configured";
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
        return false;
    }

    {
        auto cookieHeader = req.get_header_value("Cookie");
        std::string key = "sessionid=";
        size_t start = cookieHeader.find(key);
        if (start != std::string::npos)
        {
            start += key.length();
            size_t end = cookieHeader.find(";", start);
            if (end == std::string::npos) end = cookieHeader.length();
            std::string cookieValue = cookieHeader.substr(start, end - start);
            if (auth->validateApiKey(cookieValue)) return true;
        }
    }

    if (req.has_header("Authorization"))
    {
        auto authHeader = req.get_header_value("Authorization");
        if (authHeader.find("Basic ") == 0)
        {
            std::string credentials = authHeader.substr(6);
            if (auth->validateBasicAuth(credentials, "")) return true;
        }
    }



    return false;
}

void Games::handleApiv2generationGetRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        Apiv2generationGetRequest params;
        std::vector<std::string> paramErrors;
        if (!parseApiv2generationGetParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_BAD_REQUEST;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handleGetForApiv2generation(params);
        handleApiv2generationGetResponse(result, res);

    }
    catch (const std::exception& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Internal error: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}

void Games::handleApiv2generationidGetRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        Apiv2generationidGetRequest params;
        std::vector<std::string> paramErrors;
        if (!parseApiv2generationidGetParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_BAD_REQUEST;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handleGetForApiv2generationid(params);
        handleApiv2generationidGetResponse(result, res);

    }
    catch (const std::exception& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Internal error: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}

void Games::handleApiv2pokedexGetRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        Apiv2pokedexGetRequest params;
        std::vector<std::string> paramErrors;
        if (!parseApiv2pokedexGetParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_BAD_REQUEST;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handleGetForApiv2pokedex(params);
        handleApiv2pokedexGetResponse(result, res);

    }
    catch (const std::exception& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Internal error: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}

void Games::handleApiv2pokedexidGetRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        Apiv2pokedexidGetRequest params;
        std::vector<std::string> paramErrors;
        if (!parseApiv2pokedexidGetParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_BAD_REQUEST;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handleGetForApiv2pokedexid(params);
        handleApiv2pokedexidGetResponse(result, res);

    }
    catch (const std::exception& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Internal error: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}

void Games::handleApiv2versionGroupGetRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        Apiv2versionGroupGetRequest params;
        std::vector<std::string> paramErrors;
        if (!parseApiv2versionGroupGetParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_BAD_REQUEST;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handleGetForApiv2versionGroup(params);
        handleApiv2versionGroupGetResponse(result, res);

    }
    catch (const std::exception& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Internal error: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}

void Games::handleApiv2versionGroupidGetRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        Apiv2versionGroupidGetRequest params;
        std::vector<std::string> paramErrors;
        if (!parseApiv2versionGroupidGetParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_BAD_REQUEST;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handleGetForApiv2versionGroupid(params);
        handleApiv2versionGroupidGetResponse(result, res);

    }
    catch (const std::exception& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Internal error: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}

void Games::handleApiv2versionGetRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        Apiv2versionGetRequest params;
        std::vector<std::string> paramErrors;
        if (!parseApiv2versionGetParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_BAD_REQUEST;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handleGetForApiv2version(params);
        handleApiv2versionGetResponse(result, res);

    }
    catch (const std::exception& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Internal error: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}

void Games::handleApiv2versionidGetRequest([[maybe_unused]] const httplib::Request& req, httplib::Response& res, std::shared_ptr<AuthenticationManager> auth)
{
    try
    {
        if (!performAuthentication(req, auth, res))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Authentication required";
            res.status = HTTP_RESPONSE_CODE_UNAUTHORIZED;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }

        Apiv2versionidGetRequest params;
        std::vector<std::string> paramErrors;
        if (!parseApiv2versionidGetParams(req, params, paramErrors))
        {
            nlohmann::json errorJson = nlohmann::json::object();
            errorJson["message"] = "Invalid parameters";
            errorJson["errors"] = paramErrors;
            res.status = HTTP_RESPONSE_CODE_BAD_REQUEST;
            res.set_content(errorJson.dump(), "application/json");
            return;
        }
        auto result = handleGetForApiv2versionid(params);
        handleApiv2versionidGetResponse(result, res);

    }
    catch (const std::exception& e)
    {
        nlohmann::json errorJson = nlohmann::json::object();
        errorJson["message"] = "Internal error: " + std::string(e.what());
        res.status = HTTP_RESPONSE_CODE_INTERNAL_SERVER_ERROR;
        res.set_content(errorJson.dump(), "application/json");
    }
}


void Games::registerRoutes(httplib::Server& svr, std::shared_ptr<AuthenticationManager> auth)
{
    svr.Get("/api/v2/generation/", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleApiv2generationGetRequest(req, res, auth);
    });
    svr.Get("/api/v2/generation/{id}/", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleApiv2generationidGetRequest(req, res, auth);
    });
    svr.Get("/api/v2/pokedex/", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleApiv2pokedexGetRequest(req, res, auth);
    });
    svr.Get("/api/v2/pokedex/{id}/", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleApiv2pokedexidGetRequest(req, res, auth);
    });
    svr.Get("/api/v2/version-group/", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleApiv2versionGroupGetRequest(req, res, auth);
    });
    svr.Get("/api/v2/version-group/{id}/", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleApiv2versionGroupidGetRequest(req, res, auth);
    });
    svr.Get("/api/v2/version/", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleApiv2versionGetRequest(req, res, auth);
    });
    svr.Get("/api/v2/version/{id}/", [this, auth]([[maybe_unused]] const httplib::Request& req, httplib::Response& res)
    {
        handleApiv2versionidGetRequest(req, res, auth);
    });
}

} // namespace Api